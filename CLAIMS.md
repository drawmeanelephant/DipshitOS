# Active work

One row per active piece of work. Add a row (🔜) when you start, flip to 🔄 while in progress, move to Completed (✅) when done. No ceremony.

| Status | Owner | Team | Scope | Branch |
|--------|-------|------|-------|--------|
| 🔜 | Agent 1 | Shell/Process | P4: Exit status propagation — `$?` variable in shell (issue #293) | `agent/p4-exit-status` |
| 🔜 | Agent 2 | Desktop | G19: Crash recovery for apps — tombstone + window cleanup + notification on EL0 fault (issue #462) | `agent/g19-crash-recovery` |
| 🔄 | Agent 3 | Net | N1: PING.BIN — ICMP echo with RTT stats (issue #399, Lane D-NetApps, new `user/src/ping.zig`, zero collision with `shell.zig`/`notepad.zig`) | `agent/ping-n1` |
