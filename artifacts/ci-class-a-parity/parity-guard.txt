class-A parity (issue #1186) -- captured evidence
host: macOS 27.0 / GNU bash, version 5.3.15(1)-release (aarch64-apple-darwin25.4.0)

== 1. the guard reproduces the drift it exists for: HEAD's ci.yml, before the fix ==
$ CI_YML=/tmp/ci-before.yml bash tools/lint-workflows.sh   # /tmp/ci-before.yml = 'git show HEAD:.github/workflows/ci.yml'
lint-workflows: class-A parity FAILED -- 3 of 18 command(s) of 'just verify-portable' are NOT run by /tmp/ci-before.yml:
    - bash tools/inventory-gates.sh --check
    - bash tools/verify-ttf-fonts.sh
    - bash tools/verify-vf-class-a.sh
  Add the missing step(s) to /tmp/ci-before.yml: a gate that only runs locally cannot fail a merge.
rc=1

== 2. teeth: remove ONE step from the fixed ci.yml ==
lint-workflows: class-A parity FAILED -- 1 of 18 command(s) of 'just verify-portable' are NOT run by /tmp/ci-missing-one.yml:
    - bash tools/verify-ttf-fonts.sh
  Add the missing step(s) to /tmp/ci-missing-one.yml: a gate that only runs locally cannot fail a merge.
rc=1

== 3. at the branch head: actionlint + parity, both green ==
lint-workflows: class-A parity OK -- all 18 command(s) of 'just verify-portable' are run by /Users/tbuddy/t3/zig/virelaios-buffy/.github/workflows/ci.yml
rc=0
