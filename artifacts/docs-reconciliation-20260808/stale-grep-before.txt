# BEFORE (main fff37a5)
--- device absence ---
/tmp/status-before.md:72:| **VZ serial gate** | `zig build run` | ❌ **not passed** | serial still silent; marker ladder re-observed 2026-08-07 reaching `M2_SERIA` — device absence, not a crash (`artifacts/m2-marker-reverify-20260807.txt`; original 2026-08-06 run: `artifacts/m2-vz-run-20260806.txt`) |
/tmp/status-before.md:161:on **device absence in the declared windows**, not on a kernel crash.
/tmp/status-before.md:163:**The device absence is now fully explained (claim 0013, 2026-08-07).** The
--- needs re-scoping ---
/tmp/status-before.md:267:- [ ] `ls`, `cat`, and `write` persist through reboot — **needs re-scoping**: post-exit there is no ESP root / Simple File System (x3 carries handoff v2), so these need a pre-exit file window or a storage driver; see march step 15 (`docs/march-m15.md`).
--- which one wins.*unobserved ---
/tmp/status-before.md:303:  which one wins on VZ is still **unobserved** (the VZ run gate is blocked).
--- M2_SERIA.*before the monitor ---
/tmp/march-before.md:37:| 17 | **Add machine controls.** `reboot`, `shutdown`, `halt` via the Runtime Services table captured pre-exit (`ResetSystem` survives ExitBootServices) or a documented fallback (WFE loop / VM teardown). | The session can end intentionally. | ✅ (real mechanism, unit-proven; live gate ⛔ open) | `reboot`/`shutdown` now issue **real EFI Runtime Services `ResetSystem` calls** — cold for `reboot`, shutdown for `shutdown`, status 0, null data — from `kernel/src/machine.zig` (claim 0011, `agent/buffy/m15-machine-controls`), wired through the `MachineControl` seam; the runtime-services pointer is captured pre-exit (`machine.init`) and the claim-0009 NVRAM channel persists the `M2_RST!` stage immediately before the call. Arg construction is **unit-proven** (reboot→cold, shutdown→shutdown, no-pointer→honest `not_implemented`); if a firmware returns anyway the kernel parks in WFE rather than running off. **Live observation stays blocked**: the VZ serial gate (claim 0002) is still unpassed — the kernel halts at `M2_SERIA` before the monitor runs, so `ResetSystem` is unreachable on VZ this cycle and no reset/M2_RST! is observed (evidence `artifacts/m15-machine-*.txt`). Hard gate 6 flips only on an observed live reset. |
--- halts at M2_SERIA ---
 (0 hits)
--- What we directly observe about the two failing gates ---
/tmp/status-before.md:64:### What we directly observe about the two failing gates
--- kernel halts at ---
/tmp/march-before.md:37:| 17 | **Add machine controls.** `reboot`, `shutdown`, `halt` via the Runtime Services table captured pre-exit (`ResetSystem` survives ExitBootServices) or a documented fallback (WFE loop / VM teardown). | The session can end intentionally. | ✅ (real mechanism, unit-proven; live gate ⛔ open) | `reboot`/`shutdown` now issue **real EFI Runtime Services `ResetSystem` calls** — cold for `reboot`, shutdown for `shutdown`, status 0, null data — from `kernel/src/machine.zig` (claim 0011, `agent/buffy/m15-machine-controls`), wired through the `MachineControl` seam; the runtime-services pointer is captured pre-exit (`machine.init`) and the claim-0009 NVRAM channel persists the `M2_RST!` stage immediately before the call. Arg construction is **unit-proven** (reboot→cold, shutdown→shutdown, no-pointer→honest `not_implemented`); if a firmware returns anyway the kernel parks in WFE rather than running off. **Live observation stays blocked**: the VZ serial gate (claim 0002) is still unpassed — the kernel halts at `M2_SERIA` before the monitor runs, so `ResetSystem` is unreachable on VZ this cycle and no reset/M2_RST! is observed (evidence `artifacts/m15-machine-*.txt`). Hard gate 6 flips only on an observed live reset. |
