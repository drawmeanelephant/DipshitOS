# Log — agent/buffy/m5-net-tx

- **2026-08-11** — *buffy (agent/buffy/m5-net-tx)*: claim 1373 (milestone five, card N1 — virtio-net transport + TX, `docs/m5-net-tx-prompt.md`) → branched `net-tx` from merged main `bc310ef` (m4-processes tag verified at `9d7e4d5`, an ancestor) → completed the 2026-08-11 DID-correction doc propagation that was left uncommitted on `agent/buffy/m4-closeout` (hardware-contract disk bullet + claim-6420 log entry, commit 7606d00; the status.md copy was already in main) so the tree is clean → claim file `docs/claims/1373-net-tx.md` + this log → 🟡 claimed, class A implementation next (runner `--net` flag, `kernel/src/virtio_net.zig`, `net`/`netsend` monitor commands, host tests).
- **2026-08-11** — *buffy (agent/buffy/m5-net-tx)*: class A landed (runner `--net <capture>` + socketpair capture in `main.swift`; `kernel/src/virtio_net.zig` with injectable transport ops; `net`/`netsend` registry 32→34; shell help + byte-identical transcript fixture; 19 driver + 230 monitor tests; fmt/build/image/inspect/swift/context/coordination ×2/mmu-debt all green) → **live VZ probe found the transport FAILED `FEATURES_OK` with a minimal VER1 mask** (status readback 0x03; offered `0x30010028/0x5`) → root-caused empirically: the net device **requires `VIRTIO_NET_F_MTU` accepted** (a negotiated-mask ladder: VER1-only and VER1|MAC rejected, VER1|MTU|MAC accepted — `feat=0x28/0x1`), the host-set MAC is readable via the feature path (`mac=02:00:00:00:00:01 source=feature`, cfgmac agrees), the device **does NOT reset at ExitBootServices** (`net: pre-rearm st=0f` — unlike blk/entropy's st=00), and **every TX buffer must carry a 12-byte virtio_net_hdr** (the first gate stripped exactly 12 bytes; `tx_hdr_len` corrected 0→12, driver prepends a zeroed header) → **`tools/verify-live-net-tx.sh` PASS 2/2** (phase 1: `net` report + host capture byte-exactly the 46-byte known frame; phase 2: 46+46+1514 — ring reuse + honest truncation, frames=3) → **full 29-gate `verify-vz` aggregate re-ran green** (proof the `--net` mode left the default VM byte-identical; sweep log `artifacts/live-net-tx-vz-sweep.log`) → docs reconciled (march-m5.md tracker, roadmap network flip + surface-table Network row, status.md milestone-five row + gate row + afterward item, gate-inventory live-net-tx row + aggregates, hardware-contract `[observed]` net bullet with the saved logs, README, architecture) → ✅ claim flipped, PR opened.
