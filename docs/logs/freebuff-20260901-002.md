# Log — freebuff/okay-shower-thoughts-here-we-re-using-fat32-for-so-b0ce3067-2b04-4b81-984d-9fd76bfcf123

- **2026-09-01** — *buffy (freebuff/okay-shower-thoughts-here-we-re-using-fat32-for-so-b0ce3067-2b04-4b81-984d-9fd76bfcf123)*: claim 2293 in progress. **WASM milestone review sync (second review pass).** Adjudicated the ten-point review of M35 (W1–W5, #762–#766): **selectively ignored** the status.md claim (verified stale — M32/M33/M34 rows all exist on main; no "There is no M32" text anywhere) and the `--cvc-file` blocking worry (moot — HF4 landed via PR #769, the share is the stable delivery path). **Applied:** split W1 → **W1a contract freeze (#778, docs-only, parallel-safe)** + **W1b interpreter (#762 re-scoped)** so the import-contract review doesn't stall the core; memory bound frozen (**2 MiB / 32 pages, trap on `memory.grow` beyond** — no unbounded mmap); **Go wasm deferred to post-M35 (option b)** — W2's gate uses only the 684-B `zig cc` fixture; **W5 capstone frozen as `wc`** (integer-only, byte-exact counts via the file channel) so the import list is complete up front and W5 doesn't force floats; W4 floats tied to a **named C float utility** (not wc); the contract artifact named `docs/wasm-import-contract.md`; W2 primary path = `exec WASM.BIN <file>` (HF4 app delivery) with the `wasm run` monitor command deferred (drops `kernel/src/monitor.zig` from required Touches); BSS before/after + `tests/wasm-corpus/` fixtures started in W1b (the zc 8-KiB-wall lesson). Updated all five issue bodies + `docs/wasm-core-scoping.md` (header, card table, memory row, Go decision) + the line-of-sight W-row (6 open). The board is now W1a #778 + W1b #762 + W2–W5 #763–#766 under milestone #22.
