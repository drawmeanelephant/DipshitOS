# Log — agent/buffy/wms9-dsk1-drawing-apps (WMS9/DSK1 drawing-app fix)

- **2026-08-31** — *buffy (agent/buffy/wms9-dsk1-drawing-apps)*: claimed claim 0688 and root-caused the pre-existing `verify-live-wm-ipc`/`verify-live-ps`/`verify-live-sys-kill` regression class on main (also verified pre-existing on a clean `origin/main` baseline in the freebuff session's wm-ipc investigation). **Root cause:** `a2df6a0` (WMS9 span batching, 2026-08-30 13:41 — one day after the 08-29 WMS7 Gate-A PASS) introduced the mutable `fill_batcher` global in `user/src/lib/ui.zig` and routed every toolkit draw through it. DSK1 flat images map text+rodata+.data+.bss as ONE read-only W^X text aperture, so the first batched fill writes a read-only page — NOTEPAD data-aborted at its bss tail (`fault: NOTEPAD.BIN far=0x403d98 ec=0x24`, byte-identical on un-renamed `origin/main`). **Fix (the EDIT/GLOBALS/WND precedent):** (1) `build.zig` — calc/chat/desktop/file_browser/notepad/ps/settings_panel/sysmon/top → DSK3 segmented (`--segments` + `linker-segmented.ld`); (2) `image/make-image.sh` — those 9 magic checks DSK1→DSK3 (comments document the observed data-abort); (3) `kernel/src/exec.zig` — DSK3 argv (the claim-3805 "later card"): argv packs into a RESERVED DATA TAIL after the app's bss (data aperture + task uaccess regions grow by arg_block_bytes), keeping NOTEPAD's `selfdemo` argv working (the claim-4636 read-only-argv property stays DSK1-specific). **Verified live:** `verify-live-wm-ipc` **PASS** (mail_raise/config/ack_raise/config/focused/rect/wm_present all 1; `wnd: mail kind=1/2 applied=yes`, `wmrpc: raise-ack/config-ack applied=yes`, focused=2, rect=40,40,360,260), `verify-live-ps` **PASS**, `verify-live-sys-kill` **PASS**; `zig build` + image + fmt + full unit suite green; rebuilt BINs report DSK3 (NOTEPAD text=16384 bss tail=784). ✅
