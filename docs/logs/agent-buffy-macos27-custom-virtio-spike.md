# Log — `agent/buffy/macos27-custom-virtio-spike`

Append-only. See [`README.md`](README.md) for the convention.

- **2026-08-09** — *buffy*: claim 5844 filed at `a5888f1`; scope is the macOS 27 host requirement, the custom-virtio spike device, and the live `pci` command. SDK audit (Xcode 27 beta 4, SDK 27.0) shows no host-triggered guest-interrupt API exists in Virtualization.framework — the spike proves discovery only. · 🔄 in progress
- **2026-08-09** — *buffy*: claim 5844 complete — macOS 27+ is now the enforced host requirement (runner guard `>= 27`, `Package.swift` `.macOS(.v27)` at tools-version 6.4, docs updated), `zig build spike-virtio` boots a real VZ VM with a custom virtio device attached (device created, kernel reaches `dipshit>`, exit 0), and the live `pci` command observes **VID 0x1af4 / DID 0x1082** on the bus alongside console 0x1043 / blk 0x1042 / entropy 0x1044 (`artifacts/vm-spike-pci.log`). Rebased onto `388410c` (PR #55, claim 9187's real IRQ delivery) with both change sets intact; class A green (monitor 105, shell 129, transcript byte-identical, unit set, build, fmt, coordination). Evidence: `artifacts/vm-spike.log`, `artifacts/vm-spike-pci.log`. · ✅ done
- **2026-08-09** — *buffy*: claim 0828 (`custom-virtio-queue-irq`) complete — the guest side of the spike: new `kernel/src/virtio_custom.zig` driver probes DID 0x1082, negotiates VIRTIO_F_VERSION_1, sets DRIVER_OK, arms queue 0 with the known payload `DIPSHITOS-CV0x42` and kicks. The host delegate dequeues the exact 16 bytes (`CUSTOM-VIRTIO: dequeued 16 byte(s) ... ascii="DIPSHITOS-CV0x42"`), `returnToQueue` advances the used ring, and a real **SPI IRQ (INTID 0x45 = SPI 69) enters the claim-9746 vector** — same SPI as Linux virtio1. **Root cause found:** VZ's firmware never enables the PCI command register for the custom device (boots `0x10`, memory space off), so its BAR0 `0x100020000` is inert and every access external-aborts; the driver now enables it (`command=0x16`, the console's exact value) in `init()`. Also reverted a wrong 32-bit-widened accessor that faulted with alignment aborts at odd offsets (0x16/0x1e). Gate `tools/verify-custom-virtio.sh` PASS 1/1 (all ten assertions); no regressions: live timer/transcript/exceptions/unit/fmt/coord all green; claim 5844's "0x50001000 BAR" note corrected (that's BAR2; transport BAR0 = 0x100020000). Evidence: `artifacts/live-cvspike-*`. · ✅ done
- **2026-08-09** — *buffy*: claim 0828 extended to a **two-descriptor chain** — full bidirectional data flow on one element. The guest's queue-0 chain (size 2) is desc 0 = device-read payload `DIPSHITOS-CV0x42` + desc 1 = device-write reply buffer; the host delegate now writes the known reply into the element's write buffers (`element.write(...)`, `writtenByteCount` feeds the used-ring length) before `returnToQueue`. Evidence: guest reads `cvspike: reply="DIPSHITOS-CVRPLY" len=0x10` back verbatim and the used ring now reports `len=0x10` (was 0 when nothing was written); IRQ still SPI 69. Gate `verify-custom-virtio.sh` now asserts host-replied + guest-reply (12 assertions) and passes 2/2; `zig build spike-virtio` greps the reply readback; no regressions (timer/transcript/unit/fmt/coord green). · ✅ done
- **2026-08-09** — *buffy*: claim 0828 reply read is now **length-driven + hex**: the guest posts a 32-byte device-write descriptor, the host writes the 16-byte `DIPSHITOS-CVRPLY`, and the guest reads back exactly the used-ring length (`cvspike: reply-hex=44 49 50 53 48 49 54 4f 53 2d 43 56 52 50 4c 59 n=0x10 cap=0x20`) — clamped by `reply_read_len()` (host-tested), printed byte-at-a-time as hex via a new `uart_hex8` so arbitrary reply contents are faithful. Host logs the write-buffer size (`wrote 16 byte(s) reply into 32 byte(s) of write buffers`). Gate asserts the hex bytes AND `n=0x10` (2/2 boots); `zig build spike-virtio` greps the hex readback; no regressions (timer/transcript/unit/fmt/coord green). · ✅ done
- **2026-08-09** — *buffy*: claim 0828 refactored into a **reusable submit/wait/reply API** and proven with **four exchanges in one boot at varying reply sizes (8/17/25/33 bytes)** — arbitrary-length bidirectional transfers. `kernel/src/virtio_custom.zig` now exposes `submit()` (caller-owned payload + reply buffers on queue 0), `wait()` (used-ring spin returning the used length), `reply_read()` (copies exactly that many bytes); `main.zig` runs the exchange loop and drains IRQs with a bounded post-loop wait. Host delegate writes a length-matched reply (`0x40+L` repeated L times) per element. Gate `verify-custom-virtio.sh` generates the reply lines typo-proof and checks all-of (12 assertions, PASS 2/2); `zig build spike-virtio` greps the exchange reports. **Honest finding:** VZ coalesces used-buffer IRQs per burst — 4 exchanges produced exactly **1 IRQ** (`irq=1`), the IRQ is a notification not a per-element ack; all 4 replies delivered reliably. No regressions (timer/transcript/unit/fmt/coord green). · ✅ done
- **2026-08-09** — *buffy*: next-session queue recorded in claim 0828's "Next steps" section: (1) virtqueue ring allocator + multi-queue, (2) guest→host payloads > 4 KiB / multi-descriptor reads with host reassembly proof, (3) feature-negotiation depth (`VIRTIO_F_NOTIFICATION_DATA`, `VIRTIO_F_ANY_LAYOUT`) with a report of what VZ accepts, (4) a guest console/log transport on the custom device gated by host-side line echo. · 📋 queued
- **2026-08-09** — *buffy*: filed claims for claim 0828's four queued next steps on `agent/buffy/macos27-custom-virtio-spike`: 4374 `virtqueue-ring-allocator` (free-list descriptor allocator + queue 1), 9492 `multi-descriptor-payloads` (>4 KiB scatter payloads + host reassembly), 9737 `feature-negotiation-depth` (ANY_LAYOUT + NOTIFICATION_DATA report), 4837 `custom-virtio-log-transport` (guest log path over queue 1 with host echo). Indexes refreshed. · 🔄 in progress
- **2026-08-10** — *buffy*: claims 4374/9492/9737/4837 (claim 0828's four queued next steps) implemented + proven in ONE boot (`verify-custom-virtio.sh` PASS 1/1, all 17 assertions). `kernel/src/virtio_custom.zig` grew from queue size 2 → 32 with a free-list allocator (`alloc_chain`/`free_chain`), two armed queues, handle-based `submit_ex`/`wait`, and a `cvlog_puts` queue-1 log transport; `main.zig` runs two batches of four concurrent in-flight exchanges, a 12,340-byte three-descriptor payload, three log lines, and a feature report; the host delegate echoes any length and prints queue-1 log lines. Guest: `q0 heads=0x0,0x2,0x4,0x6 recycle=1`, `q0 big n=0x3034 echo=ok`, `q1 ok=3`, `irq=0x112 first=0x45 spi=0x45`. Host: `dequeued 16/12340 byte(s)` + `CUSTOM-VIRTIO-LOG: cvlog-{1,2,3}`. **Claim 9737's honest finding:** VZ offers `feat=0x530000000` (VERSION_1 + RING_PACKED/RING_EVENT_IDX/RING_INDIRECT_DESC) but **not** ANY_LAYOUT or NOTIFICATION_DATA → `acc=0x100000000 nd=0 al=0 notify=16bit`, classic kicks carry everything. **Root cause found (claims 4374/9492):** anonymous slice-array literals (`&.{a[0..]}`) const-fold into .rodata with baked image-relative pointers and the flat loader doesn't relocate — the device read GPA `0xc37f0` and `nextElement()` returned nil on every kick; the scatter array is now built in BSS at runtime (`cv_scatter`) and the transport passed on the first retry (same bug class as the claim-0015 vtable + `cv_log_lines`). No regressions: fmt/build/unit tests (10 virtio_custom tests) green, indexes refreshed. · ✅ done
- **2026-08-10** — *buffy*: claim 5275 (`tick-driven-task-scheduler`) complete — the first milestone-three tasks card: a tick-driven round-robin scheduler between two kernel tasks on the claim-9187 timer PPI. New `kernel/src/scheduler.zig` (2 fixed tasks — shell/main + a demo worker on its own static BSS stack; the switch only saves/restores the vector-frame pointer + ELR_EL1 + SPSR_EL1 per task, because the claim-9746 IRQ stubs already push/pop the register file; `exceptions.resume_frame` is staged at IRQ entry and the stubs gained `mov sp, x0` so the restore lands in the next task's frame), wired into `irq_dispatch` (timer.handle → scheduler.tick → eoi) and the boot seam (worker registered + scheduling started just before `shell.boot_and_park`). New `dipshit> tasks` command (registry 21→22) + worker progress reported from the shell idle loop (`tasks worker advances=N`, main-context console discipline). **Timer heartbeat/report lines now snapshot their counters at the event** so the strict `ticks=5 irq=5 poll=0` live-timer gate stays exact when the shell prints late under preemption. 6 new scheduler host tests + monitor/shell tests; transcript fixture regenerated. **Live gate `tools/verify-live-tasks.sh` PASS 1/1** (worker report line after ≥ 2 real context switches + responsive shell `rx-tasks-ok`; log shows `tasks worker advances=64/2112/4160` across quanta); **regressions all green live: live-timer 1/1 (heartbeat `ticks=5 irq=5 poll=0` under preemption), live-exceptions 1/1 (stub `mov sp, x0` compatible with the sync resume path), live-transcript 1/1, live-reboot 2/2, live-fs 1/1**; class A green (fmt, 16-module unit set, transcript byte-identical, build, image, inspect, swift, coordination). Evidence: `artifacts/live-tasks-*`, `artifacts/live-timer-*`. · ✅ done
- **2026-08-10** — *buffy*: claim 1370 (`docs-reconcile-prs-55-56`) filed — docs-only reconciliation of the merged PRs #55/#56 (+ the branch's #57/#58 merges, all contained in `main` 3436676): verify status.md's milestone-table real IRQ delivery / item-6 flip / macOS 27+ floor, correct the hard-gate command count to the merged **22** (20 → 21 via `pci`/claim 5844, → 22 via `tasks`/claim 5275 — the mission text's "21" predates claim 5275), add the custom-virtio discovery `[observed]` section to hardware-contract.md, and present the roadmap milestone-three cards in canonical order (allocator → exception vectors → GIC + timer → tasks → userspace). · 🔄 in progress
- **2026-08-10** — *buffy*: claim 1370 complete — docs reconciled against merged `main` 3436676 with **no code changes**: (1) `docs/status.md` hard gate corrected to **22 commands** (registry 20 → 21 via `pci`/claim 5844, → 22 via `tasks`/claim 5275 — the mission text's "21" predates claim 5275; `registry_count` in `kernel/src/monitor.zig`), milestone-table real IRQ delivery / item-6 flip / macOS 27+ floor verified present; (2) `docs/hardware-contract.md` gained the custom-virtio discovery **[observed]** — host-device bullet + dedicated section (DID 0x1082 not in the standard table, BAR0 `0x100020000`, firmware boots the PCI command register disabled at `0x10` → driver writes `0x16`, aligned-u32 config access, VZ offers `feat=0x530000000` without ANY_LAYOUT/NOTIFICATION_DATA, SPI 69 used-ring IRQ, per-burst IRQ coalescing — 4 exchanges → 1 IRQ, two split-ring queues + >4 KiB scatter payloads); (3) `docs/roadmap.md` gained an ordered **Milestone three** section (allocator → exception vectors → GIC + timer → tasks → userspace next), with the four done cards moved out of "Later milestones (sketches only, not commitments)" and the stale "next milestone is the allocator" pointer fixed. Indexes refreshed; coordination gate + test-coordination + mmu-debt + fmt green. · ✅ done
