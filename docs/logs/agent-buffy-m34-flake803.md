# Log — `agent/buffy/m34-flake803`

- **2026-09-01** — *buffy (agent/buffy/m34-flake803)*: claim 1432 — instruments vector entry to capture raw sp/elr before frame store (raw_sp = frame + 256, callee-saved x19..x28) + fixes #803 corrupted-exception-frame panic. Root cause: `monitor.cmd_write` staged 32 KiB (`virtio_file.reply_cap`) on the 16 KiB boot stack (ADR 0004 D5) reached via `shell.boot_and_park` merged frame — overflow corrupted console/state and produced deterministic EL1h data-abort at console byte loop (elr 0x3ea08, far 0xffffffffffffffff, x1 rodata, x2 22). Fix: move buffer to module BSS (`cmd_write_buf`) like `vf_write_buf`. Exceptions: extend `format_report` with `raw_sp` + `x19..x28` dump, compute `raw_sp` from frame base, enlarge report buf to 768, correct frame_read indices for callee-saved dump. Verification: `verify-unit-tests` PASS, `verify-bss-budget` PASS (10992376/11534336, 541960 headroom), fmt/coordination clean. Live gate `verify-live-exec` requires VZ (5/5 expected). 🔄 instrumentation landed, stack fix landed.

