# Log — freebuff/okay-shower-thoughts-here-we-re-using-fat32-for-so-b0ce3067-2b04-4b81-984d-9fd76bfcf123

- **2026-09-01** — *buffy (freebuff/okay-shower-thoughts-here-we-re-using-fat32-for-so-b0ce3067-2b04-4b81-984d-9fd76bfcf123)*: claim 5335 in progress. **WASM import contract audit + amendment.** Deep-audited the landed contract (PR #786, claim 7129) against the kernel dispatch table before W1b/W3 build on it. **Verified clean:** all slot numbers (7/8/12–20/21–27/28–45/63–65), the −1..−10 ErrorCode enum, file_read 2048-clamp + 0=EOF, file_write >2048→ENOSPC, truncate/free semantics, dir_list 16-entry cap, win ids 2..5 + fill rgb ≤0xffffff + get/query byte counts, audio_play caps, timer clamp `@min(@max(delay,1),3600)`, mmap 16 MiB cap + MAP flags, procs 40-B row layout, wait semantics, max_path_len 64, ≤8 handles. **Four discrepancies, three critical:** (A) **AudioInfo is 16 B** (`format u8@4, rate u8@5, period_bytes u32@8, max_len u32@12` — proven by compiling the kernel struct; the "24 bytes" comment in `virtio_snd.zig`/`ui.zig` is stale), not the 24-B all-u32 layout the contract claimed; (B) **DirEntry is `name[32], size u32, is_dir u8, reserved[3]`** (ADR 0010 D3, verified from `handle_dir_list`'s copy_out), not `name[31], type u8, size u64` — the agent had copied the HF2 channel LIST shape; (C) **file_open flags are `MODE_READ 0x1 / MODE_WRITE 0x2 / MODE_CREATE 0x4 / MODE_APPEND 0x8 / MODE_DIR 0x10`**, flags==0 → EINVAL — not "bit0=O_CREATE, bit1=O_APPEND, 0=read/write"; (D) munmap has no EFAULT path. Plus the §6 out-of-scope list omitted slots 46/47/49–53/55–62 (win-depth/notify/pipe/font/ping/net-stats). All five corrected in the amendment, with an amendment note in the doc header.
