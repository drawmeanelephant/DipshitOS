# Log — `freebuff/pull-git-and-check-status-to-make-sure-everything--9934c25c-63ea-4cf3-b3fb-4b98fb81b9f4`

- **2026-08-08** — *buffy (freebuff/pull-git-and-check-status-to-make-sure-everything--9934c25c-63ea-4cf3-b3fb-4b98fb81b9f4)*: claimed 🔄 — the next card: **virtio RX path + live-transcript gate (class B live-transcript-rx)**. With post-MMU TX reliable (claim 1517), the receive side is the last piece to a real interactive `dipshit>` session. Claim file `docs/claims/6684-virtio-rx-live-transcript.md` (ID 6684 via `tools/status/claim-id.sh`). Plan: virtio-console queue 0 (receive) configured pre-exit (256-byte WRITE descriptor, avail supply, kick after DRIVER_OK); polled `readByte` (used-ring drain → bounded FIFO → re-supply); `M15Console.readByte`/`rx_wired` wired; shell WFE idle → bounded poll delay for RX mode; runner `--script`/`--script-expect` non-interactive duplex mode; `tools/verify-live-transcript.sh` class-B gate asserting the live transcript in `vm-serial.log`; docs (gate-inventory/status/march/roadmap/testing/README/hardware-contract/justfile) updated.
- **2026-08-08** — *buffy (same branch)*: implemented + verified ✅ (claim 6684). **Kernel:** `virtio_console.zig` gains the receive queue (queue 0): configured pre-exit between queue 1 and DRIVER_OK (size 1, one 256-byte WRITE descriptor into a BSS buffer, ring GPAs as 32-bit halves, queue_enable, per-queue notify offset, initial avail supply; kicked right after DRIVER_OK); `virtio_read_byte()` polls the used ring (cache-invalidate), drains into a bounded 512-byte FIFO, re-supplies + kicks, returns one byte; `rx_armed()` accessor. `main.zig` `M15Console.readByte`/`rx_wired` dispatch to the virtio RX path (nvram builds unchanged). `shell.zig` RX-wired idle is a bounded nop delay (WFE would never wake — the device delivers input with no interrupt). **Host:** `main.swift` gains `--script <file>` / `--script-expect <substring>` — non-interactive duplex mode (tee to log, waits for `kernel terminal state`, forwards the script, `scriptPoll` exits 0 iff the expected substring appears); `startConsoleStreams` split into tee + stdin forwarding. **Gate:** `tools/verify-live-transcript.sh` (class B; `just verify-live-transcript`; in `verify-vz`), registered in gate-inventory (live-transcript-rx → PASS). **Docs:** status.md (hard gates RX + live scripted session ✅, blocker RESOLVED note, ordering item 2 done, gate table row), march-m15 steps 9/19/20, roadmap, testing.md (new step 19 + results-log entry), README, architecture, hardware-contract (queue 0 RX [observed]). **Class-B evidence:** `verify-live-transcript.sh` **3/3 boots PASS, byte-identical 4421-byte transcripts** — live `dipshit>` session with real `help`/`version`/`mem`/`echo rx-live-ok` output in `vm-serial.log` (`artifacts/live-transcript-*`); serial gate `zig build run` still passes; bad-handoff/marker/nvram-console/host-console re-run green. **Class-A:** fmt, 50 unit tests, mock transcript gate (byte-identical), builds/image/inspect/context, swift build, coordination, test-coordination 15/15, mmu-debt — all pass. ✅ done.
