# Log — freebuff/okay-we-ve-been-kind-of-freestyling-off-away-from--0584ad0f-9850-473f-8884-7c28b20acab7

- **2026-08-08** — *buffy (freebuff/okay-we-ve-been-kind-of-freestyling-off-away-from--0584ad0f-9850-473f-8884-7c28b20acab7)*: claimed 🔄 — docs-only reconciliation of the stable-context docs against actual state at HEAD `181c2fc`; claim file `docs/claims/1566-docs-current-state-reconcile.md` (deterministic ID 1566 via `tools/status/claim-id.sh`). Drift found: README kernel-tree layout predates the claim-0023 module split + ADR range 0001–0004 (now 0006) + missing NVRAM-console/host-console gate rows and `zig build nvram-console` quickstart; testing.md missing the NVRAM-console step + class-D `t0sz16` + ladder `M2_READY`; architecture.md data-flow NVRAM channel + module list stale; claim 6460 not cited outside status.md/gate-inventory; status.md class-A "Last evidence" cells predate the 2026-08-08 preflight re-run (claim 8592).
- **2026-08-08** — *buffy (freebuff/okay-we-ve-been-kind-of-freestyling-off-away-from--0584ad0f-9850-473f-8884-7c28b20acab7)*: reconciled ✅ — README: kernel-tree now lists the claim-0023 modules (mmio/mmu/pci/evidence/virtio_console/nvram_console/machine), `linker.ld` at `kernel/`, ADRs 0001–0006, `zig build nvram-console` quickstart, NVRAM-console + host-console gate rows, `verify-nvram-console.sh` in observed-behavior, claim 6460 cited in the at-a-glance and Next-steps #1, marker ladder reaches `M2_READY`. testing.md: class-D list + diagnostics list gain `t0sz16` (claim 6460), new step 13 = claim-0015 NVRAM-console gate (steps renumbered 13→18), marker ladder `M2_MAPD! → M2_MMUP! → M2_SERIA → M2_READY`. architecture.md: kernel components row + data-flow NVRAM channel (ladder reaches `M2_READY`; claim-0015 console bytes) + claim 6460 in the guest→host console interface. roadmap.md M1.5-close-out + march-m15 step 8 cite claim 6460 (6/18 boots, not reproducible, production T0SZ stays 25). status.md: gate header + class-A "Last evidence" cells refreshed to the 2026-08-08 preflight re-run (claim 8592, `artifacts/status-preflight-*.txt`); stale fmt command corrected to `zig fmt --check boot/src/*.zig kernel/src/*.zig build.zig`. Verification: indexes regenerated, `verify-coordination.sh` ok, link check clean, stale grep clean, `zig fmt --check` ok, unit tests + transcript gate pass. No kernel/host/build code touched.
- **2026-08-08** — *buffy (freebuff/okay-we-ve-been-kind-of-freestyling-off-away-from--0584ad0f-9850-473f-8884-7c28b20acab7)*: claimed 🔄 (claim `docs/claims/7896-t0sz16-residual-separation.md`, ID 7896) — investigate claim 6460 further: why did T0SZ=16 restore post-MMU virtio TX in only 6/18 boots, and design a deterministic experiment separating the start-level mismatch from the residual hang. Characterization (from claims 0010/0018/0020/0021/6460 + source): the built tables are L0-rooted but production T0SZ=25 (W=39) starts the 4 KiB walk at level 1, so EVERY fresh walk faults (misparse < 1 GiB; ROOT[1..3]=0 ≥ 1 GiB; ROOT[4]=0 for the BAR); the no-TLBI crutch hides this by riding stale firmware TLB entries; the BAR read is the first access whose entry was evicted → fault → hang (no exception vectors). T0SZ=16 fixes the walk (6/18); the residual 12/18 is post-translation — a boot-varying VZ device-emulation/race hang (or an unidentified second fault) at the first common-cfg read. Experiment: two new default-off options — `-Dtlbi-after-switch` (force an empty TLB → deterministic re-walk) and `-Dwalk-probe` (cold-address probe battery with per-probe markers) — plus a 4-cell matrix runner; pilot cells A (T0SZ=25+tlbi → expect deterministic death right after the switch) and B (T0SZ=16+tlbi → expect full probe ladder, then ~1/3 phase-C). Default builds must stay byte-identical (sha `55325752…`).
- **2026-08-08** — *buffy (freebuff/okay-we-ve-been-kind-of-freestyling-off-away-from--0584ad0f-9850-473f-8884-7c28b20acab7)*: reconciled ✅ (claim 7896) — implemented the two default-off diagnostics (`-Dtlbi-after-switch` = `tlbi vmalle1; dsb ish; isb` right after the identity-map install; `-Dwalk-probe` = cold-address probe battery P1..P5 with `M2_WP_00..05` NVRAM markers; comptime-gated, default builds byte-identical sha `55325752…`), plus `tools/verify-t0sz16-walkprobe.sh` matrix runner. **Pilot on real VZ hardware (3 boots/cell, fresh store each):** cell A (T0SZ=25+TLBI) 3/3 deterministic death at `M2_MMUP!` (wp-depth=0) — start-level mismatch confirmed; cell B (T0SZ=16+TLBI+probe) 3/3 full battery + phase C complete (9/9 with 6 more boots); cell C (T0SZ=25, no TLBI) 3/3 die between `M2_WP_04` and `M2_WP_05` — the stale-TLB crutch covers every RAM probe but NOT the virtio BAR; cell D (T0SZ=16, no TLBI) 3/3 wp-depth=6 with phase C 1/3 — reproduces claim 6460. **Key finding:** the controlled pair B vs D (same T0SZ=16, correct walk both) shows empty TLB → 9/9 complete vs stale TLB → ~1/3: the claim-6460 "residual" is **stale-firmware-TLB interference, not a device/emulator hang** (the device-level branch was falsified); boot variation = which firmware-era TLB entries survive the switch. Fix direction: T0SZ=16 + TLBI works 100% on this host; production T0SZ=25 needs L1-rooted tables for W=39 or the ADR-0006 crutch debt stands. Decoder-gap lesson: the first pilot run's wp-depth=0 for every boot was a HOST-SIDE bug (the `M2_WP_*` needles were missing from the VMRunner marker whitelist — the probes were in the store all along, reversed-byte `00_PW_2M`..`50_PW_2M`); fixed in `host/vm-runner/Sources/VMRunner/main.swift`, runner rebuilt, matrix re-run. Also: cell B gained `-Dwalk-probe` (its flags previously contradicted the design), script totals-loop syntax error fixed (bash 3.2 port), `docs/m15-postmmu-t0sz-experiment-design.md` written (claim referenced it but it didn't exist), gate-inventory gained `tlbi-after-switch`/`walk-probe`/`t0sz16-walkprobe` rows, claim 7896 flipped ✅ with the pilot table + revised interpretation.
