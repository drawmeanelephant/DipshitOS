# Log — agent/virtio virtio-input-channel

- **2026-08-24** — *virtio (agent/virtio/virtio-input-channel)*: claim 9588 opened → #523 item 3 build-out: custom-virtio input channel (contract doc → VMRunner input queue → guest driver → event FIFO), proof target = injected key arrives with events>0 without CGEvents/window activation (#179/#151) → 🔄 in progress.
- **2026-08-24** — *virtio (agent/virtio/virtio-input-channel)*: claim 9588 contract landed first (`docs/hardware-contract.md` "Input channel over the custom virtio device": queue plan, 16-byte `[kind][flags][len]`+payload envelope, 8-buffer rx pool, decode-through-`decode_keyboard_report` semantics, loud version rules) → guest end built: `virtio_custom.zig` gains queue 3 constants + `has_input_queue` + `arm_input_pool`/`poll_input`/`dispatch_input_msg` + `on_input_report` hook (+26 module tests green); `main.zig` wires `cv_input_dispatch`, arms the pool in `custom_virtio_spike` ("cvspike: q3 armed bufs=8"), and pumps `poll_input()` from the M15Console readByte idle seam. verify-unit-tests/test-console/build/image/inspect all pass. 🔄
