# Log — agent/virtio virtio-input-channel

- **2026-08-24** — *virtio (agent/virtio/virtio-input-channel)*: claim 9588 opened → #523 item 3 build-out: custom-virtio input channel (contract doc → VMRunner input queue → guest driver → event FIFO), proof target = injected key arrives with events>0 without CGEvents/window activation (#179/#151) → 🔄 in progress.
- **2026-08-24** — *virtio (agent/virtio/virtio-input-channel)*: claim 9588 contract landed first (`docs/hardware-contract.md` "Input channel over the custom virtio device": queue plan, 16-byte `[kind][flags][len]`+payload envelope, 8-buffer rx pool, decode-through-`decode_keyboard_report` semantics, loud version rules) → guest end built: `virtio_custom.zig` gains queue 3 constants + `has_input_queue` + `arm_input_pool`/`poll_input`/`dispatch_input_msg` + `on_input_report` hook (+26 module tests green); `main.zig` wires `cv_input_dispatch`, arms the pool in `custom_virtio_spike` ("cvspike: q3 armed bufs=8"), and pumps `poll_input()` from the M15Console readByte idle seam. verify-unit-tests/test-console/build/image/inspect all pass. 🔄
- **2026-08-24** — *virtio (agent/virtio/virtio-input-channel)*: host end built (`main.swift`): `--via-virtio` flag (implies custom-virtio + cvc-echo → four queues), shared `deviceQueue` static + captured device ref so injection element access stays serialized with delegate callbacks, HID token→usage map over the exact macKey/macChord vocabulary, 16-byte envelope builders, bounded-retry enqueue with loud pool-exhaustion, queue-3 notification logging, and `--via-virtio` branches for KEY-INJECT/KEY-SEQ/CHORD-SEQ that never touch a view. Loud guards added: non-SPIKE binary + custom-virtio flags is now FATAL (was a silent flag ignore); <macOS 27 already fatal globally. Base + `-DSPIKE` builds green.
- **2026-08-24** — *virtio (agent/virtio/virtio-input-channel)*: proof phase. First live run exposed two real defects, both fixed and re-proven: (1) HOST ordering — a fixed-schedule enqueue burst hit a momentarily empty rx pool and the retry jumped the queue (guest typed `inpu⏎t`); delivery is now strictly chained (each stroke enqueued only after the previous accepted; retries delay, never reorder). (2) GUEST bridge — headless boots never wired the FIFO→line-editor path for keyboards (`rp_read_source` was XHCI-only AND unconditionally focus-gated); main.zig now wires the read source for the cv-input pool too and lets keyboard bytes reach the terminal when no WM exists (armed-WM behavior unchanged — every existing --input gate also passes --display). Also fixed the gate's own assertion ('i' is HID usage 0x0c). Final: GATE_VIRTIO=1 rc=0 twice (16/16 assertions), GATE_VIRTIO=all rc=0 — classic synthesized phase AND virtio phase green in ONE run, identical guest evidence `input: armed=0 … events=6 kb-usage=0x28 kb-byte=0xa` vs classic's armed=1 line: injected keys arrive with events>0, headless, no CGEvent/NSEvent synthesis, no window (#179/#151 attacked). Contract flipped [design]→[observed]. 🔄→✅ pending final battery.
- **2026-08-24** — *virtio (agent/virtio/virtio-input-channel)*: claim 9588 ✅ DONE. Final battery all green (fmt, unit tests, test-console, build, image, inspect, swift base+SPIKE, coordination gates ×21). Proof: GATE_VIRTIO=1 rc=0 ×2, GATE_VIRTIO=all rc=0 (both phases in one run). Evidence under artifacts/live-input-virtio-*. Contract observed. Claim file flipped ✅ with the full result record.
