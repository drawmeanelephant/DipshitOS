# Log — agent/ox-alpha/hygiene-trim-hardware-contract

- **2026-08-21** — *ox-alpha (`agent/ox-alpha/hygiene-trim-hardware-contract`)*: claim 2203 — docs-only hygiene per issue #270: `docs/hardware-contract.md` trimmed from 57,874 B to a summary table + actionable facts; the full pre-trim file preserved verbatim as `docs/archive/hardware-contract-detail.md` (issue #262 archive convention). No guest/host code touched. Evidence: `wc -c` before/after in the PR body; link check over `docs/`. 🔄
- **2026-08-21** — *ox-alpha (same branch)*: claim 2203 ✅ — main file now 14,332 B (target ~15–20K): an 11-row device summary table (PCI ID, reset-at-ExitBootServices behavior, actionable quirks, claim pointers) + condensed sections (boot/handoff invariants, CPU/MMU, serial console, interrupts/timer, input). Every `[observed]` fact kept its claim citation; the verbose narratives (gpu G1–G6 color pipeline, USB enumeration, pointer-route probe, net N1–N11 phase walks, DHCP/TCP lifecycle, M1/M2 discovery history) live only in the archive copy. No inbound anchor links existed to break. Verified: key-fact grep (T0SZ=16, 1530 B RX floor, GICR SGI frame +0x10000, GSIV 30 @ 24 MHz, virtio-1.3 OK=0x8000, snd TX queue 2, custom-device command=0x16, base+0 invariant), coordination gate, zig preflight.
