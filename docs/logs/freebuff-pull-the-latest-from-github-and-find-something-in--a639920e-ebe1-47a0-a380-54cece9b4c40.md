# Log — freebuff/pull-the-latest-from-github-and-find-something-in--a639920e (serial device discovery)

- **2026-08-07** — *buffy (freebuff/pull-the-latest...)*: claim 0013 (M1.5 VZ serial device discovery) created and logged before any code. Plan: persist probe records to NVRAM, scan declared MMIO windows at virtio-page granularity, read `artifacts/efi-vars.bin` for ground truth on an Apple M4 host. Status: 🔄 in progress.
- **2026-08-07** — *buffy*: baseline on Apple M4 — ladder runs `M2_ENTRY → … → M2_MMUP! → M2_SERIA` (MMU takeover complete, probe halts at layout=none). Fresh evidence saved.
- **2026-08-07** — *buffy*: window-base blindness tested. Ground truth from the store: `0x01000000` contains the literal string `efivars\0` (Apple's EFI variable-store region, not virtio); `0x20050000` is a PL011-family UART with odd PIDs (`0x31/0x10/0x04`) — a real device, not the standard PrimeCell signature. `SEL=NONE`.
- **2026-08-07** — *buffy*: full EFI map dump — 26 descriptors, all below 4 GiB, only the two declared MMIO windows; no other device windows. Config table: 8 entries, `RSD ` (ACPI RSDP), `_SM_`/`_SM3_` (SMBIOS), no devicetree. XSDT = FACP/GTDT/APIC/MCFG — no SPCR/DBG2. DSDT is Apple's own (OEM `APPLE `, `Apple Vz`, 858 B AML): only `PCI0` + `efivars` declared.
- **2026-08-07** — *buffy*: post-exit MMIO reads of the declared windows hang on VZ (the fivars reads); moved the evidence dumps + probe to PRE-EXIT (proven reliable channel), kept post-exit persistence tiny/best-effort. Chunked the probe persist into `DipshitP0..N` ≤2 KB variables after a ~6 KB single-variable write was silently rejected.
- **2026-08-07** — *buffy*: PCI enumeration (ECAM `0x40000000`, from MCFG — after fixing a transposed MCFG signature constant that made the branch dead code): bus 0 has 4 devices; **D5 = VID 0x1af4 DID 0x1043 class 0x078000 — the modern virtio console**. Also Apple PCIe root + virtio blk/entropy. BAR0 = 64-bit BAR firmware-assigned at `0x100010000` (above the 4 GiB blanket; assignment varies across boots).
- **2026-08-07** — *buffy*: virtio-pci transport decoded via aligned-u32 config reads (VZ byte reads of config space are garbage; unaligned reads alignment-fault with no exception vectors — a real bug found and fixed): common cfg @ +0x0000, ISR @ +0x1000, notify @ +0x4000 (mult 4), device cfg @ +0x8000. Pre-exit the transport arms fully: features `0x30000000`/`0x5`, queue 1 configured, DRIVER_OK. `SEL=VIRTIO`; ladder reaches `M2_READY`.
- **2026-08-07** — *buffy*: **gate still blocked, now precisely.** Post-exit, every access to the virtio-pci transport window hangs (first banner TX dies at/around the notify write; `vm-serial.log` 0 B) — the same VZ wall that blocks post-exit MMIO reads of the declared windows. BAR rebase to below the blanket makes the device disappear (config writes don't move it post-exit); mapping the firmware base in place keeps it armed but unreachable for TX. Claim 0013 closed ⚫ blocked (discovery complete, gate not passed) with the full evidence narrative.
