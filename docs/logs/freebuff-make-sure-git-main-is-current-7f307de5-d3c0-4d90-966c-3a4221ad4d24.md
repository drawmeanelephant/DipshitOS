# Log — make-sure-git-main-is-current (claim 4922)

- **2026-08-08** — *buffy (freebuff/make-sure-git-main-is-current-7f307de5-d3c0-4d90-966c-3a4221ad4d24)*: claimed 🔄 — aligned `main` to `origin/main` (`2f20e5a`, fetch + fast-forward from `fff37a5`); discovered the exact fix this task asks for already landed upstream as claim 0176 (commit `0d69ef9`, merged in PR #43). Per coordination rules, claimed work is not duplicated: claim `docs/claims/4922-verify-ragshit-0176-landing.md` (deterministic ID 4922 via `tools/status/claim-id.sh`) instead VERIFIES the landing — reproduce pre-fix failure, confirm post-fix behavior, run full verification battery, capture before/after packets under `artifacts/`.
- **2026-08-08** — *buffy (freebuff/make-sure-git-main-is-current-7f307de5-d3c0-4d90-966c-3a4221ad4d24)*: verified ✅ — reproduced the pre-fix defect exactly with the pre-0176 code (worktree at `fff37a5`, `review . HEAD~5..HEAD --budget-chars 40000 --explain`): `virtio_pci_init` (symbol_range 165-395, 231 lines) rendered as one line `p…` while `changed_symbols` reported 40/40 (100%). Same tree/range/args with the 0176 code: excerpt spans 165-262 with signature + the changed region (lines 256-265) and exact omission markers, `changed_symbols` 27/40 (67%) with 13 weak, `## Weak / truncated coverage` lists them, never 100% while weak exist — numbers match claim 0176's recorded evidence. Regression tests (`test_review_weak_coverage.py`) fail on the pre-fix code (8 failed / 2 passed) and pass on current main. Full suite 147 passed / 1 skipped; `ragshit doctor` all OK; fresh index; real-repo dogfood at 20k/30k/40k/60k with exact accounting (`len(md)` == `actual_size` == `selection_summary.actual_chars` at every budget) and byte-identical duplicate runs (md + json); `envelope_fallback_used` false at every budget. Evidence committed to the repo under `artifacts/ragshit-0176/` (force-added over the gitignore, docs-reconciliation precedent): `before-40000.{md,json}`, `after-40000.{md,json}`, `dogfood-{20000,30000,40000,60000}.md`, `verification-summary.txt` (dogfood `.json` copies are byte-reproducible and kept out of the repo); coordination gate `verify-coordination.sh` ok.
