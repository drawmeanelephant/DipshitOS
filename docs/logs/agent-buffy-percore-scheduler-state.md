# Log — agent/buffy/percore-scheduler-state

- **2026-09-02** — *buffy (agent/buffy/percore-scheduler-state)*: claim 8477 opened — make `scheduler.current` + `pending_*` staging per-core so cores 1-3 could eventually run user tasks. 🔄 in progress.
- **2026-09-02** — *buffy (agent/buffy/percore-scheduler-state)*: claim 8477 complete. **Change:** `current` is now `[smp.max_cores]usize` (the never-written `current_by_core` vestige folded in); the five `pending_*` globals are per-core arrays; every scheduler function touching them resolves `const c = smp.core_id()` at entry and indexes `[c]`. **Key seam:** `apply_pending` now writes `exceptions.resume_frame[c]`/`resume_sp_el0[c]` instead of hard-coded `[0]` — the claim-7339 per-core exception handoff is consistent end-to-end (stage on c → apply on c → eret on c). `yield/sleep/wait/wait_event/tick/current_id` all follow; `current_task_for_core` returns `current[cid]` directly. **Correctness:** staging and apply never cross cores (SVC context stages, IRQ context applies, same core), so per-core slots have single owners — behavior on the 2-vCPU VM is byte-identical (all sites resolve to slot 0 today). Remaining SMP blockers are now explicit and localized: the PE-0 tick gate in `irq_dispatch`, the unlocked shared ready ring, and cross-core wakeups. **Evidence:** full unit suite PASS, build PASS, BSS PASS (511 080 B headroom, +~220 B for arrays), fmt/coordination clean; class-B gates plain exec: verify-live-zc 4/4, verify-live-vf 4/4, verify-cvc-echo 1/1, verify-live-concurrent 2/2 (live sleeping=2 awake=2 interleave=1 — the per-core sleep path on hardware). ✅ done.- **2026-09-02** — *buffy (agent/buffy/percore-scheduler-state)*: claim 9408 complete — tick gate lifted. **Change:** `irq_dispatch` no longer gates `timer.handle()`+`scheduler.tick()` to PE 0; secondary cores take their own timer PPI and run `tick()` on per-core state. Secondary path: acquire `sched_lock` (spinlock; `try_lock` on the one-shot witness so a deadlocked core yields evidence), verify `current[cidx]` is still the worker slot, run readiness + timeslice progress, `apply_pending`; pick-next wraps per-core (worker → idle → shell) so the secondary core parks on idle. Core-0 `on_tick` unchanged (full pick, wakeups, exit reports). **Locking:** `sched_lock` now guards shared ring mutation (`spawn`/`request_kill`/`request_report`/`maybe_report`/`maybe_heartbeat`/`wake_event_waiters`/`exit_current`/`tick`) with holder-tracked acquire/release — the event hook fires from lock-held (exit teardown, tick) AND unlocked SVC contexts, so plain `lock()` self-deadlocked (found live: WIN.BIN exit → `driving_award.close_owner` → `events.push` → hook → `lock()` same-core). Hook checks `sched_lock_held` and skips when held. **Found in passing:** `exit_current` was already re-entering tick's locked body (latent self-deadlock the new locking exposed) — split `exit_current_locked`. **Evidence:** full unit suite PASS (found + fixed the WIN.BIN exit deadlock), build PASS, BSS PASS (511 080 B headroom), fmt clean; class-B gates plain exec: verify-live-zc 4/4 **with `secondary=1` every boot** (monitor `smp` shows `task=worker state=online` off core 0 — core 1 ran the worker through the lifted tick), verify-live-vf 4/4, verify-cvc-echo 1/1, verify-live-concurrent 2/2 (sleeping=2 awake=2 interleave=1). **Boundary:** console TX is polled/unlocked, so user tasks stay on core 0; the worker (note_advance + request_report + nop-spin) is console-free by design. Task migration still out of scope. ✅ done.
