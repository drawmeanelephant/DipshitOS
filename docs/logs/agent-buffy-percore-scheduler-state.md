# Log — agent/buffy/percore-scheduler-state

- **2026-09-02** — *buffy (agent/buffy/percore-scheduler-state)*: claim 8477 opened — make `scheduler.current` + `pending_*` staging per-core so cores 1-3 could eventually run user tasks. 🔄 in progress.
- **2026-09-02** — *buffy (agent/buffy/percore-scheduler-state)*: claim 8477 complete. **Change:** `current` is now `[smp.max_cores]usize` (the never-written `current_by_core` vestige folded in); the five `pending_*` globals are per-core arrays; every scheduler function touching them resolves `const c = smp.core_id()` at entry and indexes `[c]`. **Key seam:** `apply_pending` now writes `exceptions.resume_frame[c]`/`resume_sp_el0[c]` instead of hard-coded `[0]` — the claim-7339 per-core exception handoff is consistent end-to-end (stage on c → apply on c → eret on c). `yield/sleep/wait/wait_event/tick/current_id` all follow; `current_task_for_core` returns `current[cid]` directly. **Correctness:** staging and apply never cross cores (SVC context stages, IRQ context applies, same core), so per-core slots have single owners — behavior on the 2-vCPU VM is byte-identical (all sites resolve to slot 0 today). Remaining SMP blockers are now explicit and localized: the PE-0 tick gate in `irq_dispatch`, the unlocked shared ready ring, and cross-core wakeups. **Evidence:** full unit suite PASS, build PASS, BSS PASS (511 080 B headroom, +~220 B for arrays), fmt/coordination clean; class-B gates plain exec: verify-live-zc 4/4, verify-live-vf 4/4, verify-cvc-echo 1/1, verify-live-concurrent 2/2 (live sleeping=2 awake=2 interleave=1 — the per-core sleep path on hardware). ✅ done.