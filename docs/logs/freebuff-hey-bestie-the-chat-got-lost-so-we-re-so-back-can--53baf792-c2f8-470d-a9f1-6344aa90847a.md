# Log — freebuff/hey-bestie-the-chat-got-lost-so-we-re-so-back-can--53baf792-c2f8-470d-a9f1-6344aa90847a (M14 shared user services)

- **2026-08-18** — *buffy*: worktree reset to merged M14 head `1f6bfbb` (S1+S2+S3 shipped by PRs #182/#183); the earlier S1 clipboard work in this tree was a stale duplicate and was discarded. Claiming M14 card S4 hardening (issue #178) — claim 4482 created before any code.
- **2026-08-18** — *buffy*: M14 S4 hardening (claim 4482) complete. Audit: windows per-process (`win_owned_by_caller`), files/events/timers per-process by construction, clipboard/UDP/mailbox/process-control documented-globals; the ONE gap — TCP's `owner_pid` set on connect but never enforced — closed: non-owner send/recv/close/connect now refuse EACCES (class-A test 341, syscall 322→322 tests green). Hostile-consumer proof: VICTIM.BIN (owns window 2, yield-loops) + HARDEN.BIN (separate process, attacks fill/present/close/move/query → EINVAL ×5, reports refused+survived, exits 44); `bash tools/verify-live-hardening.sh` **PASS 1/1 on VZ** (evidence `artifacts/live-hardening-*`). Full class-A portable suite green at `1f6bfbb` + change (fmt/463 tests/test-console/build/image/inspect/swift/context/coordination/mmu/glyph/mutations). Docs updated (march-m14/roadmap/status), indexes refreshed. Open threads unchanged: #179 keyboard events=0, #151 pointer class-C; issues #175–#178 still need closing notes (all four cards now have merged, gated evidence).
- **2026-08-18** — *buffy*: issue #179 follow-up (claim 8844). Synced worktree to `ec61777` (PR #184 merged). Re-ran `verify-live-input.sh` on the current console session (established ~20:13 UTC): still `events=0` with `armed=1` and host `KEY-SEQ ok=true` — the claim-4769 activation wall, reproduced in a fresh session. Fix: the runner's three synthesized-keyboard paths (`KEY-INJECT`/`KEY-SEQ`/`CHORD-SEQ`) now report the window's key/main/active state after dispatch, with an explicit WARNING naming the wall and the recovery step when the window is not key — so a silent VZ drop is diagnosed at the host instead of surfacing only as guest `events=0`. Issue #179 updated with the pinned state.
- **2026-08-18** — *buffy*: claim 8844 complete — issue #179 closed. Five gate runs today with the stock binary: PASS 3/5 (16:30/16:35/16:38 EDT, events=6) and FAIL 2/5 (16:20/16:33, events=0) — the pass/fail flip with zero code change pins the seam as session-dependent (claim-4769 activation wall), not a code bug. Runner fix: `reportKeyboardKeyState` on all three synthesized-keyboard paths reports window key/main/active at sequence start; message is non-predictive because key=false accompanied BOTH an events=0 fail and an events=6 pass. swift build green; issue #179 closed as completed with the 5-run record.
- **2026-08-18** — *buffy*: issue #151 (M8 U4 pointer) closed as a documented limitation. Re-verified: the class-C gate `verify-pointer-manual.sh` is correctly registered (`class=C kind=interactive ci=no`, gate-inventory id pointer-manual) and its plumbing is smoke-proven, but the PASS run needs a human at a real mouse (no saved PASS evidence; an agent cannot click). Pinned the unblock: VZ has no programmatic pointer API, input only translates for a KEY window, macOS 14+ refuses programmatic focus-stealing (full ladder fails), Accessibility trust does not help (claim 5776: trusted CG → frontmost but key=false → ptr-reports=0), idle hypothesis refuted (claim 4769) — so no code path unblocks synthesis; the class-C real-mouse gate is the working proof. Flipped claim 5776 → done (its gate fixes shipped in PR #174); status.md updated (no open issues remain); indexes refreshed.
- **2026-08-18** — *buffy*: drafted the milestone fifteen plan — `docs/march-m15.md` (theme: audio, wishlist 18). Four cards: A1 virtio-snd transport (`--sound` flag-gated, DID to pin at claim time), A2 bounded PCM playback + `beep`, A3 EL0 audio seam (slots 42+) + `JINGLE.BIN`, A4 hearable composition capstone. Deferred M16+ candidates noted (13/14/15/17). status.md gained an M15 planned row; roadmap.md gained the M15 section. No implementation (milestone-scope rule).

- **2026-08-18** — *buffy*: **M15 A1 shipped (claim 6140)** — virtio-snd transport. Host: flag-gated `--sound` mode (`VZVirtioSoundDeviceConfiguration` + 1 output stream + host sink; `config.audioDevices`, the SDK's actual member — `soundDevices` doesn't exist). Guest: `kernel/src/virtio_snd.zig` (discovery, caps, VERSION_1, control queue 0 armed qsz=4, DRIVER_OK, pre-exit cfg capture, post-MMU rearm) + main.zig wiring (init/identity-map/rearm) + monitor `sound` command. Live gate `verify-live-sound-device.sh` **PASS 1/1 on VZ**: DID **0x1059** (0x1040+25 prediction held), cls 0x040100, st=0x0f DRIVER_OK, rearm ok, NOT reset by VZ (st=0x0f pre-rearm, like net/gpu). **Finding:** VZ does not populate the le32 config counts — jacks/streams/chmaps 0/0/0 from pre- and post-exit reads + a 32-byte raw dump, even with 2 streams attached (experiment reverted to 1); the first cfg read attempt used the wrong u8@0/1/2 layout, fixed to le32@0/4/8 per virtio-snd §5.14.4, still 0 — recorded as hardware truth; A2 enumerates topology via JACK_INFO/PCM_INFO. Docs: march-m15 A1 ✅, status M15 row in-progress, roadmap M15 section, hardware-contract sound entry, claim 6140. Class-A re-verify pending.

- **2026-08-18** — *buffy*: claimed **M15 A2 (claim 5877)** — PCM playback: control-queue stream enumeration (the A1-finding workaround), SET_PARAMS/PREPARE/START/STOP, bounded zero-heap PCM buffer + `beep <freq> <ms>`, TX-queue submission with drained accounting, live gate `verify-live-sound-playback.sh`. Protocol pinned from the Linux uapi header (1.2 codes: PCM_INFO=3, status OK=0; VZ predates the 1.3 renumbering — observed replies pin it). Logged before code.

- **2026-08-18** — *buffy*: **M15 A2 shipped (claim 5877)** — PCM playback. Control-queue protocol in `kernel/src/virtio_snd.zig` (PCM_INFO/SET_PARAMS/PREPARE/START/STOP/RELEASE exchanges on queue 0), TX queue 2 armed, period-submission loop (4096-B periods from a tiny fixed BSS buffer), `beep <freq> <ms>` monitor command (47th command, system category). **Protocol pinned live on VZ** — three findings: (1) VZ speaks the virtio-**1.3** control renumbering (status OK=0x8000; a 1.2-style code-3 PCM_INFO got BAD_MSG 0x8001 — the 1.2 hypothesis was wrong, the reply was the truth); (2) control replies are **[status hdr][entries]** — status FIRST (Linux reads it last); (3) the used ring needs a dcache invalidate on every poll (without it the exchange times out) and the 16-bit kick is the queue-index write (the first kick used the avail index). My FMT_S32/FMT_FLOAT constants were off by one (16/18 → 17/19, pinned by the live formats=0xa0020 bitmap). Gate `verify-live-sound-playback.sh` **PASS 1/1 on VZ**: PCM_INFO S_OK (S16|S32|FLOAT @ 48000|96000 ch 1..2), negotiated FLOAT/48000/stereo, all control steps S_OK, **115200 B submitted in 4096-B periods, all drained** (14400 frames, pcm_status 0x8000, latency 4096). Docs: march-m15 A2 ✅, status/roadmap/hardware-contract updated, claim 5877 ✅, class-A fully green (transcript fixture + shell transcript updated for the new `beep` help row).

- **2026-08-18** — *buffy*: claimed **M15 A3 (claim 7636)** — the EL0 audio seam: ADR 0007 slots 42 (`sys_audio_info`) + 43 (`sys_audio_play`), implemented_count 42→44, userlib wrappers, and `JINGLE.BIN` (a recognizable melody via per-note `sys_audio_play` through the A2 beep path), live gate `verify-live-sound-app.sh`. ENXIO (-9) joins the ErrorCode enum for the honest no-device refusal. Logged before code.

- **2026-08-18** — *buffy*: **M15 A3 shipped — the EL0 audio seam (claim 7636)**. ADR 0007 slots 42/43 (`sys_audio_info` / `sys_audio_play`), `implemented_count` 42→44; userlib wrappers; JINGLE.BIN (the 26th ESP program) plays the Twinkle Twinkle phrase from EL0. Live gate `tools/verify-live-sound-app.sh` PASS 1/1 on VZ: `sys_audio_info` reports FLOAT(19)/48000(7)/stereo(2) (first-call probe+SET_PARAMS), all 14 notes play with exact accounting (96000 B quarters / 192000 B halves, 382 play calls), exit 0; syscalls report implemented=44, info calls=1, play calls=382. A1+A2 gates re-ran green after the shared-path refactor. Findings pinned live: (1) app writable data must be a STACK local — a global BSS buffer faults on write through the W^X text page; (2) `sys_audio_info` drives the negotiation on first call so the app learns the format before synthesizing. JINGLE.BIN embedded via build.zig + make-image.sh + mkfat32.py (arg 29). The nine class-B scripts that pinned `implemented=42` moved to 44 (the M14 precedent).

- **2026-08-18** — *buffy*: claimed **M15 A4 (claim 3206)** — the composition capstone: a boot chime and an event-triggered sound (window focus / clipboard copy / timer tick) through the proven EL0 seam, with `verify-live-m15-composition.sh` proving device + playback + EL0 + the hearable composition in ONE session. No new syscalls or device work. Logged before code.

- **2026-08-18** — *buffy*: **M15 A4 shipped — milestone fifteen COMPLETE (claim 3206)**. Composition capstone: (1) kernel boot chime — `snd_chime()` (660 Hz 150 ms + 880 Hz 220 ms through the A2 beep path) plays the moment the transport is live, flag-gated inside the existing `if (snd_ready)` rearm block (default VM byte-identical; unarmed refusal host-tested); (2) CHIME.BIN (27th ESP program) — arms a one-tick M14 app timer (slot 40), BLOCKS in sys_wait_event, and fires an 880 Hz blip through sys_audio_play (slots 42/43) on every TIMER event. Live gate `tools/verify-live-m15-composition.sh` **PASS 1/1 on VZ**: `chime: boot chime played (660+880)`, 3 ticks × (seq, chunks=10, played=38400), `chime: done`, exit 0, syscalls `implemented=44` with `40 sys_timer_set calls=3` / `42 sys_audio_info calls=1` / `43 sys_audio_play calls=30`, plus the `sound` device report (DID 0x1059) — device + playback + EL0 + the hearable composition in ONE session. A1/A2/A3 gates re-ran green with the chime in the boot path. CHIME.BIN wired via build.zig + make-image.sh (arg 30) + mkfat32.py. Docs: march-m15 A4 ✅ + next-action (M15 complete), status M15 row ✅, roadmap A4 + COMPLETE, hardware-contract chime entry, claim 3206 ✅, indexes refreshed, coordination green. **Nothing committed** — the full A1–A4 change set (39 files) is staged in the worktree.
- **2026-08-18** — *buffy*: M15 follow-up claim 9297 (sound stream-state control). After A1–A4 shipped (the 37-file M15 change set staged in this worktree), the next request: a bounded kernel-side volume/stream-state control (`sound mute|volume` monitor command) exposed through the EL0 seam. Design: `stream_volume` (0..100) + `stream_muted` BSS state in virtio_snd.zig, applied as an in-place gain at the `snd_audio_submit` choke point (shared by beep/chime/sys_audio_play — one knob, zero heap, accounting stays exact); monitor subcommands `sound volume <0-100>` / `sound mute <on|off>`; ADR 0007 slots 44/45 (`sys_audio_volume`/`sys_audio_mute`), implemented_count 44→46 (the nine live gates pinning 44 move to 46, the M14/A3 precedent); ui.zig wrappers; CHIME.BIN calls both live; new gate `verify-live-sound-control.sh`.
- **2026-08-18** — *buffy*: claim 9297 done. Kernel: `stream_volume` (0..100) + `stream_muted` BSS state in virtio_snd.zig, `snd_set_volume`/`snd_set_mute` (out-of-range volume refused, no clamping), and `apply_stream_gain` at the `snd_audio_submit` choke point (in-place, zero heap, 100% fast path bit-exact; scaling down never overflows). Monitor: `sound volume <0-100>` / `sound mute <on|off>` subcommands + `vol=`/`mute=` in the `sound` report (registry row usage/help/max_args updated → shell.zig + transcript fixture updated). Seam: ADR 0007 slots 44/45, implemented_count 44→46 (report + syscall/monitor fixtures + 9 live gates moved to 46), ui.zig `audio_volume`/`audio_mute`, CHIME.BIN calls both (vol=50, unmuted) before its first blip. Live: `verify-live-sound-control.sh` **PASS 1/1 on VZ** — muted 76800-B beep drained exactly (pcm_status S_OK), unmuted 57600-B at vol=30 exact, CHIME's vol=50 landed in kernel state (post-app report), syscalls 44/45 calls=1; A1/A2/A3/A4 all re-ran green. Class A: virtio_snd 20, syscall 331, monitor 432, transcript byte-identical, build clean. Docs: ADR 0007 amendment, status.md M15 follow-up note, claim 9297, log, indexes refreshed. Nothing committed — the A1–A4 + 9297 change set is staged in the worktree.
