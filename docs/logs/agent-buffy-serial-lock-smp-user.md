# Log — agent/buffy/serial-lock-smp-user

- **2026-09-02** — *buffy (agent/buffy/serial-lock-smp-user)*: claim 2369 complete — locked console TX + the first USER task on core 1. **Change:** (1) `uart_puts`/`uart_putc` run under a holder-tracked `IrqSaveSpinlock` — IRQs masked for the whole write (a plain holder-tracked lock is NOT enough: SVC prints run with IRQs unmasked, so a tick could preempt mid-line and the resumed shell — same core, "holder" — writes inside the critical section; the gate caught exactly that on run 1: `heap-oworker advances=18816` in verify-live-zc boot 3). (2) `Task.pin_core` + `pin_task`; `next_runnable_for` skips pinned candidates on every other core; `exec -c<core>` flag → `exec_file_pinned` → pin_task (static + dynamic-ELF paths). (3) Secondary-core park: `tick` captures the WFE frame (`park_sp/elr/spsr[c]`) when starting a task from parked, and `stage_secondary_park(c)` erets back to it when sleep/wait/wait_event/exit has no eligible successor (the old orelse rolled the SVC back — the task never blocked). New gate `verify-live-smp1.sh` (exec -c1 SMP1.BIN: markers, 2-tick sleep with core-1 WFE park + core-0 wake, exit 0, reap; byte-exact greps; `smp: secondary runs=N task=SMP1.BIN` — the evidence line now prints EVERY run via a per-run name ring; observed runs 2/4 = SMP1.BIN, 1/3/5 = worker). **Evidence:** live-smp1 3/3; regressions live-zc 4/4 (interleave fixed), live-concurrent 2/2, cvc-echo 1/1, live-vf 4/4; full unit suite PASS (scheduler 362, exec 493, all modules); BSS PASS (507 368 B headroom); fmt/coordination clean. **Boundary:** unpinned user tasks stay on core 0 (other syscalls touch unlocked core-0 state); migration + cross-core wakeups out of scope. ✅ done.

- **2026-09-02** — *buffy (agent/buffy/serial-lock-smp-user)*: claim 9498 complete — the userspace-service gate: unpinned user tasks run on any core. **Change:** new `kernel/src/usergate.zig` — one holder-tracked, IRQ-masking spinlock serializing every entry into shared user-visible service state: the SVC syscall dispatch on any core, the EL1h monitor command dispatch, the exit/fault teardown, demand-paging faults, and (guardedly) the IRQ tick. IRQ-masking makes a holder always run to completion (any context may spin safely); holder tracking gives same-core reentrancy (exit teardown inside a syscall proceeds under the outer hold). Why coarse: file/window/network/mailbox/events/timer/registry were written single-core with shared module globals; per-subsystem locks need a full cross-module audit, one gate restores the single-core semantic. User tasks now spawn `secondary_ok` (register_user + register_exec_user_auxv); `pin_task`/`exec -c` is now a RESTRICTION over the any-core default (pinning to 0 = core-0-only, the WM registration path). The allocator got a lock (alloc → sched_lock → gate ordering never inverted); `IrqSaveSpinlock`'s `mrs daif` got the `builtin.is_test` guard (illegal at EL0 in host tests) — exposed by the new gate callers. **The live flake it closed (the real find):** the tick originally gated its WHOLE beat and skipped when contended — but a syscall on another core holds the gate for its whole duration, so a syscall-storming core-1 task starved core-0's ticks: the ring never rotated to the idle reaper and zombies from core-1 exits piled up (verify-live-zc 3/4: `reaped=0` with exit/proc reports printed but no reap, in both the core-0 and core-1 exit cases). Fix: the tick takes `sched_lock` FIRST and rotates the ring ungated — the preemption rotation (save/pick/stage) touches only per-core staging + ready↔running flips and is the claim-2369-proven shape; `on_tick`'s registry work (app timers, WM pacing, CPU limits) and the kill conversions are gate-gated via try and skip only THAT work. `stage_selected`'s kill conversion became gate-aware (try, never spin while holding sched_lock; on contention the task runs one more quantum with kill_pending intact). **Evidence:** full unit suite PASS, build PASS, BSS PASS (507 240 B headroom), fmt/coordination clean; class-B gates plain exec: verify-live-zc 4/4 TWICE + 1/1 (reaps now land ~2 lines after EVERY exit — pre-fix the core-1-exit zombies never reaped), verify-live-smp1 3/3 (pinned core-1 lifecycle: park/sleep/wake/exit/reap), verify-live-concurrent 2/2, verify-cvc-echo 1/1, verify-live-vf 2/2. **Boundary:** the shared ready ring itself is still single-lock (sched_lock) with the pre-existing double-pick race under concurrent core preemptions; cross-core task migration and per-subsystem locks are the next steps. ✅ done.
- **2026-09-03** — *buffy (agent/buffy/serial-lock-smp-user)*: claim 2792 complete — the per-service-domain lock set replacing the coarse claim-9498 userspace gate. **Change:** new `kernel/src/svclock.zig` (usergate.zig deleted): five IRQ-masking, holder-tracked `SvcLock`s — one per domain — under the canonical acquisition order `file < net < win < ev < kernel`, with `acquire_set`/`acquire_missing`/`try_take`/`release_set` helpers (try paths never spin, safe under `sched_lock`). syscall.zig's `doms_of(number)` maps every syscall to exactly its domain(s): FILE/NET/WIN/EV syscalls take their own lock and contend only with same-domain work, exec takes file+kernel, sys_exit takes the full set (its teardown touches every domain), registry/misc syscalls take kernel, ping/write/yield/sleep take nothing; per-core `call_counts`. monitor.zig: every command row got a `.dom` bitmask (26 domain-touching rows tagged), exec() holds kernel+domains. scheduler.zig: `exit_current`/fault teardown `acquire_missing(all_bits)`; reap takes kernel only; the tick try-takes ev+kernel and runs `on_tick` (app timers/WM pacing) only when it gets them, with rotation still ungated under `sched_lock`; the CPU-limit exit (previously synchronous in on_tick) became a ring conversion armed via the new `kill_pending_status` field (task kills with status 141 at its next selection); stage_selected's kill conversion and the c!=0 lone-task conversion try_take all five. WIN-domain internal touchers gated: `input.decode_keyboard_report` (interleaves driving_award reads with app-event pushes) takes win+ev via `acquire_missing`; the shell idle loop brackets its virtio-net RX/dhcp and TCP-RTO passes under `net`, its input-drain+compositor span under `win+ev`, and `persist_window_state_tick` under `file+win`. The concurrency exposed shared state: uaccess regions/window/latch/stats became per-core arrays (two cores mid-copy corrupt each other's fault recovery otherwise); the CSPRNG keystream got its own IrqSaveSpinlock (exec ASLR placement + DNS/connect ids run on any core); `process.bind` now publishes task_id BEFORE state=.running (a cross-core find_by_task reader must never observe a stale task_id). **The root-cause find:** the multi-lock helper originally iterated the domain enum through a runtime switch — Zig emitted a `.rodata` POINTER TABLE holding the lock globals' image-relative vaddrs, and the fully-PIC kernel never relocates those, so `lock_for(.net)` returned a raw 0x5f3xx address that faulted under ASLR (guest data abort in acquire_set at boot; host unit tests never catch it — no ASLR). Fix: every helper is unrolled to direct `file.acquire()`-style adrp references. **Evidence:** full unit suite PASS (svclock added to the verify-unit-tests MODULES list — 8 svclock tests: independence, canonical order, try_take rollback/held-skip, acquire_missing), build PASS, BSS PASS (503 336 B headroom), fmt/coordination clean. Live gates on the working-tree kernel (each gate rebuilds the image): verify-live-smp1 1/1, verify-live-concurrent 1/1, verify-live-zc 1/1 + 3/3 (the compiler's core-1 file syscalls contend under FILE only; reaps land promptly), verify-live-vf 4/4, verify-cvc-echo 1/1, verify-live-sb4-damage-tracking 1/1 (a user app presents windows through the WIN domain while the idle loop's win+ev brackets composite). verify-live-win is a PRE-EXISTING gate breakage (legacy `$RUN_DIR/disk-base.img` boot path the post-HF6 overlay migration never updated — fails identically on HEAD, unrelated to this change). **Boundary:** the shared ready ring is still one `sched_lock` (the pre-existing double-pick race under concurrent core preemptions); cross-core task migration remains future work. ✅ done.

