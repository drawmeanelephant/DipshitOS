# Log — agent/t3code/729-desktop-manifest-21

- **2026-09-02** — Claimed issue #729 (`verify-live-desktop` red). HEAD
  `98a5478` == `origin/main` (already newest; no fetch needed). Mapped
  ACTIVE claims: only 9094 (`exceptions`/`scheduler`/`virtio_file`/
  `process`) and 1432 (`exceptions`/`monitor`) are 🔄; neither file in
  this claim's Touches overlaps them. Ran
  `bash tools/verify-live-desktop.sh` on HEAD: CALC/NOTEPAD/TOP/DESKTOP
  all OK, `desktop: launch CALC.BIN pid=4` + `calc: ready` observed —
  #729's ENOENT is fixed (M34 host-file-channel exec). Remaining red is
  the stale `apps=19` needle vs observed `apps=21` (sexiburger rows,
  5845d7f). Bumping the needle in `verify-live-desktop.sh` and
  `verify-live-file-browser.sh`, then re-running the gate.
- **2026-09-02** — Re-ran `bash tools/verify-live-desktop.sh`: **PASS**
  (CALC/NOTEPAD/TOP/DESKTOP OK, MANIFEST OK, LAUNCH OK, SYS_EXEC OK).
  `bash tools/verify-coordination.sh`: ok. Claim 6854 flipped ✅ done.
  Opening PR (closes #729).
