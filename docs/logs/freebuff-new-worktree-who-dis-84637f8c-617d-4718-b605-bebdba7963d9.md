# Log — `m14-s1-clipboard`: the bounded kernel clipboard (claim 0169)

## 2026-08-18 — branch opened

- Card S1 of Milestone 14 (issue #175): `sys_clipboard_set`/`sys_clipboard_get`
  (ADR 0007 slots 38–39), one bounded kernel clipboard buffer (pure BSS),
  NOTEPAD copy/cut/paste, and the terminal `clip` command.
- Branch based on `main` (`fa01bf9`).

## 2026-08-18 — done

- `kernel/src/clipboard.zig`: ONE machine-global fixed 512-byte BSS buffer +
  length + set counter (`init`/`set`/`get`/`current_len`/`sets`), zero heap.
- `kernel/src/syscall.zig`: slots 38/39 registered (`implemented_count` 38→40);
  handlers marshal bytes through the claim-6120 uaccess window, enforce the
  process-caller check, and map `EFAULT`/`EINVAL`; `clipboard.init()` in
  `init`; report + table tests updated to 40.
- `kernel/src/monitor.zig`: the `clip` command (`clip <text...>` sets it,
  `clip` pastes it) — registry 44→45; deterministic help + report output.
- `kernel/src/shell.zig` + `tests/transcript-console.txt`: help listing
  gained the `clip` row (fixture regenerated byte-exact).
- `user/src/lib/ui.zig`: `clipboard_set`/`clipboard_get` wrappers.
- `user/src/notepad.zig`: `TextBuffer.insert_slice` + Ctrl+C/Ctrl+X/Ctrl+V
  copy/cut/paste (whole-buffer, honest bound).
- `docs/decisions/0007-syscall-abi.md`: amendment freezing slots 38/39.
- Stale live gates updated `implemented=38` → `40`
  (net-tcp/udp-syscall, sleep, win-syscall/close/move).
- Class A: full `verify-portable` gate set green (456 shell tests, all
  module goldens, test-console byte-identical, build/image/inspect/context,
  coordination + mutations).
- Class B: `tools/verify-live-clipboard.sh` **PASS 1/1 on VZ** — terminal
  `clip` set/overwrite/get round-trip + `implemented=40` with slots 38/39
  present.

## 2026-08-18 — S2 claimed (claim 7323)

- Card S2 of Milestone 14 (issue #176): the bounded per-process application
  timer facility (ADR 0007 slots 40–41) posting `TIMER` events on the ADR
  0009 queue — `kernel/src/app_timers.zig`, the `TIMER` event kind (9),
  scheduler-tick fire hook, process lifecycle reset, `ui.zig` wrappers,
  `TIMER.BIN` proof program, live gate `tools/verify-live-timers.sh`,
  ADR 0007 amendment.

## 2026-08-18 — S2 done

- `kernel/src/app_timers.zig`: ONE countdown timer per process (fixed BSS
  arrays: armed flag + remaining + set/fired/cancel counters), zero heap;
  `set` clamps 0→1 and truncates at the 3600-tick bound; `cancel` reports
  whether a timer was pending; `on_tick` counts every armed timer down and
  fires exactly one `TIMER` event into the process's ADR 0009 queue.
- `kernel/src/events.zig`: the `TIMER` event kind (9).
- `kernel/src/scheduler.zig`: `app_timers.init()` in `init`; the fire runs
  from `on_tick` (the SAME host-testable tick seam as the sleep wakeups);
  the slot resets on user-task registration and on process exit (a dead
  process's timer can never fire). `kernel/src/exec.zig`: slot reset on
  exec.
- `kernel/src/syscall.zig`: slots 40/41 registered (`implemented_count`
  40→42); handlers enforce the process-caller check and return EINVAL for
  non-process callers; report + table tests updated to 42.
- `user/src/lib/ui.zig`: `timer_set`/`timer_cancel` wrappers + the
  `EVENT_TIMER` constant.
- `user/src/timertest.zig` → TIMER.BIN (twenty-third ESP program): arm →
  BLOCK in wait_event → TIMER event → cancel-none (0) → re-arm → fire →
  cancel-pending (1) → exit 23. Build/image plumbing in build.zig,
  make-image.sh, mkfat32.py; unit-test module list gains `app_timers`.
- `docs/decisions/0007-syscall-abi.md`: amendment freezing slots 40/41.
- Stale live gates + `verify-live-clipboard.sh` bumped `implemented=40` →
  42.
- Class A: full `verify-portable` gate set green (462 shell tests, all
  module goldens, test-console byte-identical, build/image/inspect/context,
  coordination + mutations).
- Class B: `tools/verify-live-timers.sh` **PASS 1/1 on VZ** — TIMER.BIN
  arm/block/fire/cancel round-trips + `implemented=42` with
  `sys_timer_set calls=3` / `sys_timer_cancel calls=2`.

## 2026-08-18 — S3 claimed (claim 3289, branch `freebuff/m14-s3-composition`)

- Card S3 of Milestone 14 (issue #177): the composition capstone — NOTEPAD
  copy/paste (S1) with a timer-driven cursor blink (S2) proven together in
  one EL0 app. Branch based on `origin/main` (`750c10f`, PR #182 merged).
- Claim file `docs/claims/3289-m14-s3-composition.md`; live gate
  `tools/verify-live-m14-composition.sh`.

## 2026-08-18 — S3 done (claim 3289, branch `freebuff/m14-s3-composition`)

- The composition capstone: NOTEPAD uses S1+S2 together in ONE EL0 session.
- `user/src/notepad.zig`: the timer-driven cursor blink — `cursor_visible`
  toggles on every `EVENT_TIMER` (kind 9), the EL0 loop re-arms
  `sys_timer_set` after each fire (2-tick cadence), a failed re-arm parks
  the cursor visible (honest degradation); plus the argv `selfdemo` mode
  (claim 4636's entry contract — argc/argv in x0/x1): paste the shared
  clipboard → copy it back → blink 6 times → exit 43.
- Class A: notepad module 21/21 (new blink + selfdemo-bound tests), full
  gate set green.
- Class B: `tools/verify-live-m14-composition.sh` **PASS 1/1 on VZ** —
  `clip hello world` (terminal half) → `exec NOTEPAD.BIN selfdemo` →
  `selfdemo pasted` → `selfdemo copied` → 6 `cursor blink` markers →
  `selfdemo done` → `notepad: exiting 43` → `tasks user-exec exited
  status=43`, and the same-boot syscalls report shows `implemented=42`
  with `sys_clipboard_set calls=1` / `sys_clipboard_get calls=1` /
  `sys_timer_set calls=7`.
- Input seam (issue #179): reproduced `events=0` on a fresh boot — the
  gate drives the composition via argv selfdemo, records the seam state in
  the report, and notes that scripted Ctrl+C/V chords regain live coverage
  when the seam recovers (the chord path itself is unchanged and
  host-tested).
