# Log — `agent/buffy/flake814-shell-park-stack`

- **2026-09-02** — *buffy (freebuff/…issues-281dbccf…)*: claim 8554 — takes issue #814 (filed from claim 3997's Z2a flake): `shell.boot_and_park`'s merged 0x4760 frame overflows the 16 KiB handoff boot stack; the park loop + parked IRQ frames land in the loader-data pocket below `stack_base`, corrupting state (the #803/#810 family). Fix direction per the issue: move the park loop onto a dedicated static task stack (32 KiB, `scheduler.task_stack_size`) via a naked SP-switch trampoline, keep pre-park setup on the boot stack, host tests pure Zig. Then prove plain exec in `verify-live-zc.sh` at BOOTS=8 on VZ. 🔄 starting.

- **2026-09-02** — *buffy*: intermediate design #1 (naked `park_entry` trampoline + fn-pointer call, park loop only on the stack) built and booted — silent death right after the SP switch. Disassembly showed why: the optimizer tail-merged the naked body into `boot_and_park`'s .virelairc exit (`b.ls 0x8d2c` jumped INTO trampoline #2 with leftover registers) so `mov sp, x2` ran with rc-remaining → sp = garbage. Also re-measured the frames: the setup-only path is ~0x4010 (Shell.init return staging at sp+0x3c40 + rc_buf + inlined restore/dispatch), NOT ~0x760 — the ENTIRE 16 KiB boot stack — so moving only the loop never could have fixed it.
- **2026-09-02** — *buffy*: final design implemented: `boot_and_park` is a ~0x50-frame wrapper; one self-contained inline-asm block saves boot SP/LR, loads x0 = monitor / x1 = park_stack top (all-register operands — no symbol fixups, no fn-pointer call, nothing to outline), switches SP and `bl`s `park_body` — the WHOLE RX path (init, history/env/window restore, .virelairc, loop) on a dedicated 64 KiB static `park_stack`. Measured in the build: boot_and_park prologue `sub sp,#0x30` on the boot stack; park_body ~0x3780 + nested ~0x4a10 dispatch frames ≪ 64 KiB. Class A green: fmt, unit tests (24/24 + all modules), verify-bss-budget (478,120 B headroom), verify-coordination, byte-exact transcript gate.
- **2026-09-02** — *buffy*: class-B proof: `verify-live-zc.sh` PASS **3/3 boots on VZ** (BOOTS=1, then BOOTS=2) — every phase green incl. the plain `exec MAIN.ELF` → `tasks user-exec exited status=72` and the `rx-zc-ok` echo, fatal=0. Decisive against the issue's ~50% plain-exec flake. Residual: intermediate marker builds and unmodified HEAD also crash/freeze post-ZC in the claim-9094 audit drain (x23/x26 clobbered with GIC values 0x1e/0x10040080; elr offset 0x37b6c, far 0x20080296) — evidence of the separately-tracked #810 family, outside #814's scope. Claim flipped ✅ done.
- **2026-09-03** — *buffy*: issue #850 (from #814's residual, the #810 family) — root cause + fix. Fresh deterministic repro on `verify-live-exec` (2/2 boots, EL1h sync `esr=0x97080010` S1PTW, `far=0x20080296`, elr kernel+0x37b7c) matched claim-9094 run-13's IDENTICAL register image (x21=0x2008011e, x23=0x1e PPI, x26=0x10040080 GICR, x19=0x10030000 GICD, x25=0x40000000) at a DIFFERENT elr — a task resumes holding the GIC/timer handler's register image. Root cause: (a) same-EL IRQ nesting (vector stubs never masked IRQ; AArch64 same-EL entry leaves DAIF unchanged; nested timer IRQ inside the tick overwrote the global `resume_frame`, so the outer handler saved/restored the nested mid-handler frame) and (b) SMP (2-vCPU VM; core 1's exception entry unconditionally writes the same global, clobbering core 0's staged frame). Fix: `msr daifset, #2` at `exc_fp_common` entry + per-core `resume_frame`/`resume_sp_el0` arrays (exceptions.zig, scheduler.zig, host-test index in syscall.zig). Also migrated 12 stale live-gate scripts (args/ipc/m21-*/scale/win*) from the removed `$RUN_DIR/disk-base.img` writable-copy path to `GATE_RUNNER_ARGS` (broken since M34 HF6 issue #740) + hardened ipc's unbound `nsends`. Verified: class-A green, transcript byte-identical, **19/19 live VZ boots across 9 gates** (exec 5/5, vf 3/3, zc 3/3, procs 2/2, lifecycle 2/2, concurrent, long-lived, ipc, scale). Issue #850 filed with full evidence; claim 9094 flipped ✅.
