# Log — `site-current-state`: refresh the public GitHub Pages site to current reality (claim 1662)

## 2026-08-16 — branch work

- **buffy (`docs/site-current-state`)**: claim 1662 → the public `site/` tree
  had stopped at milestone seven; updated 13 pages to reflect milestones
  0–12 shipped, M13 in progress (B2 manifest + B3 FILE.BIN browser live,
  B1/B4 open), the 34-slot syscall ABI (slots 0–33), the RFC 1035 DNS
  resolver, the userland file ABI, the desktop apps (CALC/NOTEPAD/TOP/
  DESKTOP/FILE), event routing, the 43-gate VZ sweep, and the current
  honest bounds (no TCP server/listen, no routing beyond NAT, the M8 U4
  pointer-focus live seam as manual/CG gates). Evidence: the docs-gate
  sequence re-ran green locally against the pinned Boris revision
  (`boris validate` + full compile, rendered HTML contains the new
  content). No `docs/status.md` or planning-tree edits. → ✅ done.
