# Log — agent/buffy/audit-percore-counters

- **2026-09-02** — *buffy (agent/buffy/audit-percore-counters)*: claim 8513 opened — cross-core IRQ-context counter audit (claim-7339 follow-up; the user asked to fix the remaining `handled_count_value`-class shared globals). 🔄 in progress.
- **2026-09-02** — *buffy (agent/buffy/audit-percore-counters)*: claim 8513 complete. **Audit:** walked the secondary-core exception path (stub → exc_dispatch → irq_dispatch → handle_sgi/timer.arm/note_irq → eoi). **Racy and fixed:** `exceptions.handled_count_value` (RMW at exc_dispatch entry on every core), `gic.irqs_acked`/`first_intid` (RMW from note_irq on every core's ack). **Hardened despite being single-core today:** `virtio_custom.irq_count`/`irq_first` (SPIs IROUTER-pinned to PE 0, but the path is reachable on every core). **Verified safe:** timer.* (core-0-gated handle), smp.ipi_counts (per-core rows), resume_count_value/resume_armed (sync-only path), dispatcher fns (boot-time writers), uaccess.* (SVC, core 0). **Fix:** per-core arrays in all three modules (literal `cpu_cores = 4` in gic/virtio_custom — smp imports gic, no cycle; host-guarded `core_index()` reading MPIDR_EL1.Aff0); `handled_count()` and `gic.acked_total()` sum; monitor prints `acked_total()` + `first_intid[0]`; main.zig cvspike reads slot 0 — the gate-pinned `cvspike: irq= first=0x45` line is byte-identical. **Evidence:** unit suite PASS, BSS PASS (511 208 B headroom), verify-live-zc 4/4, verify-cvc-echo 1/1 (guest-irq + first=0x45 green), verify-live-vf 4/4, all plain exec; fmt/coordination clean. ✅ done.