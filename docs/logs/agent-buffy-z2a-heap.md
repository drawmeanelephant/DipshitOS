# Log — agent/buffy/z2a-heap

- **2026-09-02** — *buffy (agent/buffy/z2a-heap)*: claim 3997 opened → issue #756 (Self-hosting Z2a: Heap). 🔄 in progress.
- **2026-09-02** — *buffy (agent/buffy/z2a-heap)*: claim 3997 complete → issue #756. Added `zc.mmap` (slot 63, RW|ANON|PRIVATE|POPULATE) to the compiler's builtins and the prelude shim plus pointer-form file ops; root-caused and fixed a kernel defect the card exposed — `sys_mmap` armed uaccess regions only transiently while `handle_svc` re-arms from the task TCB every entry, so kernel copies into mmap'd memory EFAULTed (now registered in the TCB too, matching exec.zig). Fixture `tests/zc-corpus/z2a-heap.z` does a chunked bump-arena build (1 KiB landing zone + append-copy to the bump offset, then compact + drain in ≤2048 B writes) with a byte-exact DATA.TXT→OUT.TXT round trip. Gate seeds MAIN.Z + 2500 B DATA.TXT host-side, runs `strace exec ZC.BIN` (asserting the full 1838-byte source read — discovered ZC.BIN reads its source in a single file_read, so MAIN.Z must stay <2048 B; the 2366-byte fixture failed in-guest at line 52 = byte 2049), then `exec MAIN.ELF`, and cmp's OUT.TXT byte-for-byte. PASS 3/3 final (six clean boots of the shape; fileeq=1, fullread=1, fatal=0). Also documented a latent kernel-shell race (shell.boot_and_park idle loop faulting on corrupted callee-saved regs around task-exit reports; plain-exec boots flaky, strace-pacing reliable) for its own card. 32/32 zc unit tests, full monitor suite, and BSS budget (541 960 B headroom) green. ✅ done.
