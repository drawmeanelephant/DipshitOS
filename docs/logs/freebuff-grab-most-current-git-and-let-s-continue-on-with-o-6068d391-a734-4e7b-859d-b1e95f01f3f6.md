# Log — freebuff/grab-most-current-git-and-let-s-continue-on-with-o-6068d391-a734-4e7b-859d-b1e95f01f3f6

- **2026-08-09** — *buffy (freebuff/grab-most-current-git-and-let-s-continue-on-with-o-6068d391-a734-4e7b-859d-b1e95f01f3f6)*: claim 2233 → synced to newest `origin/main` (`706712c`, PR #52 / claim 7392 merged; branch had 0 unique commits, clean fast-forward `181c2fc..706712c`) → claimed the status continuation (verify the full gate suite at the newest HEAD + reconcile stale status/claim surface) → **re-ran the full gate set at the merged HEAD `706712c` → class A 11/11 + class B 10/10 all green** (serial takeover `zig build run` — banner + 27-descriptor map `key=0x2d4` + `dipshit>` prompt, runner exit 0; bad-handoff `kernel_rc=0x2`; marker ladder to `M2_TXOK!`; nvram-console 82 chunks/5130 B reconstructed; host-console scripted + PTY; live-transcript RX 1/1; live-fs persistence pair 1/1; live-timer 1/1 with the poll heartbeat — IRQ delivery stays VZ-blocked per claim 7948; live-reboot 2/2; live-exceptions 1/1) → status surface checked: claim 7392's stale-⛔ corrections (0002/0011/0013) and claim 5162's AGENTS.md/roadmap updates held at this HEAD, no drift found → added the reverify pointers to `docs/status.md` (gate-table header chain + VZ serial gate row) → indexes regenerated, coordination gate + test-coordination green → ✅ done (evidence: `artifacts/gates-reverify-20260809-706712c.txt`, `artifacts/classB-chunk{1,2,3}-706712c.log`).
- **2026-08-09** — *buffy (freebuff/grab-most-current-git-and-let-s-continue-on-with-o-6068d391-a734-4e7b-859d-b1e95f01f3f6)*: claim 6420 (FAT32 storage driver) → claimed the milestone-three card: real post-exit ESP reads/writes via a virtio-blk transport + FAT32 driver, replacing claim 3475's NVRAM persistence medium → **implemented + verified → ✅ done**: new `kernel/src/fat.zig` (GPT + FAT32 mount/list/read/write over injected sector I/O, 11 host tests; validated against the real `mkfat32.py` image with a throwaway host harness — mount, list, read KERNEL.BIN at its true 767280 bytes, write + remount readback) and `kernel/src/virtio_blk.zig` (modern virtio-pci transport; queue 4, one request at a time, drain-before-submit, generous completion poll); `esp.zig` rewritten to the live-FAT window (NVRAM fs path removed); mmu.zig takes multiple extra Device windows; monitor/shell/transcript/unit-test gate updated. **Two hardware discoveries on real VZ**: (1) the disk is virtio-blk **DID 0x1042** (class 0x018000), not the spec's 0x1041 — the standard DID table doesn't apply (console is 0x1043, entropy 0x1044; ECAM 0xff000000, MCFG rev-1 layout); (2) VZ **resets the device at ExitBootServices** (status reads 0 post-exit, queue dead — the mount/snapshot all run pre-exit so the break only showed up on the first post-exit `write`), fixed by re-arming the queue post-MMU (`blk_rearm`, common-config MMIO writes verified DRIVER_OK; PCI discovery stays pre-exit per claim 0013). Also fixed en route: the gate's `script-expect "hello world"` matched the echoed input line and killed the VM before the write finished — expect is now the cat-reply + prompt; the on-disk listing after reboot shows the FAT 8.3 short name `HELLO.TXT` (case-insensitive assertion). **Class A green** (fmt, unit tests incl. fat 11/11, transcript byte-identical, build/image/context, coordination, test-coordination 15/15, mmu-debt) **and class B green on VZ**: `verify-live-fs.sh` 1/1 pair (run A `write: ok (persisted 11 bytes to FAT on the ESP)` + `hello.txt [esp]` listed + cat reply; run B same disk image lists `HELLO.TXT [esp]` + cat — persistence on the disk itself) plus live-transcript RX 1/1, marker `M2_TXOK!`, host-console, live-reboot 2/2, live-timer, live-exceptions 1/1. Evidence: `artifacts/gates-fat32-6420.txt`, `artifacts/live-fs-*`. Docs updated: status.md, roadmap.md, march-m15.md, README.md, architecture.md, testing.md, gate-inventory.md, hardware-contract.md (the disk contract now records DID 0x1042 + the ExitBootServices reset), AGENTS.md.
