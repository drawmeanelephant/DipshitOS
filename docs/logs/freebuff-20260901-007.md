# freebuff-20260901-007 — buffy

- **2026-09-01** — *buffy (freebuff/okay-shower-thoughts-here-we-re-using-fat32-for-so-b0ce3067-2b04-4b81-984d-9fd76bfcf123)*: claim 7188 started. **M35 W2 (#763) — wasm run entry + live proof.** Wired `user/src/wasm.zig`'s `_start` to the DSK3 argv contract (`exec WASM.BIN HELLO.WASM` — the loader refuses argv for ELF images, card 3e), split `parse` → `parseInto` (the 77 KiB Module return frame would smash the 32 KiB guest stack), read the module through the guest file API (slots 23/24/26, 32 KiB chunks — the HF4 share transport), mmap'd the 2 MiB linear memory at runtime (slot 63 — a 2 MiB `.bss` would blow the 256 KiB staging budget), and dispatched the two W2 imports: `env.write` → console byte-exact, `env.exit` → `sys_exit` status. Switched the build.zig wasm target to segmented DSK3 (51,104 B: text 20,480 / data 30,576 / bss 144,888). `zig test user/src/wasm.zig` 13/13 — the C module's fixture caught four real bugs (code-section count = defined funcs; data-section flags-0 quirk; `seen[11]` OOB; clang's trailing `unreachable` after noreturn exit → `guest_exit` trap). New gate `tools/verify-live-wasm.sh`: host builds the C hello with `zig cc -target wasm32-freestanding -nostdlib -fno-sanitize=undefined -g0` — byte-deterministic with a fixed output basename (name-section only entropy; `-g0` strips the DWARF source-path leakage), pinned sha `edb6407b…` (411 B, committed as `user/src/wasm-corpus/hello-env.wasm`) — drops WASM.BIN + HELLO.WASM into the share, asserts `hello, wasm!` byte-exact + `tasks user-exec exited status=55` + shell alive. BSS before/after artifacts written. Monitoring checks.
