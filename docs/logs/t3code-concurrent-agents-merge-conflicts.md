# Log — t3code concurrent-agents merge conflicts

- **2026-08-24** — *ox-alpha (t3code/concurrent-agents-merge-conflicts)*: claim 2599 opened → move coordination-index generation from every branch to a merge-time CI bot (drift-tolerant PR gate, structure-only local check, resolve-by-regeneration recipe for legacy index-region conflicts) → motivated by the two recent conflicts, both inside generated index regions → 🔄 in progress.
- **2026-08-24** — *ox-alpha (t3code/concurrent-agents-merge-conflicts)*: claim 2599 PR #532 merged (ae8610b); first live bot run **failed** as predicted-by-nothing: the main ruleset forbids direct pushes (require-PR + no admin bypass per docs/branch-protection.md), remote rejected `HEAD -> main` after all 3 rebase retries. Correction to this branch's earlier design: workflow now force-updates one branch (`indexes/bot-regenerate`) and keeps a single auto-merge squash PR open; requires `INDEXES_PAT` secret and "Allow auto-merge" enabled; `[skip ci]` dropped so the required check reports on the bot PR. Tables on main are currently missing post-merge rows (tolerated by the drift-immune gate) until the first bot PR lands.
- **2026-08-24** — *ox-alpha (t3code/concurrent-agents-merge-conflicts)*: claim 2599 ✅ DONE. Second live failure also fixed and observed: run [32722456541](https://github.com/drawmeanelephant/DipshitOS/actions/runs/32722456541) pushed as `github-actions[bot]` (403) because `actions/checkout@v4` forces a GITHUB_TOKEN `extraheader` that beats credential helpers; #534 unsets it + `gh auth setup-git`. Then OBSERVED end-to-end: merge → workflow run 32723105968 ✅ → bot pushed `indexes/bot-regenerate` → PR #535 opened → required macOS check pass (4m42s) → auto-squash-merged `2954d68` → main's tables carry the 2599/5069 rows, logs index at 93 rows. Branches never touched the tables. Coordination suite 21/21; `verify-coordination` ok. Residual known-good behavior: every future flip/merge triggers one more bot cycle automatically.
