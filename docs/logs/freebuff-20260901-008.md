# Log — freebuff/okay-shower-thoughts-here-we-re-using-fat32-for-so-b0ce3067-2b04-4b81-984d-9fd76bfcf123

- **2026-09-01** — *buffy (freebuff/okay-shower-thoughts-here-we-re-using-fat32-for-so-b0ce3067-2b04-4b81-984d-9fd76bfcf123)*: claim 3456 started. **M35 W3 (#764) — import breadth: the frozen env.* surface + wasm window/file apps.** Implemented all 29 contract imports + the W2 write/exit pair in `user/src/wasm.zig`: a comptime `frozen_imports` signature table that validation enforces BEFORE start (`UnknownImportModule` for WASI etc., `UnknownImport`, `ImportSignature`), one generic `svcN` seam + 20 new syscall wrappers (file 23–27+34–37, win 12–20, audio 42–45, timers 40/41, mmap 63 + munmap 64, procs 7, wait 8), per-import wasm range checks (OOB traps `bounds`, never EFAULT — contract §3), and a host capture seam (call log + per-import canned returns + out-buffer fills) so every arm is testable without a real svc. `max_imports` 16→32 (+640 B Module, measured). Added `tests/virelai.h` (all 30 imports + constants + 40 B/16 B struct asserts), `tests/virelai-probe.c` + `tools/verify-virelai-probe.py` + `zig build shim-check` (probe import table == frozen 30 — the "contract alone" acceptance item), and the two live fixtures `tests/winapp.c`/`tests/fileapp.c` → pinned `user/src/wasm-corpus/winapp.wasm` (603dc2ab, 982 B) / `fileapp.wasm` (9f31d07e, 767 B), byte-identical across fixed-basename rebuilds. `zig test` **20/20** (7 new W3 tests: validate rejects foreign/unknown/badsig; all 30 dispatch with canned returns + log; file path/buffer carrying; win_get/query out-fills; audio/timer/mmap/procs/wait; OOB pointers trap), gate script extended to one `--display` boot running hello + winapp (fill observed via scanout capture + dui rect row + EINVAL −1 proof) + fileapp (FILE.TXT 512 B byte-exact echo, exit status 512). WASM.BIN 55,200 B ≈ 196 KiB staged, inside budget. Verification in flight.
- **2026-09-01** — *buffy*: rebased onto origin/main through HF6 (c6c6ddc — fat.zig/virtio_blk deletion, single host share). Auto-merge kept W3 intact; kept only the file_table clamp fix (W3 gate finding: host READ of remaining file smashed 128-B staging without `min(r.data.len, out_buf.len-total)`), dropped the scheduler `pc=` delta (unrelated to W3, origin/main test expects no pc). Re-verified class-A: `zig test user/src/wasm.zig` 20/20, `zig build shim-check` PASS (frozen 30), `zig build`/`image`/`wasm` PASS (55200 B, 24576/30576/152728), `zig fmt --check` PASS, `verify-coordination` PASS, `verify-bss-budget` PASS (10959608/11534336), deterministic fixture rebuilds byte-identical (603dc2ab/9f31d07e). Ready for PR.
- **2026-09-01** — *buffy*: review round (pre-merge, PR #807). Four findings from the pre-merge review fixed in `user/src/wasm.zig`: (1) `checkFrozenImport` never checked the RESULT type — `env.win_open -> i64` validated and then read an i32-pushed `Value` through the i64 lane (stale-stack high bits); result type is now part of the frozen-signature check. (2) `file_close` discarded the kernel slot-26 result and always returned 0; it now passes through (contract §5.1 EBADF). (3) Contract §3 zero-length discipline: len==0 valid at ANY ptr in write/file_read (was: OOB-ptr trap), and `storeSlice` no longer indexes the store at a wild ptr for len==0 (latent panic: dir_list root form with a garbage path_ptr). (4) `dir_list` entry math in u64 (`max_entries*40` wrapped u32 past the range check); capture-seam out-fills clamped via `captureFill`; `tools/verify-live-wasm.sh` clears stale `artifacts/gpu-screen-*` at run start so a previous run cannot satisfy the red-fill proof. Known nit left deliberately: winapp.c's dead `w3: unreachable\n` write declares 17 bytes of a 16-byte literal — repinning the fixture for a never-taken branch is not worth re-rolling the class-B gate (noted in the claim for W5's rebuild). Re-verified class-A: `zig test` **23/23** (20 + 3 new: ImportSignature result-type rejection, §3 zero-length any-ptr, dir_list wrap-proof), `shim-check` PASS, `zig fmt --check` PASS, `zig build`/`image`/`wasm` PASS (WASM.BIN byte-identical 55200 B, 24576/30576/152728), `verify-coordination` PASS, `verify-bss-budget` PASS (10959608/11534336), fixtures unchanged (603dc2ab/9f31d07e). Claim flipped ✅ done.- **2026-09-01** — *buffy*: class-B recalibration on the review host (Apple silicon, macOS 27). First live reruns of the gate: boot 1 hit **flake #803** verbatim (data-abort `sp=0`/`x2=0x8000`/"parking" right after the two short wasm reaps, mid-spin of the window app); boot 2 hit it again silently (VM ended, fileapp console output lost — the issue's console-corruption signature). The window path itself was perfect both times (dui row rect=100,100,96,48, blits≥1 post-raise, red fill 10,436-10,440 px vs the claim's measured 10,436). Boot 2 also exposed a REAL gate bug the author's run must have dodged: the winapp's 120,000,000-iteration hold spin never finished inside the runner's 120 s window (>80 s observed, still spinning) — `status=21` was unreachable. Fix: hold reduced to 15,000,000 iterations (~10-15 s, the comment's stated intent) + the dead 17→16 write-length nit fixed in the same source; `winapp.wasm` regenerated and repinned `ee33f184df3a5fed1cfc610b467b3595814afa2ab751cfc6fb84f85a32e353f6` (982 B, byte-identical across same-basename rebuilds — determinism recipe re-verified), gate pins updated, `fileapp.wasm` untouched (9f31d07e). Claim notes updated; rerun-until-clean per the #803 protocol.
