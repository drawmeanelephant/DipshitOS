# Claim: ProgressBar widget for ui.zig (GH #220)

- **Owner:** Muse Spark (`agent/buffy/arc1-progressbar`)
- **Prompt / plan:** `docs/m17-desktop-completeness.md` Arc1 Widget Depth + GH #220
- **Scope:** Arc1 pure `user/src/lib/ui.zig` ProgressBar — value f32 0.0–1.0, bordered rect + accent fill, 0%/50%/100% fill math, optional centered label with contrast inversion, indeterminate 20px sliding block driven by `sys_timer_set` slot 40 / `TIMER` kind 9, no new ABI/event, host tests + BSS budget gate, FETCH.BIN nice-to-have
- **Depends on:** M17 C1-10 done (ff19197), #218 ScrollView + #219 Checkbox/Toggle done (main 80f5b44), M14 timers live (slot 40)
- **Status:** ✅ done 2026-08-21 — `user/src/lib/ui.zig` ProgressBar + 5 host tests, `zig test` 22/22, `verify-bss-budget` 9788088/11534336, `verify-coordination` PASS

## Notes

Implements ProgressBar per GH #220 grooming: `ProgressBar{rect, value: f32, label: []const u8, indeterminate: bool, offset: i32, dir: i32}`. Determinate: `value` clamped 0–1 (NaN→0), `fill_w = floor(inner_w * value)` where `inner = rect.inset(1,1)`, `draw_rect` bg surface + border + accent fill, label centered via per-char `draw_char` with contrast inversion (over-fill/block → 0xffffff white on accent, over-bg → theme_text_primary). Indeterminate: 20px block (`indeterminate_width` 20, step 4) slides via `handle_event(EVENT_TIMER kind 9)` → `tick()` which bounces at `max_offset = inner.w-20`; arming pattern is app-side `timer_set(2)` per grooming, widget only consumes TIMER. Host tests cover 0%/50%/100% fill + 25% floor + clamp + NaN, indeterminate slide 0→20→max→bounce→0 (step 4, dir flip), per-char contrast over fill vs block, draw no-panic for all states + small rect edge, initWithValue + theme switch (light/dark/amber). Zero heap, pure BSS value type. `FETCH.BIN` consumer is nice-to-have per grooming — widget is ready (`ProgressBar{rect, value, label, indeterminate}` + `handle_event` + `timer_set` loop pattern documented), no FETCH.BIN mutation required for gate. Verified: `zig test user/src/lib/ui.zig` 22/22 PASS, `zig build` PASS, `verify-bss-budget` PASS 9788088/11534336 (1746248 headroom), `zig fmt --check` PASS, `verify-coordination` PASS.
