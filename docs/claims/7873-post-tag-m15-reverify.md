# Claim: Post-tag M1.5 re-verification at `m1.5-interactive-monitor`

- **Owner:** buffy (`freebuff/grab-latest-git-and-check-out-status-and-let-s-get-15800f9b-d72c-4035-ac26-ae778c52b296`)
- **Prompt / plan:** user request 2026-08-09 — "Reconcile the status.md gate table and testing.md with a fresh full class A + class B re-verification run at the m1.5-interactive-monitor tag, and record the evidence as a post-tag reverify claim." The milestone was tagged `m1.5-interactive-monitor` (commit `74a51f3`, claim 3475); this claim re-runs the **entire** class A + class B gate set at that tag, reconciles `docs/status.md` (gate table + reverify note) and `docs/testing.md` (checklist evidence) with the results, and records the evidence as the post-tag reverify.
- **Scope:** full class A set (`just verify-portable`: fmt, unit tests, transcript, build, image, inspect, swift, context, coordination, coordination tooling, mmu-debt) + full class B set (`just verify-vz`: serial takeover, bad-handoff, marker, nvram-console, host-console, live-transcript, live-fs, live-timer, live-reboot) + the live-exceptions gate (claim 9746, a class B gate in `docs/gate-inventory.md` not listed in the `verify-vz` aggregate); docs reconciliation (status.md gate table rows + reverify header, testing.md gate checklist); claim + log + indexes.
- **Depends on:** claim 3475 (the milestone close-out it re-verifies), the class A/B gate tooling itself.
- **Status:** ✅ done 2026-08-09 — **the full class A + class B gate set re-run green at the `m1.5-interactive-monitor` tag (`74a51f3`, clean tree)** (summary evidence `artifacts/gates-reverify-20260809-m15-tag.txt`). Class A (11/11): fmt, unit tests (95), `test-console` (110 + byte-identical transcript), build, image, inspect, swift runner build, context, coordination, coordination tooling (15 tests), mmu-debt. Class B (10/10 VZ hardware gates): serial takeover (`zig build run` — banner + memory map + `dipshit>` prompt), bad-handoff (`kernel_rc=0x2`), marker (ladder to `M2_TXOK!`), nvram-console (post-exit console bytes reconstructed), host-console, live-transcript (live RX), **live-fs (ls/cat/write persist through reboot, 1/1 pair)**, live-timer (GIC+CNTP polled heartbeat; IRQ delivery still VZ-blocked per claim 7948), live-reboot (reboot resets / shutdown powers off, 2/2 boots), live-exceptions (real `udf` reported + resumed). Docs reconciled: `docs/status.md` gate-table reverify note + `docs/testing.md` post-tag reverify entry; claim/logs indexes refreshed.

## Notes

**Why this card:** the gate table in `docs/status.md` carries dated re-verification evidence per HEAD (claim 8073 at `076ddf1`, 2026-08-08); the milestone tag is a new frozen point, so a full re-run at the tag gives the tag itself dated evidence and flushes any doc drift introduced by the close-out sweep.

**Verification:** every gate below is run at `m1.5-interactive-monitor` (= `74a51f3`, clean tree) and its own evidence saved under `artifacts/`. Class A is portable/CI; class B boots real VZ VMs on Apple silicon and cannot be proven by CI. Results recorded in the status.md gate table + testing.md checklist; this claim's summary line in `docs/claims/README.md` carries the ✅/❌ outcome.
