# Claim: Verify ragshit review coverage-truncation fix (claim 0176) landed at current main

- **Owner:** buffy (`freebuff/make-sure-git-main-is-current-7f307de5-d3c0-4d90-966c-3a4221ad4d24`)
- **Prompt / plan:** task prompt 2026-08-08 — make review-packet coverage claims decision-useful under hard budget pressure (a structurally large changed symbol must not be reported as fully/usefully covered on a one-line prefix). On aligning to current `main` (`2f20e5a`), the identical fix was already landed upstream as claim 0176 (commit `0d69ef9`, PR #43): anchor-aware truncation + weak/truncated coverage. Per AGENTS.md coordination rules, claimed/landed work is not duplicated: this claim VERIFIES the landing — reproduce the pre-fix failure from the pre-0176 code, confirm the post-fix behavior on current main, run the complete verification battery (full suite, doctor, fresh index, dogfood at multiple budgets, determinism byte-compare, exact size accounting, coordination gate), and capture before/after packets under `artifacts/`.
- **Scope:** verification only. `tools/ragshit/` is read-only unless a real gap is found (then a small follow-up fix lands under this claim). Coordination files: this claim, this branch's log, generated indexes. Evidence under `artifacts/` (gitignored, cited here).
- **Depends on:** current main with claims 9112 (`ragshit review`), 3320 (accounting/stale/shell hardening), and 0176 (coverage truncation) landed — the fix being verified; do not reopen those systems.
- **Status:** ✅ done 2026-08-08 — verification complete: pre-fix defect reproduced exactly (`p…` one-line `virtio_pci_init` + `changed_symbols: 40 / 40 (100%)`), post-fix packet renders signature + changed region with 13 weak symbols (27/40, never 100% while weak exist); full suite 147 passed / 1 skipped, doctor ok, dogfood at 20k/30k/40k/60k with exact size accounting (`len(md)` == `actual_size` == `selection_summary.actual_chars`) and byte-identical duplicate runs, envelope never used; new weak-coverage regression tests fail on the pre-fix code (8 failed / 2 passed). Evidence committed to the repo under `artifacts/ragshit-0176/` (force-added over the `artifacts/*` gitignore, docs-reconciliation precedent): `before-40000.{md,json}`, `after-40000.{md,json}`, `dogfood-{20000,30000,40000,60000}.md`, `verification-summary.txt` (the dogfood `.json` copies are byte-reproducible and kept out of the repo)

## Notes

Deterministic claim ID from `bash tools/status/claim-id.sh '<branch>' 'verify-ragshit-0176-landing'` = 4922.

Starting main SHA: `2f20e5aaad571440ab88e8caf6a246a2e9329aab` (origin/main after `git fetch`; local `main` fast-forwarded from `fff37a5`).

Claim 0176's own log records the exact reproduction: on main `fff37a5e`, `review . HEAD~5..HEAD --budget-chars 40000 --explain` rendered `kernel/src/virtio_console.zig`'s `virtio_pci_init` (231 lines) as one line while `changed_symbols` reported 100% covered. Pre-fix mechanism confirmed by reading `bc76c42`'s source: `_truncate_excerpt_line_aware` keeps only the START of the structural chunk, `_truncate_to_budget` shaves the largest candidate repeatedly, and coverage counted a one-line prefix as covering the whole symbol (no weak concept). Post-fix (current main): anchor-aware `_build_excerpt` (structural anchor + changed-line core/neighborhood, git `--unified=2` context), two-phase `_truncate_to_budget` (useful floor, then below-floor/last-resort drop), `weak`/`weak_reason` excluded from covered counts (`coverage_detail.<dim>.weak`, `missing_coverage`, `## Weak / truncated coverage`, additive JSON `weak` array), framing-loop plateau fix in `cli.py`.
