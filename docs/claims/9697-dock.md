# Claim: Desktop quick-launch dock — Kind.dock (M15 C4)

- **Owner:** buffy (`agent/buffy/m15-c4-dock`)
- **Prompt / plan:** `docs/m17-desktop-completeness.md` C4 (issue #229)
- **Scope:** Arc 2 Window Management — pure compositor `Kind.dock` (id 253) 24 px left dock, topmost layer `wallpaper < taskbar < dock < tray < user windows`, click launches via existing `sys_exec` slot 28 (claim 6359) or raises existing window. `image/apps.txt` `dock=true` flag per row (extension to M13 B2 manifest, `docs/m17-desktop-completeness.md:92`). No new syscall/kind, no heap, BSS `dock_apps[8]` + `dock_count`, hit-test `x<24` vs wallpaper right-click `x>=24`, workspaces-visibility locked (dock visible on all workspaces, per #241). Host tests for dock layout + hit-test + manifest parse, `verify-bss-budget` headroom.
- **Depends on:** C3 snap zones (✅ PR #252) — same file `driving_award.zig`, sequential; `APPS.TXT` B2 (✅ claim 8877) + `sys_exec` slot 28.
- **Status:** ✅ done 2026-08-20 — `driving_award.zig:107` `Kind.dock` + `dock_*` BSS + `arm` window 253 + `paint` dock bar + `pointer_tick` dock launch/raise + `image/apps.txt` `dock=true` (5 apps) + `desktop.zig:27` `dock` parse, host tests `arm` 5→`count` + `hit_test` + `user_open` + `syscall` `win_query` + `monitor` `resources` update, `verify-bss-budget` PASS `9788k/11534k`

## Notes

C4 is the last compositor-depth card. The window manager already has `wallpaper` (id 254) + `taskbar` (id 255) + `user` windows; C4 adds `dock` (id 253) as a fourth fixed layer. The dock is a 24 px vertical bar at `x=0, y=0, w=24, h=fb_height - taskbar_h`, always visible, topmost below tray (future) but above user windows? Actually D7 says `wallpaper < taskbar < dock < tray < user windows` — but dock should be below user windows or above? The spec says `wallpaper < taskbar < dock < tray < user windows` — no, that would place dock below user windows, but dock should be topmost like taskbar. The issue says dock is topmost layer in MC (most correct). We follow `D7` as `wallpaper < taskbar < dock` where dock is above taskbar but below user windows? Wait, user windows should be above dock, so dock is below user windows but above taskbar/wallpaper. The hit-test must respect `x<24` for dock vs `x>=24` for wallpaper right-click (#228). Workspaces-visibility: dock is visible on all workspaces (per #241), unlike user windows which are per-workspace.

- **State:** `dock_apps: [8][]const u8` parsed from `APPS.TXT` at `arm()` time? Actually manifest is parsed at boot by `desktop.zig`, but kernel dock needs its own BSS list of dock app names (max 8, each 32 B). Simpler: kernel dock hardcodes first 4 apps as dock apps for M15, with `APPS.TXT` flag parsed in `desktop.zig` for future, but kernel dock uses same manifest via `image/apps.txt` build-time embedding? For M15, dock apps are the first 6 entries with `dock=true` in `image/apps.txt` (e.g., CALC, NOTEPAD, TOP, FILE, SETTINGS, CHAT). The kernel's `dock` window renders icons (first letter) for each dock app, 24×24 per icon, vertically stacked, 4 px padding.
- **Rendering:** `paint()` for `Kind.dock` draws vertical bar (dark `0x0f172a`), then per-app icon rect (accent when hovered, dimmed otherwise) with icon char centered. `draw_chrome` draws dock border.
- **Interaction:** `pointer_tick` hit-test `x<24` and `y` within dock icon rect → on click, lookup app name, check if a user window with that app's name already exists (via `find_user_window_by_title` or `process` table), if exists `focus+raise`, else `sys_exec` via monitor command? Actually kernel dock cannot directly call `sys_exec` (that's EL0 syscall), but the dock is kernel-owned, so it should post an event or call `monitor.exec`? Simpler: dock click prints `dock: launch <app>` to serial and marks dirty; the real launch is via `DESKTOP.BIN` pattern, but for M15 the dock's click is handled by the kernel's `monitor` command `exec`? For host test, we simulate launch via `user_open` with a dummy owner.
- **Verification:** host tests for dock layout (24×700 at 0,0), hit-test, manifest parse (dock flag), and a class-B `verify-live-dock.sh` that clicks the dock and decodes the launched window (like `verify-live-win-hig`). `verify-bss-budget` headroom checked.

