# Claim: M28 Symmetric Multi-Processing (SMP)

- **Owner:** buffy (`agent/buffy/m28-smp`)
- **Prompt / plan:** https://github.com/drawmeanelephant/DipshitOS/issues/595
- **Scope:** Milestone 28 — SMP: secondary CPU core discovery and bringup via PSCI CPU_ON, spinlock primitives, per-core scheduler runqueues, cross-core IPIs, and class-B live hardware verification gate
- **Touches:** kernel/src/spinlock.zig, kernel/src/psci.zig, kernel/src/smp.zig, kernel/src/gic.zig, kernel/src/timer.zig, kernel/src/exceptions.zig, kernel/src/mmu.zig, kernel/src/scheduler.zig, kernel/src/main.zig, kernel/src/monitor.zig, kernel/src/shell.zig, tools/verify-unit-tests.sh, tools/verify-live-smp.sh, justfile, docs/march-m28.md, docs/status.md
- **Depends on:** —
- **Heartbeat:** 2026-08-27
- **Status:** ✅ done

## Notes

Milestone 28 enables multi-core SMP execution on Apple Silicon (Issue #595):
1. C1: PSCI secondary core bringup (`PSCI CPU_ON` HVC calling convention, per-core secondary stacks, MMU identity map, GIC/timer per-core initialization).
2. C2: Per-core scheduler state (`current_task[MAX_CORES]`, per-core idle loops, multi-core task scheduling).
3. C3: Spinlock synchronization primitives (`Spinlock`, `IrqSaveSpinlock` native AArch64 atomics and interrupts preservation).
4. C4: Cross-core IPI (GICv3 SGI `ICC_SGI1R_EL1` for reschedule and TLB shootdown).
5. C5: Class-B live hardware gate `tools/verify-live-smp.sh` testing multi-core bringup and execution on Virtualization.framework hardware (1/1 boot passed, cores=2 online=2).
