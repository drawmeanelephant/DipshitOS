# Claim: Milestone-three close-out (full gate re-run, docs reconciliation, milestone tag)

- **Owner:** buffy (`agent/buffy/m3-closeout`)
- **Prompt / plan:** `docs/march-m3.md` step 8 — "Run the complete class-A and class-B suites at the candidate tag, reconcile status/roadmap/hardware-contract, and create the milestone tag only after every gate passes." Also archives the completed milestone-three prompt/design docs (the `> ARCHIVED`-labeled files still sitting in `docs/` root) into `docs/archive/`.
- **Scope:** full class A set (`just verify-portable`: fmt, unit tests, `test-console`, build, image, inspect, swift runner build, context, coordination, coordination tooling, mmu-debt) + full class B set (`just verify-vz` at this HEAD: serial takeover, bad-handoff, marker, nvram-console, host-console, live-transcript, live-fs, live-timer, live-tasks, live-userspace, live-svc, live-uaccess, live-addrspaces, live-lifecycle, live-exec, live-sleep, live-reboot); docs consolidation (`docs/march-m3.md` row 8 → done, `docs/status.md` milestone-three row + gate reverify pointer, `docs/roadmap.md` Milestone three header, archived prompt docs + reference updates); milestone tag created only after every gate passes.
- **Depends on:** march-m3 steps 1–7, all landed on `main` (claims 8215, 3594, 6120, 5804, 6729, 6783, 3200; merged via PRs #60/#64/#66/#67).
- **Status:** ✅ done 2026-08-10 — **the full class A + class B gate set re-ran green at the milestone-three candidate HEAD `0c119d8`** (merged main after PRs #60/#64/#66/#67; class A 11/11 + class B 17/17; evidence `artifacts/gates-reverify-20260810-m3-closeout.txt` + `artifacts/classB-chunk{1,2,3,4}-m3-closeout.log`). Docs reconciled (status.md milestone row + gate-table reverify chain, roadmap Milestone-three header, march-m3 row 8 → ✅, testing.md close-out entry; completed M1.5/M3 prompt + design docs archived to `docs/archive/`; coordination gate + context builder + test fixture repointed at the active `docs/march-m3.md`). Milestone tagged **`m3-userspace`** at `0c119d8`.

## Notes

**Why this card:** milestone three's per-card work is fully landed; step 8 is the close-out lane (lane E in `docs/march-m3.md`). It re-runs the entire gate set at a candidate commit so the tag carries dated evidence (same shape as claims 0658/2233/7873 for M1.5), reconciles the living docs with the landed state, and cuts the milestone tag only when everything is green.

**Doc consolidation:** the M1.5 + M3 one-shot prompt/design docs in `docs/` root were labeled `> ARCHIVED` as their work landed (claim 8176 labeled 8 frozen docs; the M1.5 designs were already frozen) but never moved. This claim moves the completed ones into `docs/archive/` per `docs/archive/README.md` and updates the surviving references (`docs/march-m3.md`, `docs/status.md`). The runner scripted-input card (`docs/m3-runner-scripted-input-prompt.md`) is also complete — its `--script`/`--script-expect` mode landed inside claim 6684's runner work — so it is archived with a note.

**Verification:** every gate runs at the candidate HEAD on a clean tree; class A is portable/CI, class B boots real VZ VMs on Apple silicon (this host). Full transcript saved under `artifacts/`; results recorded in the status.md gate table and the claim/log indexes; the milestone tag is created only after the full set is green.
