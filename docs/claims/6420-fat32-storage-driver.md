# Claim: FAT32 storage driver — post-exit ESP reads/writes, replacing the NVRAM persistence medium

- **Owner:** buffy (`freebuff/grab-most-current-git-and-let-s-continue-on-with-o-6068d391-a734-4e7b-859d-b1e95f01f3f6`)
- **Prompt / plan:** task prompt 2026-08-09 — "Implement a real FAT32 storage driver as the next milestone-three card, closing the last deferred piece from claim 3475 (post-exit reads/writes on the ESP, replacing the NVRAM persistence medium)."
- **Scope:** milestone-three storage card. New `kernel/src/virtio_blk.zig` (modern virtio-pci block transport — the runner attaches the disk as `VZVirtioBlockDeviceConfiguration`; mirrors the proven `virtio_console` transport patterns: pre-exit capability walk + queue setup, post-MMU sector I/O through the identity map's extra Device window) and `kernel/src/fat.zig` (GPT partition discovery + FAT32 mount/list/read/write over an injected sector interface, host-testable). `esp.zig` drops the NVRAM fs backend (claim 3475's `DipshitF:*` variables) and serves `ls`/`cat`/`write` from the live FAT; mmu.zig takes multiple extra Device windows (console BAR + blk BAR); monitor/shell/tests/docs/gate (`verify-live-fs.sh`) updated. Hard gate: `write` → reboot → `ls`/`cat` persistence observed on real VZ hardware (class B, same disk image across boots). **Two hardware discoveries:** VZ presents the disk as virtio-blk **DID 0x1042** (not the spec's 0x1041), and **resets the device at ExitBootServices** — the queue is re-armed post-MMU (`blk_rearm`, common-config MMIO writes verified DRIVER_OK).
- **Depends on:** claims 1517 (post-MMU transport access), 6684 (queue patterns), 3475 (the file window being replaced), 5162/3972 (allocator — not used here, fixed BSS only).
- **Status:** ✅ done 2026-08-09 — class A green (fmt, unit tests incl. fat 11/11, transcript byte-identical, coordination, test-coordination 15/15, mmu-debt) and class B green on real VZ hardware: `verify-live-fs.sh` 1/1 pair (run A writes `hello world` to the ESP FAT volume, `write: ok (persisted 11 bytes to FAT on the ESP)`; run B — same disk image — lists `HELLO.TXT [esp]` and cats it) plus live-transcript, marker (`M2_TXOK!`), host-console, live-reboot 2/2, live-timer, live-exceptions 1/1. Evidence: `artifacts/gates-fat32-6420.txt` + `artifacts/live-fs-*`. Docs/status/roadmap/march/README/architecture/testing/gate-inventory/hardware-contract/AGENTS updated.

## Notes

Deterministic claim ID from `bash tools/status/claim-id.sh '<branch>' 'fat32-storage-driver'` = 6420.

Ground truth for the driver: `image/mkfat32.py` builds the exact image the kernel must parse (GPT: header at LBA 1, entries at LBA 2, ESP type GUID `C12A7328-F81F-11D2-BA4B-00A0C93EC93B`, ESP at LBA 2048; FAT32: bps 512, spc 1, reserved 32, 2 FATs, root cluster 2). The loader writes BOOTED.TXT/MEMMAP.TXT/LOADER.TXT via EFI Simple File System pre-exit on the same volume, so the driver's FAT writes must remain structurally valid for firmware re-reading. No allocation, no interrupts, fixed BSS (the codebase's honesty rules).
