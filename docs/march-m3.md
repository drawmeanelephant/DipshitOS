# Milestone three march — userspace (living tracker)

## Where we are

> [`docs/status.md`](status.md) is the canonical source for milestone-level
> facts, current gates, and what comes next. This file holds only
> milestone-three's per-card detail and collision-free agent split, following
> the same status/tracker boundary as the archived [`docs/march-m15.md`](archive/march-m15.md).
> Claim work first (`docs/claims/`), append to the branch log (`docs/logs/`),
> and link saved evidence before changing a row to `✅`.

The allocator, exception-vector, timer-interrupt, and first kernel-task cards
precede this tracker and are complete. The march below starts at the EL0/SVC
boundary and carries milestone three through its close-out.

Legend: ⬜ not started · 🔄 in progress · ✅ done · ⛔ blocked (note why).

| # | Card | Status | Evidence | Notes |
|---:|------|--------|----------|-------|
| 1 | **EL0/SVC kernel boundary.** Run the smallest real EL0 task, cross into EL1 through SVC, return to EL0, and remain preemptible alongside the EL1 tasks. | ✅ done | [Claim 8215](claims/8215-el0-task-svc-boundary.md) | Landed before this tracker: the strict class-B userspace gate passed 3/3 boots, with task/timer/exception live regressions and the portable suite green. It intentionally leaves process abstraction, separate address spaces, user loading, and the broader syscall ABI to later cards. |
| 2 | **Syscall ABI + dispatch table.** Freeze the numbered contract, runtime-built dispatch table, argument/result plumbing, errors, ADR, portable coverage, and a dedicated live SVC gate. | ✅ done | [Claim 3594](claims/3594-syscall-abi-dispatch.md); ADR: [0007-syscall-abi](decisions/0007-syscall-abi.md); card: [m3-syscall-abi-prompt](archive/m3-syscall-abi-prompt.md) | Landed via PR #64 on 2026-08-10: ADR 0007 freezes x8 number, x0–x5 arguments, x0 result; the runtime-built 64-slot table implements slots 0–3 (`ping`/`write`/`yield`/`exit`) and returns `ENOSYS` for reserved 4–63; `sys_write` is bounded to the kernel-known EL0 apertures and the low-4-GiB identity blanket; scheduler yield/exit hooks and deterministic `syscalls` counters; live-svc gate passed 2026-08-10, 1/1 (evidence: `artifacts/syscall-abi-3594/verification-summary.txt`). |
| 3 | **uaccess: fault-safe copy-in/copy-out.** Add bounded user-memory transfer primitives and enforce the reserved `EFAULT` (`-3`) contract without letting a bad pointer crash EL1. | ✅ done | [Claim 6120](claims/6120-uaccess-fault-safe-copy.md); ADR: [0007-syscall-abi](decisions/0007-syscall-abi.md) amendment | Landed 2026-08-10: `kernel/src/uaccess.zig` — bounded `copy_in`/`copy_out` over the EL0 text (read) + stack (read/write) apertures; EFAULT for out-of-region/overflow/unmapped/permission; a masked fault-recovery window converts a real EL1 data abort during a copy into EFAULT (volatile window state after an optimizer-reordering park was root-caused and fixed); `sys_write` migrated; `uaccess` monitor command + EL0-payload EFAULT exercise; live gate `tools/verify-live-uaccess.sh` passes 1/1 (`valid=1 fault=1 recovered=1`), `verify-live-svc` updated to `write=3`, all live regressions green (evidence: `artifacts/m3-uaccess-live.txt`, `artifacts/live-uaccess-*`). |
| 4 | **Per-task user address spaces.** Give user tasks their own TTBR0 mappings while keeping the kernel in TTBR1; enforce UXN/PXN and exclude MMIO from EL0. | ✅ done | [Claim 5804](claims/5804-per-task-address-spaces.md); ADR: [0007-syscall-abi](decisions/0007-syscall-abi.md) amendment; hardware contract: [hardware-contract.md](hardware-contract.md) | Landed 2026-08-10 with the **VZ TTBR1 fallback**: live measurement proved TTBR1 translation incompatible with this kernel's tables (4 KiB tables → first-descent walk faults; 64 KiB-aligned tables → Normal-WB data aborts), so the kernel stays identity-mapped in TTBR0 (TTBR1=0) and each task gets its own TTBR0 root carrying an EL1-only kernel overlay; the EL0 task's root clones the identity tree and overlays text+stack leaves (W^X, UXN/PXN). EL0 reaches ONLY those leaves; MMIO is excluded by EL1-only AP bits (`el0_device=0` on VZ). Scheduler switches TTBR0 per task; `addrspaces` monitor command reports the split; live gate `tools/verify-live-addrspaces.sh` passes 1/1 with all live regressions green (evidence: `artifacts/m3-addrspaces-live.txt`, `artifacts/live-addrspaces-*`). Translation/hardware assumptions recorded in `docs/hardware-contract.md`. |
| 5 | **User task lifecycle.** Add spawn/exit/reap, explicit task states, and an idle task around the existing tick-driven scheduler. | ✅ done | [Claim 6729](claims/6729-task-lifecycle.md) | Landed 2026-08-10: explicit `State` per pool slot (free→ready→running→zombie→free), bounded `spawn`, `exit_current` → zombie, scheduler-owned idle task (ring fallback + one-zombie-per-iteration reaper), `spawn` monitor command with a dedicated demo stack, `tasks` reports `state=` + pool/zombie counts, per-task report slots (the worker no longer starves the demo's reports). Load-bearing fix: the claim-9746 vector frame was extended 20→32 slots to save x19..x28+x29 — without it a context switch corrupted the resumed task's live callee-saved registers (measured on VZ: the shell's `mon` in x19 = 0x872 → sync external abort). Live gate `tools/verify-live-lifecycle.sh` passes 1/1; all live regressions green (evidence: `artifacts/live-lifecycle-*`). |
| 6 | **Load and exec a real user program from the ESP.** Read through the existing FAT32 storage path and enter the loaded program at EL0. | ✅ done | [Claim 6783](claims/6783-esp-exec-user-program.md) | Landed 2026-08-10: a separate `user/src/main.zig` EL0 program is built into a flat `USER.BIN` (elf2bin DSK1, embedded on the ESP by the image builder); the `exec [<file>]` monitor command reads it through the claim-6420 FAT path into a fixed 4 KiB BSS page, strips the 24-byte header, rebuilds the EL0 user root around the loaded page with claim 5804's `build_user_root` (post-install — the identity-mapped kernel keeps `@intFromPtr` physical), and spawns it as an EL0t task, gated on the previous user task being gone (one user program at a time). The loaded program's `sys_write` markers + two sequenced pings + `sys_exit` (status 42) + idle reap are all observed on VZ; live gate `tools/verify-live-exec.sh` passes 1/1 with all shared-seam regressions green (lifecycle/addrspaces/uaccess/svc/userspace/tasks/timer). |
| 7 | **Blocking syscalls.** Wire sleep/yield and wakeup behavior into the tick scheduler without busy-waiting or breaking shell responsiveness. | ✅ done | [Claim 3200](claims/3200-blocking-syscalls.md); ADR: [0007-syscall-abi](decisions/0007-syscall-abi.md) (slot-4 amendment) | Landed 2026-08-10: slot 4 (`sys_sleep`) blocks the calling task for N scheduler ticks; scheduler gains `blocked` state, per-task `wakeup_tick`, and timer-driven `wake_expired` on every tick; the ESP-loaded user program yields, sleeps 2 ticks (asserts return 0), and exits with status 43; worker advances prove other tasks keep progressing during the sleep; live gate `tools/verify-live-sleep.sh`. |
| 8 | **Milestone-three close-out.** Run the complete class-A and class-B suites at the candidate tag, reconcile status/roadmap/hardware-contract, and create the milestone tag only after every gate passes. | ✅ done | [Claim 0707](claims/0707-m3-closeout.md) | Closed 2026-08-10 at candidate `0c119d8` (merged main after PRs #60/#64/#66/#67): **class A 11/11 + class B 17/17 all green** (evidence `artifacts/gates-reverify-20260810-m3-closeout.txt`, `artifacts/classB-chunk{1,2,3,4}-m3-closeout.log`); status/roadmap/march-m3 reconciled; the completed M1.5/M3 prompt + design docs archived to `docs/archive/`; milestone tagged **`m3-userspace`** at the verified commit. |

## Best agent split

| Agent / lane | Owns | Sequence / collision rule |
|--------------|------|---------------------------|
| **A — Concurrent docs/tooling** | This tracker (`docs/march-m3.md`) and the ragshit index/bundle/review card (`tools/ragshit/` plus ignored `artifacts/` evidence) | May run concurrently with the kernel-boundary lane. It does not edit the kernel, `docs/status.md`, `docs/gate-inventory.md`, or live-gate scripts. The ragshit card is defined in the archived [`m3-ragshit-dogfood-prompt.md`](archive/m3-ragshit-dogfood-prompt.md). |
| **B — EL0/SVC then syscall ABI** | Steps 1–2. While active, this serialized lane owns `kernel/src/{exceptions,scheduler,monitor,main,console}.zig`, `docs/status.md`, `docs/gate-inventory.md`, and `tools/verify-live-*.sh`. | Step 1 lands first; step 2 rebases on that result and extends its seams. No other lane edits any listed file until the syscall ABI PR lands. Steps 1–2 are complete via claims 8215 and 3594; the serialization rule is now satisfied. |
| **C — Optional runner input** | `host/vm-runner/` only, following the archived [`m3-runner-scripted-input-prompt.md`](archive/m3-runner-scripted-input-prompt.md) (its `--script` fixture mode landed inside claim 6684) | File-disjoint from lanes A and B, so it may run concurrently. Defer its manual VZ run whenever the EL0/SVC or syscall live gates use the same host; file independence does not remove host-level test contention. |
| **D — Follow-on userspace** | Steps 3–7, one claimed card at a time: uaccess → per-task address spaces → user lifecycle → ESP exec → blocking syscalls. | Starts after the syscall ABI PR lands. Steps 3–7 are complete via claims 6120, 5804, 6729, 6783, and 3200. Each card based on the preceding landed card and published before the next began; this serialized their shared exception, scheduler, MMU, monitor, status, inventory, and live-gate surfaces. |
| **E — Close-out** | Step 8: complete gate rerun, documentation reconciliation, and milestone tag. | Starts only after steps 1–7 are landed and unclaimed work is clear. It owns the close-out docs and evidence surfaces until the tag is cut. **Complete 2026-08-10 (claim 0707):** full class A + class B gate set green at `0c119d8`, docs reconciled, milestone tagged **`m3-userspace`**. |

Merge through short-lived PR branches per ADR 0003. Before starting any lane,
create its deterministic claim and branch log, refresh the generated indexes,
and check the active claims again; the split above is an ownership plan, not a
substitute for the repository's one-editor-per-file rule.
