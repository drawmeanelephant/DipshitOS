# Milestone 11 — Eleven — desktop platform & GUI apps (ADR 0011) — archived detail

> **Archived from `docs/status.md` on 2026-08-21 (issue #262).**
> The canonical one-line summary now lives in `docs/status.md` Current position table.
> This file preserves the full narrative that was previously inline in `docs/status.md`
> so the live tracker can stay ~150–200 lines. See also [`docs/march-m11.md`](../march-m11.md) and the claim files cited below.

## One-line summary (now in `docs/status.md`)

| Eleven — desktop platform & GUI apps (ADR 0011) | Micro-widget toolkit, CALC/NOTEPAD/TOP/DESKTOP.BIN | ✅ done 2026-08-16 (claim 2427, cards A0–A5) |

- **Close date:** 2026-08-16
- **Claim:** 2427
- **March tracker:** [`docs/march-m11.md`](../march-m11.md)

## Full narrative as it appeared in `docs/status.md` (pre-compression)

The following is the verbatim Current position table row for M11, previously at `docs/status.md:46`.

```text
| Eleven — desktop platform & GUI apps (ADR 0011) | A recognizable graphical desktop for userland applications: normative ADR 0011 UI contract (A0), the zero-heap micro-widget toolkit `user/src/lib/ui.zig` + `font8x8.zig` (A1), and four consumer GUI apps — `CALC.BIN` calculator (A2), `NOTEPAD.BIN` editor with persistent `/data/notes.txt` load/save (A3), `TOP.BIN` graphical process monitor with click-to-kill (A4), and the `DESKTOP.BIN` launcher capstone (A5) | ✅ **done 2026-08-15 (cards A0–A5 live), shipped 2026-08-16** — ADR 0011 desktop & GUI contract (claim 0664), micro-widget toolkit `user/src/lib/ui.zig` + `user/src/lib/font8x8.zig` with 0 heap allocation / pure static BSS (claim 8155), graphical calculator `CALC.BIN` (claim 8401), graphical text editor `NOTEPAD.BIN` (claim 3234), graphical process monitor `TOP.BIN` (claim 0680), and desktop launcher `DESKTOP.BIN` (claim 2427) with class-B capstone live gate `tools/verify-live-desktop.sh` **PASS 1/1 on VZ** — CALC.BIN OK, NOTEPAD.BIN OK, TOP.BIN OK, DESKTOP.BIN OK. **Post-ship polish (all merged to main):** CALC.BIN checked arithmetic + repeat-last-op + memory keys (claim 7869, PR #155), the EL0 process-control seam — `sys_exec` (ADR 0007 slot 28, claim 6359, PR #156, DESKTOP.BIN actually launches apps) + the `sys_wait_event` block/wake x0-clobber fix it uncovered, the toolkit `ui.exec_program`/`ui.kill_process` wrappers, `sys_kill` slot 29 (claim 7604, PR #157 — TOP.BIN's Kill button is real, live gate `tools/verify-live-sys-kill.sh` PASS on VZ), and NOTEPAD's scrollable viewport (claim 1771, PR #158). See [`docs/march-m11.md`](march-m11.md). |
```

### Readable paragraph form

**Milestone:** Eleven — desktop platform & GUI apps (ADR 0011)

**What it proved / is:** A recognizable graphical desktop for userland applications: normative ADR 0011 UI contract (A0), the zero-heap micro-widget toolkit `user/src/lib/ui.zig` + `font8x8.zig` (A1), and four consumer GUI apps — `CALC.BIN` calculator (A2), `NOTEPAD.BIN` editor with persistent `/data/notes.txt` load/save (A3), `TOP.BIN` graphical process monitor with click-to-kill (A4), and the `DESKTOP.BIN` launcher capstone (A5)

**Status:** ✅ **done 2026-08-15 (cards A0–A5 live), shipped 2026-08-16** — ADR 0011 desktop & GUI contract (claim 0664), micro-widget toolkit `user/src/lib/ui.zig` + `user/src/lib/font8x8.zig` with 0 heap allocation / pure static BSS (claim 8155), graphical calculator `CALC.BIN` (claim 8401), graphical text editor `NOTEPAD.BIN` (claim 3234), graphical process monitor `TOP.BIN` (claim 0680), and desktop launcher `DESKTOP.BIN` (claim 2427) with class-B capstone live gate `tools/verify-live-desktop.sh` **PASS 1/1 on VZ** — CALC.BIN OK, NOTEPAD.BIN OK, TOP.BIN OK, DESKTOP.BIN OK. **Post-ship polish (all merged to main):** CALC.BIN checked arithmetic + repeat-last-op + memory keys (claim 7869, PR #155), the EL0 process-control seam — `sys_exec` (ADR 0007 slot 28, claim 6359, PR #156, DESKTOP.BIN actually launches apps) + the `sys_wait_event` block/wake x0-clobber fix it uncovered, the toolkit `ui.exec_program`/`ui.kill_process` wrappers, `sys_kill` slot 29 (claim 7604, PR #157 — TOP.BIN's Kill button is real, live gate `tools/verify-live-sys-kill.sh` PASS on VZ), and NOTEPAD's scrollable viewport (claim 1771, PR #158). See [`docs/march-m11.md`](march-m11.md).

### Notes on `## What comes immediately afterward`

M11 closed after the `## What comes immediately afterward` ordered list was frozen;
its cards are tracked in [`docs/march-m11.md`](../march-m11.md) and its close-out is noted
only in the Current position table row above (now archived here). No numbered entry for M11
existed in the pre-compression list (which ended at item 18 for M8).

### Gates and claims

Primary claim: **2427** (see `docs/claims/2427-*.md` if present).
Full gate inventory: [`docs/gate-inventory.md`](../gate-inventory.md)
Hardware contract: [`docs/hardware-contract.md`](../hardware-contract.md)

---
_Generated by compression of `docs/status.md` (issue #262, 2026-08-21). Do not edit the one-line summary in `docs/status.md` without updating this archive if the narrative is still relevant._