# Milestone 9 — Nine — interactive application events (ADR 0009) — archived detail

> **Archived from `docs/status.md` on 2026-08-21 (issue #262).**
> The canonical one-line summary now lives in `docs/status.md` Current position table.
> This file preserves the full narrative that was previously inline in `docs/status.md`
> so the live tracker can stay ~150–200 lines. See also [`docs/march-m9.md`](../march-m9.md) and the claim files cited below.

## One-line summary (now in `docs/status.md`)

| Nine — interactive application events (ADR 0009) | Per-process event queues, sys_poll_event/wait_event, KEYTEST.BIN | ✅ done 2026-08-15 (claim 9328, cards E0–E6) |

- **Close date:** 2026-08-15
- **Claim:** 9328
- **March tracker:** [`docs/march-m9.md`](../march-m9.md)

## Full narrative as it appeared in `docs/status.md` (pre-compression)

The following is the verbatim Current position table row for M9, previously at `docs/status.md:44`.

```text
| Nine — interactive application events (ADR 0009) | Route keyboard, pointer, and window lifecycle events directly to focused EL0 applications via per-process event queues; expose non-blocking `sys_poll_event` (slot 21) and blocking `sys_wait_event` (slot 22) syscalls; verified via `KEYTEST.BIN` interactive event loop live on VZ | ✅ **done 2026-08-15 (cards E0–E6 live)** — ADR 0009 contract (claim 7463), kernel per-process event FIFO `kernel/src/events.zig` (claim 7670), keyboard event routing (claim 7206), pointer/click routing (claim 9228), window lifecycle events `WIN_FOCUS`/`WIN_BLUR`/`WIN_CLOSE` (claim 0293), event syscall seam slots 21/22 in `kernel/src/syscall.zig` + `kernel/src/scheduler.zig` (claim 1016), and capstone interactive EL0 application `KEYTEST.BIN` (`user/src/keytest.zig`, claim 9328) with class-B live gate `tools/verify-live-events.sh` **PASS 1/1 on VZ**. |
```

### Readable paragraph form

**Milestone:** Nine — interactive application events (ADR 0009)

**What it proved / is:** Route keyboard, pointer, and window lifecycle events directly to focused EL0 applications via per-process event queues; expose non-blocking `sys_poll_event` (slot 21) and blocking `sys_wait_event` (slot 22) syscalls; verified via `KEYTEST.BIN` interactive event loop live on VZ

**Status:** ✅ **done 2026-08-15 (cards E0–E6 live)** — ADR 0009 contract (claim 7463), kernel per-process event FIFO `kernel/src/events.zig` (claim 7670), keyboard event routing (claim 7206), pointer/click routing (claim 9228), window lifecycle events `WIN_FOCUS`/`WIN_BLUR`/`WIN_CLOSE` (claim 0293), event syscall seam slots 21/22 in `kernel/src/syscall.zig` + `kernel/src/scheduler.zig` (claim 1016), and capstone interactive EL0 application `KEYTEST.BIN` (`user/src/keytest.zig`, claim 9328) with class-B live gate `tools/verify-live-events.sh` **PASS 1/1 on VZ**.

### Notes on `## What comes immediately afterward`

M9 closed after the `## What comes immediately afterward` ordered list was frozen;
its cards are tracked in [`docs/march-m9.md`](../march-m9.md) and its close-out is noted
only in the Current position table row above (now archived here). No numbered entry for M9
existed in the pre-compression list (which ended at item 18 for M8).

### Gates and claims

Primary claim: **9328** (see `docs/claims/9328-*.md` if present).
Full gate inventory: [`docs/gate-inventory.md`](../gate-inventory.md)
Hardware contract: [`docs/hardware-contract.md`](../hardware-contract.md)

---
_Generated by compression of `docs/status.md` (issue #262, 2026-08-21). Do not edit the one-line summary in `docs/status.md` without updating this archive if the narrative is still relevant._