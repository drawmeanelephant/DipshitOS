# Claim: milestone six card G2 — framebuffer text rendering

- **Owner:** buffy (`agent/buffy/m6-text`)
- **Prompt / plan:** `docs/m6-text-prompt.md`
- **Scope:** milestone six, card G2 — the on-screen words. Guest
  `kernel/src/text.zig` (a fixed BSS bitmap font + putc/puts/cursor/
  line-wrap/bounded-scrollback/clear raster, injectable canvas,
  host-tested byte-exact); boot-time wiring that paints the banner +
  `dipshit>` prompt over G1's fill and re-runs the transfer/flush; the
  `text` / `text put <string>` / `text clear` monitor commands (registry
  35→36); class A host tests; class B live gate
  `tools/verify-live-text.sh` (the captured PNG shows TEXT — the screen
  is no longer a solid fill).
- **Depends on:** milestone six card G1 (claim 6053 — the virtio-gpu
  transport + framebuffer) on merged main. No M5-close gate (the shared
  seam is the proof); the default VM must stay byte-identical.
- **Status:** ✅ **done 2026-08-12** — `agent/buffy/m6-text`

## What landed

- **`kernel/src/text.zig`** (~370 lines): the public-domain font8x8
  basic glyph table (ASCII 0x20–0x7e, 8×8 bit-packed rows) in fixed BSS
  (`kernel/src/font8x8.zig`), plus a pure raster layer — `init`/`clear`,
  `putc`/`puts`, cursor tracking, line wrap at 160 cols, a bounded
  128-line scrollback ring (drop-oldest), `present()` pushing the
  region through G1's `gpu_transfer`/`gpu_flush` unchanged. The
  renderer writes B,G,R,X per G1's observed format and is host-tested
  against an injectable mock canvas — 21 tests (golden glyphs, wrap,
  scroll, clear, bounds, cursor, composition, `present` CmdResult
  mapping).
- **Boot wiring (`kernel/src/main.zig`):** after G1's `gpu_setup`, the
  kernel paints the SAME banner + prompt the serial log carries —
  "DipshitOS - AArch64 firmware-assisted kernel monitor" /
  "DipshitOS: terminal loop, meet the monitor." / "Type 'help' before
  touching anything expensive." / "dipshit> " (fg 0x00ff00 on bg
  0x101418) — and pushes it through G1's transfer/flush (`text: boot
  banner presented`). The boot-time transport counters shift G1's
  `cmds=6` → `cmds=8` (the banner's TRANSFER+FLUSH), recorded in the
  G1 gate.
- **Monitor (`kernel/src/monitor.zig`, registry 35→36):** `text`
  (report rows/cols/cell/cursor/lines/fg/bg), `text put <string...>`
  (render + flush on demand), `text clear`; shell help + the
  byte-identical transcript fixture updated (the `text` row sits
  between `screen` and `shutdown` in registry order).
- **Class A:** fmt, the full unit suite (34 modules — text.zig's 21
  tests + a new monitor `text` command test), byte-identical transcript,
  build/image/inspect, swift build, context, coordination, mmu-debt —
  all green.
- **Class B gate `tools/verify-live-text.sh` — PASS 1/1 on VZ:** the
  scripted boot runs `text`; the gate asserts the shared-seam serial
  evidence (`gpu: pre-rearm st=00`, `text: boot banner presented`, the
  full `text` report `rows=90 cols=160 cell=8x8 cur=3,9 lines=4 fg=
  0x…ff00 bg=0x…1418`, and the banner + prompt lines on serial) AND
  decodes the captured PNG itself: the banner region samples green-family
  foreground over dark background (fg=0.255 / bg=0.745 sampled — the
  screen is no longer monochrome) with the region below all background.
  Registered in `tools/sweep-vz.sh` (the aggregate is now 36 gates) +
  `justfile`.
- **Aggregate:** the full **36-gate `verify-vz` sweep re-ran green
  36/36** (`artifacts/m6-text-vz-sweep.log`) — proof the G2 surface left
  every existing gate (incl. G1's screen gate, updated honestly to
  `cmds=8`) byte-identical. Docs reconciled: march-m6 G2 row, roadmap G2
  bullet + Graphics row, status milestone-six row + next-step, README,
  architecture, gate-inventory (36-gate aggregate + the live-text row),
  claim 6053/3194.

## Claim-time observations (recorded honestly)

- The G1 gate's exact counter assertion `cmds=6` needed the honest
  update to `cmds=8`: the G2 boot banner's `present()` adds its own
  TRANSFER+FLUSH on top of G1's 6 setup commands — still
  `errors=0 timeouts=0`, and the first G1 screen-gate re-run caught it
  (the aggregate FAILed exactly there before the update).
- Live-pixel bound (the G1 precedent): the captured PNG is color-managed
  + retina-scaled (2560×1440), so the live assertion is "text is visible
  in the expected region with the expected color family" — byte-exact
  glyph shapes live in the class A mock (golden-glyph canvas tests).
- No new hardware-contract entries: G2 renders into G1's observed device
  + format; nothing new about the pixel path was observed beyond what G1
  recorded.

## Bounds

No Road Pops / input / window manager (G3–G5); the console stays on
serial. No default-config or framebuffer/format changes, no heap or
unbounded tables, no syscall numbering (ADR 0007 frozen), no device
attaches, no unobserved `[observed]` claims.
