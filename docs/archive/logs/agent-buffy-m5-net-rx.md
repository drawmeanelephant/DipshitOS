# Log — agent/buffy/m5-net-rx

- **2026-08-11** — *buffy (agent/buffy/m5-net-rx)*: claim 6076 (milestone five, card N2 — virtio-net raw Ethernet RX, `docs/m5-net-rx-prompt.md`) → branched `net-rx` from merged main `0a3139e` (PR #86, the N2 prompt doc itself; N1's OBSERVED contract is the baseline) → claim file `docs/claims/6076-net-rx.md` + this log → 🟡 claimed, class A implementation next (runner `--net-inject`, `kernel/src/virtio_net.zig` RX path — buffer supply, used-ring drain, bounded FIFO, MAC filter — `net recv` subcommand + RX counters, host tests).
- **2026-08-11** — *buffy (agent/buffy/m5-net-rx)*: claim 6076 implementation + close-out. **Guest driver** (`kernel/src/virtio_net.zig`, +4 host tests → 23): queue-0 buffer supply from fixed BSS (one 4096-byte RX buffer, one-request-at-a-time like N1's TX), kick + POLLED used-ring drain (the used-buffer IRQ recorded unobserved, not assumed), MAC filter (own + broadcast accepted, other dropped with a counter), bounded 4-slot frame FIFO (pure BSS, drop-oldest overflow, card-3d push/drain), `net recv` subcommand printing the received frame byte-exact, `net` report gains rx counters + `rx-rings` + `rx-obs`; shell idle loop drains the FIFO; the transcript fixture's help line updated. **Runner** (`host/vm-runner/Sources/VMRunner/main.swift`): `--net-inject <file>` writes the EXISTING `--net` attachment's socket end ONCE when the guest's `net: rx-armed` serial marker appears (deterministic trigger, not a sleep; OFF by default — the default VM is byte-identical). **Claim-time experiments** (all pinned in the hardware contract): (a) RX-header question ANSWERED — the device writes a 12-byte virtio_net_hdr into RX buffers (first frame device-len 72 for 60 bytes; first 16 bytes `00…01 00 ff ff ff ff` — `num_buffers=1`); (b) the device REFUSES an RX buffer under **1530 bytes** (`rx_buf_len=1526/1528/1529` wedged the whole device — no frame written, used ring never advanced, TX stalled; 1530 works; production 4096); (c) injection direction confirmed host→guest via the attachment's OTHER socket end (a write to the runner's own read end only lands in the capture file). **Class A:** fmt, 30-module unit suite, byte-identical transcript, build/image/inspect, swift build, context, coordination ×2, mmu-debt — all green. **Class B:** `tools/verify-live-net-rx.sh` **PASS 3/3 on VZ** — phase 1 injects the 60-byte broadcast known frame → `net recv` byte-exact (frames=1, filtered=0) AND the guest re-sends it (the capture is byte-exactly the injected fixture — raw Ethernet frames back and forth); phase 2 own-MAC 46-byte frame received byte-exact; phase 3 foreign-MAC frame dropped (`net recv: no frames`, `filtered=1`, rx-obs still records the delivery). The **29-gate `verify-vz` aggregate re-ran green 29/29** (`artifacts/m5-net-rx-vz-sweep.log`) — the `--net-inject` mode left the default VM byte-identical. Docs reconciliation landed (march-m5 N1 row completed + N2 row flipped, roadmap, status.md, gate-inventory, hardware-contract RX `[observed]`, architecture, README, justfile + sweep GATES). → ✅ done, PR #87.
