# Log — `freebuff/grab-newest-git-and-check-status-if-there-s-any-im-b9d5c028-9379-48e0-8ddb-ebfbf45ef2df`

- **2026-08-08** — *buffy (freebuff/grab-newest-git-and-check-status-if-there-s-any-im-b9d5c028-9379-48e0-8ddb-ebfbf45ef2df)*: claim 8073 → fast-forwarded onto the newest `origin/main` (`a680575` → `076ddf1`, 37 commits; branch had 0 unique commits) and re-verified the full gate suite at that HEAD → all class A gates pass (fmt, 63/63 unit tests, test-console 78/78 byte-identical transcript, build/image/inspect/context, swift runner build, verify-coordination, test-coordination 15/15, verify-mmu-debt) and the primary class B VZ serial gate passes live (`zig build run` — banner + 27-descriptor map `key=0x2c4` + `kernel terminal state` + `dipshit>` prompt in `vm-serial.log`, runner exit 0) → ✅ done. Evidence: `artifacts/gates-reverify-20260808-076ddf1.txt`, `artifacts/vm-serial.log`; gate table updated in `docs/status.md`; no kernel/tooling changes needed.

- **2026-08-08** — *buffy (freebuff/grab-newest-git-and-check-status-if-there-s-any-im-b9d5c028-9379-48e0-8ddb-ebfbf45ef2df)*: claimed 🔄 — next roadmap card: **exception vectors (claim 9746)** — VBAR_EL1 vector table + basic synchronous/IRQ handlers, the first half of roadmap item 5 (GIC + timer is the next card). Plan: new `kernel/src/exceptions.zig` (2048-byte 16-entry table of self-contained stubs; shared dispatch reads ESR/FAR/ELR/SPSR, emits an `[EXC]` report, resumes only a deliberate test fault via `eret`, else parks); install in `kernel_main`; new `dipshit>` command `fault` (registry 15→16) triggering `udf` with a resume-armed flag so the shell resumes — live evidence on VZ; host tests for the pure pieces; class B gate `tools/verify-live-exceptions.sh`; transcript fixture updated for the new help line; status/roadmap updated.

- **2026-08-08** — *buffy (freebuff/grab-newest-git-and-check-status-if-there-s-any-im-b9d5c028-9379-48e0-8ddb-ebfbf45ef2df)*: claim 9746 → implemented + verified → ✅ done. `kernel/src/exceptions.zig` (16-slot EL1t/EL1h/EL0 vector region of self-contained stubs, shared `exc_dispatch` emitting the `[EXC]` report, resume-armed test-fault path, 8 host tests), `fault` command (registry 16), install wired into `kernel_main` AFTER the identity-map switch. **Root cause found mid-verification:** installing VBAR_EL1 before ExitBootServices hangs ~7/8 VZ boots inside pre-exit firmware calls (ladder stops at M2_ENTRY/M2_CMAP!) — the kernel's vectors replace the firmware's while Boot Services are still active; moving install post-MMU fixed it (8/8 `zig build run`, then live-exceptions gate 2/2). Evidence: class A all green (fmt, 72/72 unit tests, transcript byte-identical, build/image/inspect/context/swift, coordination); class B `tools/verify-live-exceptions.sh` PASS 2/2 — a real `udf` fault is reported (`[EXC] sync from EL1t`, `ec=0x00 unknown-reason`) and resumed with the shell continuing (`fault: handled, resumed after faulting instruction` → `echo rx-exc-ok`), `artifacts/live-exceptions-*`; `zig build run` + `verify-live-transcript.sh` still pass.

- **2026-08-08** — *buffy (freebuff/grab-newest-git-and-check-status-if-there-s-any-im-b9d5c028-9379-48e0-8ddb-ebfbf45ef2df)*: claimed 🔄 — next roadmap card: **GIC + generic timer interrupts (claim 7948)** — roadmap item 5's remaining half. Program the interrupt controller (GIC, distributor + CPU interface) and the ARM generic timer (CNTP), deliver the timer tick into the claim-9746 EL1 IRQ vector, prove it live with a class B gate. Plan: new `kernel/src/gic.zig` (ACPI MADT discovery pre-exit — GICD base @+8 / GICR @+4 / GICC @+32, kind from GIC version field + presence; GICv3 system-register CPU interface + GICv2 GICC MMIO; distributor/redistributor PPI-30 config; ack/EOI for the IRQ handler) and `kernel/src/timer.zig` (GTDT discovery — EL1 phys timer GSIV @+56; CNTFRQ/CNTPCT/CNTP_CVAL/CNTP_CTL arm/re-arm; tick counter; 5-tick heartbeat). `exceptions.zig` gains an IRQ-dispatcher hook (kind==irq with a registered dispatcher → dispatch + `eret`, else the claim-9746 report+park). Kernel seam: discover pre-exit (via `pci.dump_acpi`'s APIC/GTDT cases), program post-MMU, register `gic.dispatch` (ack → timer.handle → eoi; no console in IRQ context), then unmask IRQs (`daifclr #2`). Heartbeats print from the shell idle loop (safe: main context). New `dipshit>` command `timer` (registry 16→17). Class B gate `tools/verify-live-timer.sh`. Docs: hardware-contract GIC `[inferred] → [observed]`, status/roadmap item 5 done.

- **2026-08-08** — *buffy (freebuff/grab-newest-git-and-check-status-if-there-s-any-im-b9d5c028-9379-48e0-8ddb-ebfbf45ef2df)*: claim 7948 → implemented + verified → **⛔ blocked (delivery), poll fallback shipped**. New `kernel/src/gic.zig` (ACPI MADT discovery pre-exit; GICv3 distributor + CPU-interface programming post-MMU; ack/EOI path; VZ-layout fallback GICD 0x10000000/GICR 0x10010000) and `kernel/src/timer.zig` (GTDT discovery — EL1 phys PPI 30; CNTFRQ/CNTP arm/re-arm + ticks), wired into `kernel_main` (discover pre-exit, program post-MMU, unmask IRQs), IRQ-dispatcher hook in `exceptions.zig`, `dipshit> timer` command (registry 17), poll-driven heartbeat from the shell idle loop, class B gate `tools/verify-live-timer.sh` (3/3). **The finding:** on Apple VZ the GIC accepts every config write and read-backs verify (GICD_CTLR=0x50 → v3, ICC_SRE=7, PMR/IGRPEN1, SPI-32 isen/igrp/prio, virtio ISR=0x1) but **never delivers an interrupt to the guest** — GICR @ 0x10010000 is RAZ/WI (TYPER/WAKER/IGROUPR0/ISENABLER0 all 0, no PrimeCell CIDR), the CNTP comparator provably fires (ISTATUS=1) yet HPPIR0/1 stay 1023 and zero exceptions are taken; VZ's `VZGICConfiguration` API is not exposed on this macOS SDK. Landing the honest poll-driven heartbeat (ISTATUS/CNTPCT consumed in the idle loop) keeps the timer feature live-gate-verified while the IRQ-delivery half is documented as a hard platform wall. Evidence: class A all green (fmt, 84/84 unit tests incl. 12 new, transcript byte-identical, build/image/inspect/context/swift, coordination); class B `tools/verify-live-timer.sh` PASS 3/3, `verify-live-exceptions.sh` + `verify-live-transcript.sh` regressions green; `artifacts/live-timer-*`. hardware-contract GIC `[inferred] → [observed]` (incl. the delivery wall); status/roadmap/gate-inventory updated.
