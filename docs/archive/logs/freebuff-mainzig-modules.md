# Log — freebuff/mainzig-modules

- **2026-08-07** — *buffy (freebuff/mainzig-modules)*: claim 0023 → mechanical split of `kernel/src/main.zig` into `mmio.zig`/`mmu.zig`/`evidence.zig`/`pci.zig`/`virtio_console.zig` (no behavior change; markers/values/order preserved; `docs/status.md` untouched) → claim file + this log created, indexes refreshed → 🔄 in progress.
- **2026-08-07** — *buffy (freebuff/mainzig-modules)*: claim 0023 → main.zig 2508 → 758 lines; handoff-v2 now the shared `handoff.zig` module (valid_handoff = mirror checks + `handoff.validate`); `tools/verify-mmu-debt.sh` now greps `kernel/src/mmu.zig` (comments moved with install_identity_map); dead code (virtio_init, pci_write32, pci_read8/unaligned, probe_variable_name, dump_16550_line) moved verbatim, not dropped → **KERNEL.BIN byte-identical across all four extractions** (580312 B, sha `55325752…`, same as the post-step-A binary) → every portable gate green at each step (fmt, unit tests, `zig build`/image/inspect, coordination, mmu-debt, transcript) → **VZ hardware gates pass unchanged** (verify-marker ladder ends `M2_TXST!` at every pass; verify-nvram-console reconstructs the full post-exit shell session) → all 9 diagnostic builds (`-D{nvram-console,preexit-tx,tx-diag,tx-transition-a..d,fw-mmu-capture,bad-handoff}`) compile → ✅ done (evidence: `artifacts/mainzig-baseline-*`, `mainzig-gate{1..4}.txt`, `mainzig-gate*-marker.txt`, `mainzig-gate4-nvram.txt`, `mainzig-final-marker.txt`).
