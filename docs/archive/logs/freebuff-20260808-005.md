# Log — `freebuff/pull-git-and-check-status-to-make-sure-everything--d4bf6a7f-c051-49b8-a1c4-bc479835e531`

- **2026-08-08** — *buffy (freebuff/pull-git-and-check-status-to-make-sure-everything--d4bf6a7f-c051-49b8-a1c4-bc479835e531)*: claimed the M1.5 close-out card — live reboot/shutdown observation (hard gate 6; `docs/claims/0527-m15-live-reboot-shutdown.md`). Claim 0011 shipped + unit-proved the real `ResetSystem` mechanism but the live half stayed blocked; claims 1517 (post-MMU virtio TX) + 6684 (live RX, runner `--script`/`--script-expect`) have since unblocked the live `dipshit>` shell, so a live `ResetSystem` can be driven and observed. 🔄 in progress.
- **2026-08-08** — *buffy (freebuff/pull-git-and-check-status-to-make-sure-everything--d4bf6a7f-c051-49b8-a1c4-bc479835e531)*: **live reboot/shutdown OBSERVED end to end (claim 0527) ✅** — hard gate 6 closed. New class-B gate `tools/verify-live-reboot.sh` (+ `just verify-live-reboot`, added to `verify-vz`) drives the real EFI `ResetSystem` (claim 0011, `kernel/src/machine.zig`) from a live `dipshit>` shell via the claim-6684 scripted-input runner mode and asserts the machine-level effects: `reboot` → second full takeover + fresh memory-map key in `vm-serial.log` (VM keeps running); `shutdown` → runner reports `VM ended … (state=0)` = `.stopped` (machine powered off). `BOOTS=2` → **4/4 PASS**, byte-identical serial sizes per command (5900 B / 2956 B; evidence `artifacts/live-reboot-*`). Honest finding: the claim-0011 `M2_RST!` NVRAM marker (best-effort by design — machine.zig: "the real evidence is the reset call itself") is absent from the store snapshot in every run; the write races the teardown, and the machine-level reset/power-off is the evidence. Docs: hard gate 6 flipped, blocker/ordering updated in `docs/status.md`; march steps 17/20 + roadmap M1.5-close-out + README + gate-inventory updated; claim 0527 flipped ✅; indexes regenerated. Class-A gates re-run green (fmt, unit, transcript, builds, swift, coordination, test-coordination, mmu-debt).
- **2026-08-08** — *buffy (freebuff/pull-git-and-check-status-to-make-sure-everything--d4bf6a7f-c051-49b8-a1c4-bc479835e531)*: claimed the next card — physical page allocator over the captured EFI map (`docs/claims/3972-physical-page-allocator.md`). First-fit bitmap allocator over ConventionalMemory (fixed 128 KiB BSS bitmap over the 4 GiB identity-map span), wired post-exit in `kernel_main`; `pages` shell command (stats + deterministic selftest); mock-transcript fixture regenerated; `alloc` added to the unit-test module list. 🔄 in progress.
- **2026-08-08** — *buffy (freebuff/pull-git-and-check-status-to-make-sure-everything--d4bf6a7f-c051-49b8-a1c4-bc479835e531)*: **physical page allocator done (claim 3972) ✅** — `kernel/src/alloc.zig` (18 unit tests) + post-exit `alloc.init` wiring in `kernel_main` + `pages`/`pages selftest` commands + `alloc` in the unit-test module list + mock-transcript fixture regenerated (2299 → 2922 B, byte-locked gate still byte-identical). Live VZ observation (`artifacts/live-pages-{run,serial}.log`): `pages` reports the REAL pool (`total=0xee2b` = 60971 pages, 7 conventional regions, span 0xffd7) and the selftest allocates the largest contiguous run (32768 @ `0x70000000`), proves exhaustion, and restores the pool. Honest finding surfaced live: the pool is fragmented across regions so whole-pool allocation is not contiguous — the battery allocates the largest free run instead (the mock's single-region fixture had masked this; `largest_free_run()` added + tested). All class-A gates green (fmt, 62 unit tests, byte-identical transcript, builds, swift, coordination, test-coordination, mmu-debt); class-B regressions re-run green (serial gate, live-transcript 1/1, live-reboot 2/2). KERNEL.BIN 596296 → 732600 B (the 128 KiB bitmap + command). Docs: claim 3972 flipped ✅, status.md ordering item 4 updated (allocator done; exception vectors/GIC next), roadmap/README pointers updated.
