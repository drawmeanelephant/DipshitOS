> ARCHIVED — frozen historical record; do not treat as an active plan.
> Milestone four closed 2026-08-11; canonical status
> lives in [`docs/status.md`](../status.md).

# Milestone four march — real randomness (living tracker)

## Where we are

> [`docs/status.md`](status.md) is the canonical source for milestone-level
> facts, current gates, and what comes next. This file holds only
> milestone-four's per-card detail and collision-free agent split, following
> the same status/tracker boundary as [`docs/march-m3.md`](march-m3.md)
> (closed 2026-08-10, tag `m3-userspace`). Claim work first
> (`docs/claims/`), append to the branch log (`docs/logs/`), and link saved
> evidence before changing a row to `✅`.

Milestone four is the first post-milestone-three stream: the kernel's first
real randomness source (virtio entropy + CSPRNG), which unlocks ASLR andanything that needs secure random bytes, the general (non-ESP)
filesystem on the claim-6420 storage driver, and the process abstraction
above the task pool (the program's image + address space + lifecycle + exit
status as one object — with the concurrent-processes follow-on removing the
last single-program constraint: two live user processes, each with its own
root + pages + task). The remaining later-milestone item from
[`docs/roadmap.md`](roadmap.md) (network — now archived at
[`docs/archive/roadmap-m5.md`](archive/roadmap-m5.md)) is sketched below as a
⬜ row so
the tracker shows the whole milestone-four horizon. **Milestone four is
CLOSED 2026-08-11 (claim 2839, tag `m4-processes` at `9d7e4d5`): the full
class A + class B gate set re-ran green at the candidate** — see row 5.

Legend: ⬜ not started · 🔄 in progress · ✅ done · ⛔ blocked (note why).

| # | Card | Status | Evidence | Notes |
|---:|------|--------|----------|-------|
| 1 | **Virtio entropy driver + CSPRNG (real randomness).** A modern virtio-pci entropy driver (DID 0x1044) with the claim-6420 post-MMU re-arm lesson, a freestanding ChaCha20 CSPRNG (RFC 7539, KAT-pinned), a boot-time 64-byte seed from the real device, the `random [n]` monitor command, and one real consumer of the seed (EL0 user-stack ASLR — exec'd programs AND the boot-time static payload). | ✅ done | [Claim 2665](claims/2665-m4-entropy-csprng.md), [Claim 3693](claims/3693-m4-aslr-boot-stack.md); prompt: [m4-entropy-csprng-prompt](archive/m4-entropy-csprng-prompt.md) | Landed 2026-08-10: `kernel/src/virtio_entropy.zig` (pre-exit discovery, post-MMU `entropy_rearm` — VZ resets the device at ExitBootServices, observed `pre-rearm st=00` — 256-byte capped reads with short-read retry), `kernel/src/csprng.zig` (ChaCha20, RFC 7539 §2.2.1/§2.3.2/§2.4.2 KATs in `zig test`, 64-byte seed all used), boot seed post-allocator (`entropy: seeded n=64`), `random [n]` (registry 27→28). **ASLR consumer (claim 2665 + follow-on claim 3693):** the exec path rebuilds the user root with a CSPRNG-randomized stack VA, and the boot-time static EL0 payload's root is rebuilt post-seed too (`aslr: boot user stack=0x…`, covering the full `.userbss` section so the timer-preemption witness stays mapped) — EVERY EL0 task gets per-boot stack ASLR. New class-B gate `tools/verify-live-entropy.sh` **PASS 2/2** — two boots produced DIFFERENT `random 32` hex, DIFFERENT exec stack VAs, and DIFFERENT boot-time stack VAs (evidence `artifacts/live-entropy-*`, `artifacts/m4-entropy-live.txt`); `verify-live-addrspaces.sh` updated to assert the randomized stack is in-band instead of an exact value; all shared-seam live regressions green (exec/sleep/addrspaces/svc/uaccess/userspace/lifecycle/tasks/timer/transcript/fs/reboot). |
| 2 | **General (non-ESP) filesystem.** The claim-6420 FAT32 storage driver serves the ESP's FAT volume; a general filesystem (arbitrary disk layout, directories, file API beyond the ESP window) remains future work (roadmap). | ✅ done | [Claim 3678](claims/3678-general-filesystem.md); prompt: [m4-general-fs-prompt](archive/m4-general-fs-prompt.md) | Landed 2026-08-10 in four stages: **A** `fat.mount_partition(ops, base_lba)` mounts any FAT32 volume at an arbitrary LBA (GPT→ESP discovery stays in `mount`; `Geo.esp_lba`→`vol_lba`); **B** the slot walk generalizes to arbitrary directory cluster chains with `/`-path resolution (`EFI/BOOT/BOOTAA64.EFI` reachable, `..`/`.` handled, `WriteResult.bad_path` for bad parents); **C** monitor path args (`ls [<dir>]`, `cat <file|path>` — honest 2048-byte read cap, never silent truncation) with registry help + transcript fixture updated byte-exactly; **D** the disk image carries a second FAT32 partition — `image/mkfat32.py` grew to a 128 MiB default with a 36 MiB DATA volume (Linux-FS GUID) at LBA 188383, `fat.mount_data` mounts it by GUID, new `mount <esp|data>` command (registry 28→29) switches the active volume and re-snapshots the window with an honest label, and the write reply names the real volume. New class-B gate `tools/verify-live-gfs.sh` **PASS 1/1 on VZ**: the DATA partition mounts by GUID, lists/reads, `hello.txt` written to it and still listed + printed after a fresh boot against the same disk (evidence `artifacts/live-gfs-*`); live-fs regression 1/1. |
| 3 | **Process abstraction.** A process object above the fixed task pool (address space, state, exit/reap, loader) — the milestone-three task lifecycle is the seam (claims 6729/6783). | ✅ done | [Claim 3848](claims/3848-process-abstraction.md); prompt: [m4-process-abstraction-prompt](archive/m4-process-abstraction-prompt.md) | Landed 2026-08-10: **`kernel/src/process.zig`** — a bounded BSS registry (8 descriptors, no allocation) where each Process owns the loaded image (entry VA + content length), the address space (root phys, text/stack VAs), the lifecycle state (created → running → exited), and the **exit status (snapshotted at exit — it survives the executor task's reap)**; `create` takes the first free slot, else recycles the oldest exited. **Real consumers:** `exec_file` creates + binds a process (its sticky module globals are gone; `loaded()` reads the current process; `ExecResult.process_full` for the exhausted registry), the boot-time static EL0 payload registers as a process too, `exit_current` notifies the registry (exception-context safe), and the shell idle loop prints the process exit report (`procs <name> exited status=<n>`). **Observability:** the `procs` command (registry 29→30) prints the table — `procs: id=… name=… state=… task=… stack=0x… exit=…`, exited rows showing `task=reaped` with the kept status. New class-B gate `tools/verify-live-procs.sh` **PASS 1/1 on VZ**: the exec'd USER.BIN is a process (`state=running` with its ASLR stack VA) alongside the boot payload's `state=exited task=reaped exit=7`, and `procs USER.BIN exited status=43` prints with the unchanged task lifecycle (evidence `artifacts/live-procs-*`); live-exec + tasks/userspace/lifecycle/svc regressions green. |
| 3a | **Concurrent processes (follow-on to card 3).** The process abstraction (claim 3848) made image + address space + lifecycle + exit status one object, but the exec gate (`scheduler.user_root_in_use`) still forced ONE live user program; this follow-on removes that last single-program constraint — each process owns its own TTBR0 user root + allocator-backed text/user-stack/EL1-stack pages + per-task syscall regions, so a second program loads and runs while the first is alive. | ✅ done | [Claim 0826](claims/0826-concurrent-user-address-spaces.md); prompt: [m4-concurrent-processes-prompt](archive/m4-concurrent-processes-prompt.md) | Landed 2026-08-10 in three stages. **A** `mmu.build_user_root` now RETURNS the fresh root's phys per call (the global stays as the diagnostics' latest-root view; `addrspaces` prints `tables=NN/256`), `process.zig` gains allocator-owned `AddrSpace` phys/pages + a `KernelStack` (the executor's EL1 exception stack — two EL0t tasks cannot share the static one), reap/recycle free the owned pages, the scheduler TCB gains per-task `UserRegions` + `has_free_slot()` + `register_exec_user(root, stack, kstack, …)`, `syscall.handle_svc` arms the CURRENT task's uaccess regions at SVC entry, and `exec.zig` allocates 5 pages per program (text/stack/EL1-stack) with `rebuild_user_root(…) ?RootInfo`; the exec gate is GONE (`ExecResult` drops `user_busy`, gains `out_of_memory`; failure paths free what they allocated). Host tests pin two live processes (exec twice without exit, distinct roots/stacks/tasks, third exec → `pool_full`). **B** new class-B gate `tools/verify-live-concurrent.sh` **PASS 1/1 on VZ** — `exec USER.BIN` twice back to back; the `procs` snapshot shows TWO `name=USER.BIN state=running` rows with distinct task ids + stack VAs; both programs' EL0 markers land twice with the worker's advances between their sleep/wake phases; both reach `user: awake` (the exit/reap report flags are single-slot first-wins, so those lines assert ≥1 — the gate documents the collapse); full window capture (no script-expect) because the 1 s tick makes a USER.BIN lifetime ~10 s (evidence `artifacts/live-concurrent-*`). **C** docs reconciled (this row, roadmap, status, README, gate-inventory). Full shared-seam live sweep green — exec/procs/addrspaces/tasks/userspace/svc/uaccess/lifecycle/sleep/entropy all PASS 1/1 against the relaxed gate. |
| 3b | **Long-lived process among live peers (follow-on 2 to card 3).** Claim 0826 proved TWO live processes — but only as two copies of the SAME program (USER.BIN), both of which exited after a few ticks. This follow-on proves the machinery against DISTINCT programs and a permanent occupant: a second DSK1 image COUNTER.BIN (`user/src/counter.zig`) never exits — it loops forever writing its own `counter: alive` marker (frozen sys_write + sys_yield only, NO sys_exit) — while USER.BIN is exec'd, runs, exits, is reaped (its allocator pages return), and is re-exec'd into the freed slot; with the counter + the re-exec'd program both live, a further exec hits the capacity gate (`pool_full`). | ✅ done | [Claim 4613](claims/4613-long-lived-process.md); prompt: [m4-long-lived-process-prompt](archive/m4-long-lived-process-prompt.md) | Landed 2026-08-10 in three stages. **A** `user/src/counter.zig` (naked asm, marker shape host-tested via a `pub const marker`; `build.zig`/`make-image.sh`/`mkfat32.py` embed COUNTER.BIN — the image builder self-verifies the third program in the listing); kernel change: the lifecycle reap now returns the exited process's OWNED pages to the allocator at the same reap that frees its executor slot (`process.release_pages_on_reap` — the exited descriptor stays in `procs` with its status, `task=reaped`), the missing half of claim 0826's "freed at reap/recycle" contract. Host tests pin the counter's marker shape, exec-by-name of BOTH programs, the pool-gate-with-one-spare (counter + user = 5/5, third exec → `pool_full` with the free count unchanged), and the exact +5 page recovery across exit → reap → re-exec. **B** the runner gains a SECOND scripted phase (`--script2`/`--script2-after` — the primary script is one burst, claim 6684, so the re-exec that must land after the first USER.BIN exits+is reaped can't be in it); new class-B gate `tools/verify-live-long-lived.sh` **PASS 1/1 on VZ** — phase 1 `ls | exec COUNTER.BIN | exec USER.BIN | procs | pages | echo`, phase 2 (after `tasks user-exec reaped`) `exec USER.BIN | procs | exec USER.BIN | pages | echo rx-long-lived-ok`; the serial log shows the counter `state=running task=2` across BOTH procs reads with 21 `counter: alive` markers spanning the whole log (first before the first USER exit, still landing after the last), USER#1 `state=exited task=reaped exit=43`, the re-exec landing in the freed slot (task=3 reused, a NEW stack VA), `exec: no free scheduler pool slot`, and `pages` free=0xfd54 in BOTH phases (5 returned, 5 re-allocated — leak-free recycle); no exception park, shell responsive (evidence `artifacts/live-long-lived-*`). Full shared-seam live sweep green — exec/procs/concurrent/tasks/lifecycle/addrspaces/sleep/svc/uaccess/userspace/entropy all PASS 1/1. **C** docs reconciled (this row, roadmap, status, README, gate-inventory), claim flipped. |
| 3c | **Kill — the kernel owns process lifetime (follow-on 3, card 3c).** Claim 4613 proved a process can REFUSE to exit (COUNTER.BIN loops forever) — but nothing could END it: no command could terminate a running process, so a permanent occupant was permanent even if the operator changed their mind. This card proves the OS, not the program, owns process lifetime: a `kill <pid|name>` monitor command (registry 30→31) arms the target's TCB, and the ring converts the target's NEXT scheduled quantum (or yield/sleep wake) into the EXISTING exit path with the reserved status 137 (no syscall — ADR 0007 frozen; the scheduler switching core untouched). The killed process flows through the real lifecycle — exit → zombie → idle-reap → page return → slot reuse. | ✅ done | [Claim 7786](claims/7786-kill-command.md); prompt: [m4-kill-prompt](archive/m4-kill-prompt.md) | Landed 2026-08-10 in three stages. **A** `scheduler.request_kill(id)` (`KillResult` ok/not_found/already_exited/refused — shell + idle are refused) arms a `kill_pending` flag on the TCB; `stage_current` (the selection point of every switch) converts an armed selection into `exit_current(137)` — the killed task never resumes and the report carries 137; `cmd_kill` (by `procs` id or name) with exact refusal strings; host tests pin arming by id/name, the 137 flow, the +5 page recovery, and re-exec after kill; the runner gains a THIRD scripted phase (`--script3`/`--script3-after`). **B** new class-B gate `tools/verify-live-kill.sh` **PASS 1/1 on VZ** — phase 1 `ls | exec COUNTER.BIN | procs | pages | echo`, phase 2 (after the first `counter: alive`) `kill COUNTER.BIN | echo rx-kill-killed`, phase 3 (after `tasks user-exec reaped`) `procs | pages | exec USER.BIN | procs | echo rx-kill-ok`; the serial log shows exactly ONE `counter: alive` marker (line 74) then `kill: COUNTER.BIN armed` (line 76) with NO marker after the kill line, `tasks user-exec exited status=137` / `procs COUNTER.BIN exited status=137` / `tasks user-exec reaped`, the row `name=COUNTER.BIN state=exited task=reaped stack=0x6ac40000 exit=137`, `pages` free 0xfd59 → 0xfd5e (EXACT +5 recovery), and the re-exec'd USER.BIN running on the SAME task id (2) the counter had (evidence `artifacts/live-kill-*`). Full 12-gate shared-seam live sweep green — exec/procs/concurrent/tasks/lifecycle/addrspaces/sleep/svc/uaccess/userspace/entropy/long-lived all PASS 1/1. **C** docs reconciled (this row, roadmap, status, README, gate-inventory), claim flipped. |
 | 3d | **Per-process exit reports — kill the first-wins collapse (follow-on 3, card 3d).** Claims 0826/4613 documented the debt: the exit/reap report is a single first-wins-while-undrained flag, so N exits in one idle-loop window collapse to ONE report line and the concurrent/long-lived gates must assert ≥1 instead of exact counts. This card replaces the three single-slot report flags with small bounded FIFOs (4 slots of name+status, BSS — no allocation) drained IN ORDER by the shell idle loop AND the monitor without double-printing; N exits in one window print exactly N report lines, in order; overflow drops the oldest (documented + host-tested). ADR 0007, the scheduler switching core, and the lifecycle states are untouched — reporting machinery only. | ✅ done | [Claim 1014](claims/1014-exact-exit-reports.md); prompt: [m4-exit-reports-prompt](archive/m4-exit-reports-prompt.md) | Landed 2026-08-10 in three stages. **A** the three single-slot report flags become bounded rings: `scheduler.exit_report_pending` → the `tasks <name> exited status=N` ring (name+status), `process.exit_report_pending` → the `procs <name> exited status=N` ring (per-slot name buffers — process names are owned copies, task names static slices), `scheduler.reap_report_pending` → the `tasks <name> reaped` ring; pushed from exception context (pure BSS writes), drained IN ORDER by the shell idle loop and the monitor without double-printing; drop-oldest overflow; host tests pin 2–3 exits in one window → exactly N ordered lines, no double-print, drop-oldest. **B** the two previously-≥1 gates tighten to EXACT counts: `tools/verify-live-concurrent.sh` **PASS 1/1 on VZ** — both USER.BIN exits → exactly 2 `tasks user-exec exited status=43` / 2 `procs USER.BIN exited status=43` / 2 `tasks user-exec reaped`; `tools/verify-live-long-lived.sh` **PASS 1/1** — the phase-1 USER.BIN exit + the phase-2 re-exec exit stay distinct (exactly 2 of each) and the boot payload's `tasks user-el0 exited status=7` stays its own line (the first long-lived run FAILED on a gate bug — a legacy boolean line clobbered the exact count; the serial evidence showed both exits at lines 89/119 — the kernel was correct — fixed, re-run green). Full 12-gate shared-seam live sweep green — exec/procs/concurrent/tasks/lifecycle/addrspaces/sleep/svc/uaccess/userspace/entropy/long-lived all PASS 1/1 (evidence `artifacts/live-3d-*`). **C** docs reconciled (this row, roadmap, status, README, gate-inventory), claim flipped. |
| 3e | **Exec context block — arguments to EL0 (follow-on 3, card 3e).** The tokenizer already splits `exec <file> [arg...]` (18-token cap), but `monitor.exec` ignored the extras — a program's identity was its image only, and the same binary could not distinguish itself per exec. This card packs a bounded argv block (8 args × 32 B, NUL-terminated, per-arg 31-byte truncation; >8 args refused honestly) into the process's OWN text page right after the loaded content — the text leaf is already EL0 read-only (W^X, AP=read-only), so the block is a READ-ONLY leaf with ZERO extra pages (the per-program 5-page budget and every exact-count page gate stay untouched) — and extends the ENTRY contract (NOT a syscall; ADR 0007 frozen): `_start` receives `argc` in x0 and the block VA in x1 via the claim-9746 frame slots (the seam where `build_initial_frame` zeroes the frame). The text aperture extends over the block, so uaccess copy_in reads it and copy_out faults (host-tested both directions). | ✅ done | [Claim 4636](claims/4636-exec-args.md); prompt: [m4-exec-args-prompt](archive/m4-exec-args-prompt.md) | Landed 2026-08-10 in three stages. **A** `exec_file(name, args)` packs the block into the staging buffer (copied into the process's OWN text page — no extra page), `argv_va_for` pins the 8-aligned block offset + the exact 4 KiB boundary (`no_args_room`), `register_exec_user` gains argc/argv_va written into the frame's x0/x1 slots, `cmd_exec` forwards `args[1..]` (registry `max_args` = 1 + 8), and USER.BIN prints one `user: arg=<n>` line per arg BEFORE its existing markers (no-arg execs stay byte-identical). Host tests pin pack shape, per-arg truncation, >8-arg refusal, the read-only-leaf uaccess contract both directions, and per-exec distinct argv (exec alpha + exec beta → own text page + own block). **B** new class-B gate `tools/verify-live-args.sh` **PASS 1/1 on VZ** — `exec USER.BIN alpha` + `exec USER.BIN beta` back to back: the SAME binary loads twice (`exec: loaded USER.BIN size=` x2), the procs snapshot shows two `state=running` rows with distinct task ids + stack VAs, the DISTINCT markers (`user: arg=alpha` / `user: arg=beta`) prove which invocation is which, both programs complete (status 43 — EXACT FIFO counts: 2 of each exit/reap line), a third exec is `pool_full` (5/5, no spare), and the shell stays responsive. (The first live run caught a payload bug — the prefix write length was 11 for the 10-char `user: arg=` string, so it swallowed the following newline and printed `user: arg=` + newline + arg on separate lines; fixed to #10, re-run green. A gate bug then failed on the exact-line grep when the first program's marker landed on the shell's trailing `dipshit> ` prompt line — switched to substring match, re-run green.) Full 12-gate shared-seam live sweep green — exec/procs/concurrent/tasks/lifecycle/addrspaces/sleep/svc/uaccess/userspace/entropy/long-lived + the new args gate + kill all PASS 1/1 (evidence `artifacts/live-args-*`). **C** docs reconciled (this row, roadmap, status, README, gate-inventory), claim flipped. |
| 3f | **IPC — distinct processes exchange data (follow-on 3, card 3f).** Coexistence is proven (claims 0826/4613), but two live processes cannot COMMUNICATE — the strongest remaining proof of "real processes" is end-to-end data flow between them, not just simultaneous markers. This card lands the FIRST inter-process data path: a bounded per-process kernel mailbox (4 × 64 B BSS ring per process id — no allocation) behind TWO new syscalls (the card's ONE ABI change, following the `sys_sleep` slot-4 precedent): `sys_ipc_send(target, buf, len)` = slot 5 (copy_in from the caller's region into the TARGET's ring; full → `ENOSPC` -5) and `sys_ipc_recv(buf, max)` = slot 6 (copy_out the caller's OWN ring; empty → 0; peek → copy_out → drop, so an EFAULT never loses a message). A process can only reach its own mailbox (recv) and a live target's (send) — cross-process isolation, uaccess-bounded both directions, ring reset on process create/recycle. `mbox [<pid>]` monitor command (registry 31→32) dumps per-process pending/sent/recv + the queued bytes. COUNTER.BIN gains a periodic send (`ipc: ping <d>` every 3 iterations, target pid parsed from its argv — card 3e's entry contract) and a THIRD image PEER.BIN (`user/src/peer.zig` through the parameterized build pipeline) recv-loops forever and echoes `peer: got ping <d>` — TWO never-exiting programs exchanging bytes. Pool math at 5 slots: counter + peer + shell + worker + idle = 5/5, NO spare (a third exec is `pool_full` — the 3b capacity proof reused under IPC; the 3g capstone raises the budget). | ✅ done | [Claim 5965](claims/5965-ipc-mailbox.md); prompt: [m4-ipc-prompt](archive/m4-ipc-prompt.md) | Landed 2026-08-10 in three stages. **A** `kernel/src/mailbox.zig` (pure BSS storage: send/peek/drop, per-pid sent/recv counters, init/reset; `scheduler.init` clears it, the exec + boot-payload seams reset the recycled pid's ring), the syscall layer (slots 5/6 handlers, ENOSPC -5, `implemented_count` 5→7, `syscalls` rows 0–6, error precedence pinned — full-ring ENOSPC before uaccess EFAULT, truncation both directions), `mbox` monitor command + host tests, PEER.BIN + the counter's argv-parsed sender payload (naked asm decimal parse/format on its OWN stack — a host-audit caught the payload writing the newline one byte past the uaccess stack region's exclusive top, fixed; cadence every 3rd iteration so the bounded ring never accumulates), the build pipeline (`build.zig`/`make-image.sh`/`mkfat32.py` embed PEER.BIN, self-verified in the listing). Host tests pin the ring wrap/full/empty/reset, the send/recv round-trip between two processes, isolation rejections (free/exited/out-of-range targets EINVAL), the EFAULT-keeps-the-message recv, the frame marshaling for slots 5/6, the mbox command output, and PEER.BIN + COUNTER.BIN by name with the 5/5 pool_full (all green). **B** new class-B gate `tools/verify-live-ipc.sh` **PASS 1/1 on VZ** — phase 1 `ls | exec PEER.BIN | exec COUNTER.BIN 1 | procs | echo`, phase 2 (after `peer: got ping 1`) `mbox | procs | exec USER.BIN | echo rx-ipc-ok`; the serial log shows the counter's `ipc: ping N` sends and the peer's byte-exact `peer: got ping N` echoes INTERLEAVED across the whole log (send N → echo N, every send echoed, first echo after the first send), `mbox` shows PEER.BIN `pending=0 sent=1 recv=1` (the bounded ring drained — sent − recv == pending, pending ≤ 1 invariant) and COUNTER.BIN `pending=0 sent=0 recv=0`, both processes still `state=running` at the final procs and neither ever exits, the third exec is `exec: no free scheduler pool slot` (5/5, no spare), and the shell stays responsive (evidence `artifacts/live-ipc-*`). (The first live run caught the cadence: with the worker eating most of each round the counter ran ~17 iterations per window — an every-8th send yielded only 2 sends; switched to every 3rd, re-run green with 5 sends + 5 echoes.) Full 12-gate shared-seam live sweep green — exec/procs/concurrent/tasks/lifecycle/addrspaces/sleep/svc/uaccess/userspace/entropy/long-lived + the args + kill gates all PASS 1/1. **C** docs reconciled (this row, roadmap, status, README, gate-inventory), claim flipped. |
| 3g | **Pool scale — FOUR live user programs (follow-on 3, card 3g, the capstone).** Every prior card documented the 5-slot budget (3b/3c/3f: "5/5, NO spare"; 3a/3e: one spare) — this card DELIBERATELY raises the scheduler pool `max_tasks` 5 → 7 (`idle_id` stays `max_tasks - 1`) and re-derives the gates: shell + worker + FOUR EL0t user slots + idle (the 4th user slot is the "spare" while only three are live). A BUDGET change only — ADR 0007, the switching core, the lifecycle states, and the ring mechanics untouched; the pool is a BSS array (no allocation). The page-table carve-out survey (kernel root + 4 user roots, `addrspaces: tables=NN/256`) shows the roots stay well inside the 256-page budget (observed 150/256). Every capacity assertion re-derives: the exec/scheduler host tests (`pool_full` at the new budget, exact free-counts on the refused path, `has_free_slot` first-checked), the transcript fixture (`tasks: pool=4/5` → `pool=4/7`: the boot snapshot is shell + worker + user-el0 + idle = 4 registered over 7), and the live gates (args/ipc's `pool_full` moves to the FIFTH exec at 7/7; long-lived's ending becomes the one-spare scenario — counter + two users + spare — with the page counts differing by exactly the second program's 5 pages). | ✅ done | [Claim 5795](claims/5795-pool-scale-seven.md); prompt: [m4-pool-scale-prompt](archive/m4-pool-scale-prompt.md) | Landed 2026-08-11 in three stages. **A** `kernel/src/scheduler.zig` `max_tasks` 5→7 (BSS `[max_tasks]Task`, `idle_id = max_tasks - 1`, shell + worker + 4 user slots + idle), the re-derived pool-capacity host tests (scheduler 100/100: four user tasks fill 7/7, the fifth registration fails bounded; exec 152/152: FOUR USER.BINs load concurrently with distinct roots/pages/tasks and a FIFTH exec is `pool_full` with the exact free-count unchanged — the refused path leaks nothing; the permanent-occupant + recycle test re-derives at 4 programs), `mmu.build_user_root` 3-root budget pin (kernel root + 3 user roots < half the 256-page carve-out), the transcript fixture `pool=4/5` → `pool=4/7`, and the monitor/shell expectations (all green). **B** new class-B gate `tools/verify-live-scale.sh` **PASS 1/1 on VZ** — `exec COUNTER.BIN` + `exec USER.BIN` ×3 back to back: the procs snapshot shows FOUR `state=running` user rows (one counter + three USER.BINs) with FOUR distinct task ids + stack VAs, all three USER.BINs run their EL0 flow with their markers + the counter's `counter: alive` markers interleaving with the worker's advances across the whole log, the FIFTH exec is `exec: no free scheduler pool slot` (7/7 — shell + worker + 4 users + idle), `addrspaces: tables=150/256` stays inside the carve-out (106 pages headroom), the counter is still `state=running` at the final procs, and the shell stays responsive (evidence `artifacts/live-scale-*`). (First run caught two gate bugs: the interleave assertion looked between the three USER.BIN hello markers, which all land in one scripted burst — fixed to use the counter's whole-log markers; and my tables assertion demanded ≤128/256 but the honest 4-root survey is 150/256 — relaxed to ≤200 with the observed value recorded.) The full shared-seam sweep re-derived against the 7-slot pool: 12-gate sweep (exec/procs/concurrent/tasks/lifecycle/addrspaces/sleep/svc/uaccess/userspace/entropy/long-lived) + args (now FOUR argv-distinguished USER.BINs, FIFTH exec `pool_full`) + kill + ipc (counter + peer + 2 USER.BINs fill 7/7, FIFTH exec refused) all PASS 1/1. **C** docs reconciled (this row, roadmap, status, README, gate-inventory), claim flipped. |
| 4a | **Process observability — sys_procs introspection syscall (follow-on 4, card 4a).** The process registry exists (claim 3848) and the monitor can print it — but only the EL1h monitor can; an EL0 program has no way to tell who else is alive. This card gives EL0 a READ-ONLY view of the process table: `sys_procs(buf, max)` = slot 7 (the card's ONE ABI change, ADR 0007 amendment) copies a bounded snapshot out into the caller's region through uaccess — one fixed 40-byte row per non-free descriptor (u64 pid, u64 state code, u64 exit status, name[16] NUL-padded), `max` truncating to whole rows, marshaled into a fixed BSS scratch (no allocation). PEER.BIN (reused — the pool stays 7/7, no fourth image) polls `sys_procs` once per quantum until it sees a running peer, then prints `peer: sees <pid> <name> <state>` per row and falls into its existing recv loop — a process-level view of the table REACHABLE FROM EL0, distinct from the monitor's own `procs` read. | ✅ done | [Claim 5799](claims/5799-procs-syscall.md); prompt: [m4-procs-syscall-prompt](archive/m4-procs-syscall-prompt.md) | Landed 2026-08-11 in three stages. **A** `process.snapshot` (the fixed-width row marshaling, free rows skipped, exit status surviving the reap — claim 3848's snapshot contract), the ABI amendment (slot 7 `sys_procs`, `implemented_count` 7→8, `syscalls` rows 0–7; ADR 0007 updated; every existing syscall number 0–6 frozen), the syscall handler (whole-row truncation at `max` — a partial row is never copied; `max==0` → 0 rows; EFAULT for a bad buffer — the claim-6120 contract; a fixed BSS scratch, no allocation), the EL0 payload in PEER.BIN (phase-1 sys_procs poll → `peer: sees <pid> <name> <state>` per row incl. the exited boot payload's row → the unchanged recv loop; marker shape host-pinned via `sees_prefix`), and the re-derived `syscalls` expectations (monitor test + the sleep live gate's `implemented=8`). Host tests pin the row shape/marshaling, truncation (floor to whole rows), EFAULT (bad buf + read-only target), the live-registry reflection (running/exited/created rows), and the slot-7 frame marshaling (all green). **B** new class-B gate `tools/verify-live-procs-syscall.sh` **PASS 1/1 on VZ** — `exec PEER.BIN` + `exec COUNTER.BIN 1`: the peer's `peer: sees 0 user-el0 exited` / `peer: sees 1 PEER.BIN running` / `peer: sees 2 COUNTER.BIN running` rows land in the serial log (the counter's RUNNING row read FROM EL0 — the headline), the monitor's own `procs` read shows both running with distinct tasks + stacks (distinct from the EL0 view), the IPC flow still echoes (`peer: got ping` lines — the phase-1 read doesn't disturb the recv loop), neither ever exits, shell responsive (evidence `artifacts/live-procs-syscall-*`). Full 12-gate shared-seam live sweep green — exec/procs/concurrent/tasks/lifecycle/addrspaces/sleep/svc/uaccess/userspace/entropy/long-lived all PASS 1/1, plus the args/kill/ipc/scale gates (the ipc gate's PEER.BIN assertions hold with the phase-1 read — the sees lines match no `name=`/`state=` pattern and the echo cadence is untouched). **C** docs reconciled (this row, roadmap, status, README, gate-inventory), claim flipped. |
| 4b | **IPC depth — more messages per process ring (follow-on 4, card 4b).** The card-3f mailbox is 4 × 64 B per process — only four outstanding messages fit, so a bursty flow (more than 4 sends before the peer drains) would refuse with ENOSPC. This card raises `mailbox.max_messages` 4 → 8 — a DATA-PATH CONSTANT, not a syscall number (the follow-on-4 set's ABI amendments are ONLY slots 7/8, on cards 4a/4c; ADR 0007 documents the choice). The truncation contract is unchanged: a message > 64 B still truncates at the slot bound, a full ring still refuses with the same `ENOSPC` -5 (now at the 9th send), the same empty → 0 recv, the same drain invariant `sent − recv == pending ≤ capacity`, the same cross-process isolation. COUNTER.BIN's send cadence becomes a BURST: every 6th iteration it sends 6 messages back-to-back in ONE quantum (the peer is not scheduled mid-burst), then 5 quiet iterations (the peer drains 1 per round — the ring peaks at 6 of the 8 slots and drains to 0 before the next burst: NO ENOSPC, deterministically); each send checks its return and prints a distinct `ipc: enospc` marker on failure. | ✅ done | [Claim 3179](claims/3179-ipc-depth.md); prompt: [m4-ipc-depth-prompt](archive/m4-ipc-depth-prompt.md) | Landed 2026-08-11 in three stages. **A** `kernel/src/mailbox.zig` `max_messages` 4→8 (fixed BSS, no allocation), the re-derived mailbox ring wrap/full/empty/reset tests (fill 8, the 9th send refuses, counters track 9) + the ipc syscall send test (7 more sends fill the 8 slots, the 9th is ENOSPC) + the `mbox` output shape, and COUNTER.BIN's burst payload (6 sends per burst quantum via a nested asm loop, each send checking its result and printing the `ipc: enospc` marker on failure — the marker shape host-pinned via `enospc_marker`). Host tests all green. **B** the re-derived class-B gate `tools/verify-live-ipc.sh` **PASS 1/1 on VZ** — the serial log shows the counter's 6-message bursts (`ipc: ping N` … `ipc: ping N+5` back-to-back, no echo between — the peer cannot drain mid-burst) interleaved with the peer's byte-exact echoes, ZERO `ipc: enospc` lines, the log's peak (sends − echoes) = 6 — the ring at its deepest, > 4 messages queued at once and ≤ 8 (never over the re-derived capacity), every send except the last in-flight burst window (≤ 6) echoed, `mbox` shows the peer's ring drained at the new bound (pending ≤ 8, sent − recv == pending — observed `pending=5 sent=6 recv=1` mid-drain) and the counter's empty, both processes never exit, a fifth exec is `pool_full` at 7/7, shell responsive (evidence `artifacts/live-ipc-*`). Full 12-gate shared-seam live sweep + args/kill/scale + the card-4a procs-syscall gate all PASS 1/1. **C** docs reconciled (this row, roadmap, status, README, gate-inventory, ADR 0007 data-path note), claim flipped. |
| 4c | **Exit-status propagation to a peer — sys_wait (follow-on 4, card 4c).** Processes exist, exit, and are observed (4a reads the table FROM EL0) — but a process cannot WAIT on a peer: the strongest remaining "real OS" proof after the introspection view is deeper process flow than one small ring, and the status of an exited process is only visible through the monitor. This card lands `sys_wait(target)` = slot 8 (the follow-on-4 set's explicit slots 7/8 amendment, ADR 0007 — the `sys_sleep` slot-4 and ipc slots-5/6 precedents; every existing number 0–7 frozen): the caller blocks until the target process exits and returns its status — bounded, kernel-owned, NOT POSIX wait (no zombies, no fds). A running target parks the caller through the claim-0635 sleep seam (`scheduler.wait_current`: the SVC frame stays on the caller's kernel stack; the exit path's `wake_waiters` flips it back to `ready` and patches the status into the saved frame's x0, so the syscall return lands when the ring resumes it); an already-exited target returns its stored status immediately; EINVAL for a non-process caller, a free/out-of-range target, a `created` target (it may never run — the kernel refuses a waiter that could never wake), or a self-wait (the refused deadlock). The block is event-driven — the tick clock never wakes a waiter. The third program STATUS43.BIN (a FOURTH ESP image through the same build pipeline) prints its alive marker, sleeps 6 ticks (a deterministic window), and exits 43; COUNTER.BIN exec'd with the wait target in its argv prints `ipc: waiting pid=<n>`, blocks, and prints `ipc: saw pid=<n> status=<s>` on wake — the EL0 proof of the propagation. | ✅ done | [Claim 9946](claims/9946-wait-exit.md); prompt: [m4-wait-exit-prompt](archive/m4-wait-exit-prompt.md) | Landed 2026-08-11 in three stages. **A** the scheduler's event block (Task `wait_pid`, `wait_current` — the sleep seam minus the deadline, `wake_waiters` patching the status into the waiter's saved frame, `wake_expired` never touching an event-blocked task, `exit_current` waking waiters right after the registry records the exit), `process.on_task_exit` returning the exited pid, the ABI amendment (slot 8 `sys_wait`, `implemented_count` 8→9, `syscalls` rows 0–8; ADR 0007 updated; every existing syscall number 0–7 frozen), the handler (already-exited immediate return; EINVAL for non-process/free/created/self targets), the THIRD program `user/src/status43.zig` → STATUS43.BIN (alive marker → 6-tick sleep → exiting marker → exit 43; embedded through `build.zig`/`make-image.sh`/`mkfat32.py`, self-verified in the listing) and COUNTER.BIN's wait mode (argv[1] wait target parsed alongside the existing argv[0] IPC target, the `ipc: waiting pid=` / `ipc: saw pid=` markers host-pinned). Host tests pin the wait table row, the immediate-return + EINVAL refusals, the full block → target-exit → wake → status-in-saved-frame cycle, the slot-8 frame marshaling, and the 9-row `syscalls` report (all green). **B** new class-B gate `tools/verify-live-wait.sh` **PASS 1/1 on VZ** — phase 1 `ls | exec STATUS43.BIN | exec COUNTER.BIN 0 1 | procs | echo`, phase 2 (after `ipc: waiting pid=1`) `tasks | procs | echo`: the serial log shows `status43: alive`, the counter's `ipc: waiting pid=1`, a `tasks` snapshot with TWO `state=blocked` user-exec rows (the sleeping STATUS43 + the waiting counter — the blocking proof) while `procs` still shows `id=1 name=STATUS43.BIN state=running` (the target ALIVE while the waiter is blocked), then `status43: exiting`, the counter's `ipc: saw pid=1 status=43` (the observation — the ring resumes the counter directly after the target's exit, its slot being next, so it precedes the shell's report drain), and the kernel's agreeing records `tasks user-exec exited status=43` / `procs STATUS43.BIN exited status=43` / `tasks user-exec reaped` (evidence `artifacts/live-wait-*`). Full 12-gate shared-seam live sweep green — exec/procs/concurrent/tasks/lifecycle/addrspaces/sleep/svc/uaccess/userspace/entropy/long-lived all PASS 1/1, plus the args/kill/ipc/scale/procs-syscall gates. **C** docs reconciled (this row, roadmap, status, README, gate-inventory, ADR 0007 slot-8 amendment), claim flipped. |
| 4 | **Network stack.** No networking today (the runner attaches none; ADR/AGENTS milestone rules forbid it). | ⬜ sketch | — | Not started — roadmap "Eventually" item; needs the process abstraction first (now satisfied — the follow-on 4 set landed; the milestone-five plan is `docs/archive/m5-net-tx-prompt.md`, card N1). |
| 5 | **Milestone-four close-out.** Full class A + class B gate re-run at the candidate HEAD, docs reconciliation, M4 prompt docs archived, milestone tag only when every gate passes (the claim-0707 pattern). | ✅ done | [Claim 2839](claims/2839-m4-closeout.md); prompt: [m4-followon4-prompt](archive/m4-followon4-prompt.md) | Closed 2026-08-11 at candidate `9d7e4d5` (merged main after PRs #81/#82/#83): **class A 11/11 + class B 28/28 all green** (evidence `artifacts/gates-reverify-20260811-m4-closeout.txt` + `artifacts/m4-closeout-classA-1.log` + the per-gate `vz-live-*` logs); status/roadmap/march-m4/testing reconciled; the 15 completed M4 prompt docs archived to `docs/archive/` with live references updated; milestone tagged **`m4-processes`** at the verified commit. |

## Best agent split

| Agent / lane | Owns | Sequence / collision rule |
|--------------|------|---------------------------|
| **Buffy — cards 1 + 2 + 3 + 3a + 3b** | The entropy/CSPRNG lane: `kernel/src/{virtio_entropy,csprng}.zig`, the seed call in `main.zig`, `random` in `monitor.zig`, the exec-path ASLR seams (`userspace`/`scheduler`/`exec`), `tools/verify-live-entropy.sh`, `docs/march-m4.md`, `docs/status.md`, `docs/gate-inventory.md`, `docs/hardware-contract.md`, `docs/roadmap.md`. Plus the general-filesystem lane: `kernel/src/{fat,esp}.zig` volume/path generalization, `mount`/`ls [dir]`/`cat <path>` in `monitor.zig`, `image/mkfat32.py` + `make-image.sh` second partition, `tools/verify-live-gfs.sh`. Plus the process-abstraction lane: `kernel/src/process.zig`, the exec/scheduler seams, `procs` in `monitor.zig`, `tools/verify-live-procs.sh`. Plus the concurrent-processes follow-on: `kernel/src/{mmu,process,scheduler,syscall,exec,monitor,main}.zig` per-process roots/pages/regions, `tools/verify-live-concurrent.sh`. Plus the long-lived-process follow-on 2: `user/src/counter.zig`, the second ESP image (`build.zig`/`make-image.sh`/`mkfat32.py`), the reap page-return seam (`process`/`scheduler`/`monitor`), the runner's `--script2` second phase, `tools/verify-live-long-lived.sh`. Plus the kill command follow-on 3 card 3c: `scheduler.request_kill` + the armed-selection exit path (`monitor`/`scheduler`), `tools/verify-live-kill.sh`, the runner's `--script3` third phase. Plus the exact-exit-reports follow-on 3 card 3d: the exit/reap report FIFOs (`scheduler`/`process` name+status rings, drop-oldest overflow), the exact-count assertions in `tools/verify-live-concurrent.sh` + `tools/verify-live-long-lived.sh`. Plus the exec-args follow-on 3 card 3e: the bounded argv block (`exec.zig` pack + `register_exec_user` frame x0/x1 + `cmd_exec` arg forwarding + USER.BIN arg printing), `tools/verify-live-args.sh`. Plus the IPC mailbox follow-on 3 card 3f: `kernel/src/mailbox.zig` (per-process 4×64 B BSS rings, send/peek/drop, sent/recv counters), the ADR 0007 slots 5/6 amendment (`sys_ipc_send`/`sys_ipc_recv`, ENOSPC -5, `syscalls` rows 0–6), the `mbox` monitor command, the sender payload in `user/src/counter.zig` (argv-parsed target + every-3rd-iteration send) + the third image `user/src/peer.zig` (recv-loop echo, PEER.BIN through the build pipeline), `tools/verify-live-ipc.sh`. Plus the pool-scale follow-on 3 card 3g (the capstone): `scheduler.zig` `max_tasks` 5→7, the re-derived pool-capacity host tests + transcript fixture (`pool=4/7`), the mmu 3-root budget pin, the re-derived args/ipc/long-lived live gates, `tools/verify-live-scale.sh`. | **Complete 2026-08-10** — card 1 (claims 2665 + 3693): driver + CSPRNG + seed + command + ASLR consumer, live gate 2/2. Card 2 (claim 3678): volume/path generalization + second on-disk partition, live gate 1/1. Card 3 (claim 3848): process object + registry + consumers + `procs` command, live gate 1/1. Card 3a (claim 0826): per-process address spaces + relaxed exec gate, live gate 1/1 + full shared-seam sweep green. Card 3b (claim 4613): the second image + never-exiting COUNTER.BIN + recycle-under-a-permanent-occupant (pages returned at reap, re-exec lands, `pool_full` with both live), live gate 1/1 + full shared-seam sweep green. Card 3c (claim 7786): the `kill` command — kernel-owned lifetime (armed TCB → existing exit path with status 137, exact refusals, +5 page recovery, re-exec into the freed slot, `--script3` third phase), live gate 1/1 + full 12-gate shared-seam sweep green. Card 3d (claim 1014): the exit/reap report FIFOs — no more first-wins collapse, exact-count assertions in the concurrent + long-lived gates, live gates 1/1 + full 12-gate shared-seam sweep green. Card 3e (claim 4636): exec args — the bounded 8×32 B argv block in the program's own read-only text leaf (zero extra pages), the x0/x1 entry-contract extension (ADR 0007 frozen), USER.BIN printing `user: arg=<n>`, live gate 1/1 + full 12-gate shared-seam sweep green. Card 3f (claim 5965): IPC — the bounded per-process kernel mailbox + the ADR 0007 slots 5/6 amendment (`sys_ipc_send`/`sys_ipc_recv`, ENOSPC -5) + the `mbox` command + the counter's argv-targeted periodic send + PEER.BIN's recv-loop echo (TWO never-exiting programs exchanging bytes, byte-exact in the serial log), live gate 1/1 + full 12-gate shared-seam sweep green. Card 3g (claim 5795, 2026-08-11): pool scale — `max_tasks` 5→7 (shell + worker + FOUR user slots + idle), FOUR live user programs on VZ (counter + 3 USER.BINs, distinct tasks + stacks), a FIFTH exec `pool_full` at 7/7, `addrspaces: tables=150/256` inside the carve-out; every capacity assertion re-derived (exec/scheduler host tests, transcript `pool=4/7`, args/ipc's refusal to the FIFTH exec, long-lived's one-spare ending with exact 5-page delta), new live gate `tools/verify-live-scale.sh` PASS 1/1 + full re-derived shared-seam sweep green. Card 4a (claim 5799, 2026-08-11): process observability — `sys_procs(buf, max)` = slot 7 (ADR 0007 amendment, implemented_count 7→8): a bounded read-only snapshot of the process table copied out through uaccess (fixed 40-byte rows, whole-row truncation at `max`, fixed BSS scratch), PEER.BIN's phase-1 poll printing `peer: sees <pid> <name> <state>` per row (incl. the exited boot payload) — the process table read FROM EL0, distinct from the monitor's `procs`; new live gate `tools/verify-live-procs-syscall.sh` PASS 1/1 (the peer's `peer: sees 2 COUNTER.BIN running` row, the ipc flow still echoing, both never-exiting) + full shared-seam sweep + args/kill/ipc/scale green. Card 4b (claim 3179, 2026-08-11): IPC depth — `mailbox.max_messages` 4→8 (a data-path constant, ADR 0007 note, NO ABI change), the counter's 6-per-burst / 5-quiet send cadence with an `ipc: enospc` failure marker, host tests re-derived at the new capacity (9th send refuses), the re-derived live-ipc gate PASS 1/1 (burst of 6 back-to-back sends with zero enospc, the log's peak sends−echoes = 6 — > 4 messages queued at once, never over 8 — mbox drained at the new bound) + full shared-seam sweep + args/kill/scale + procs-syscall green. Card 4c (claim 9946, 2026-08-11): exit-status propagation — `sys_wait(target)` = slot 8 (ADR 0007 slots 7/8 amendment, implemented_count 8→9): the caller blocks until the target process exits and returns its status (event-driven block through the sleep seam; `wake_waiters` patches the status into the waiter's saved frame at exit; already-exited returns immediately; EINVAL for non-process/free/created/self targets — bounded, kernel-owned, NOT POSIX wait); the THIRD program STATUS43.BIN (`user/src/status43.zig`, a fourth ESP image through the same pipeline) sleeps 6 ticks then exits 43; COUNTER.BIN's wait mode (argv[1] target) prints `ipc: waiting pid=<n>` and blocks, then `ipc: saw pid=<n> status=<s>` on wake; new live gate `tools/verify-live-wait.sh` PASS 1/1 (the two blocked user-exec tasks in the phase-2 `tasks` snapshot while STATUS43's procs row still reads `state=running` — the blocking proof — then the saw line agreeing with the kernel's `tasks user-exec exited status=43` / `procs STATUS43.BIN exited status=43` records) + full shared-seam sweep + args/kill/ipc/scale/procs-syscall green. Close-out (claim 2839, 2026-08-11): full class A 11/11 + class B 28/28 re-run at the candidate `9d7e4d5` (`artifacts/gates-reverify-20260811-m4-closeout.txt`), the M4 prompt docs archived, milestone tagged **`m4-processes`**. Docs reconciled. |

Merge through short-lived PR branches per ADR 0003. Before starting any lane,
create its deterministic claim and branch log, refresh the generated indexes,
and check the active claims again; the split above is an ownership plan, not a
substitute for the repository's one-editor-per-file rule.
