# Milestone five march — networking (living tracker)

## Where we are

> [`docs/status.md`](status.md) is the canonical source for milestone-level
> facts, current gates, and what comes next. This file holds only
> milestone-five's per-card detail and collision-free agent split, following
> the [`march-m3.md`](march-m3.md) / [`march-m4.md`](march-m4.md) pattern.
> It was created for card N1 (2026-08-11, claim 1373) per
> [`docs/m5-net-tx-prompt.md`](m5-net-tx-prompt.md); a card's row flips to
> ✅ only with real observed class-B evidence, never code-complete alone.

Milestone five is the network milestone: the last "Eventually" item in
[`docs/roadmap.md`](roadmap.md) (the virtio surface table's Network row).
Milestone four is CLOSED (claim 2839, tag `m4-processes`) — the process/IPC
foundations the network sketch depended on are landed. The rungs of the
ladder, in order: **N1 virtio-net transport + TX** → N2 raw Ethernet RX →
N3 ARP → N4 IPv4/ICMP (later cards sketched only).

Legend: ⬜ not started · 🔄 in progress · ✅ done · ⛔ blocked (note why).

| # | Card | Status | Evidence | Notes |
|---:|------|--------|----------|-------|
| N1 | **Virtio-net transport + TX.** The runner's flag-gated `--net <capture-file>` mode attaches `VZVirtioNetworkDeviceConfiguration` with a `VZFileHandleNetworkDeviceAttachment` (fixed host MAC 02:00:00:00:00:01; OFF by default — the default VM stays byte-identical). The guest's `kernel/src/virtio_net.zig` discovers the modern virtio-net device (DID 0x1041) pre-exit, negotiates features, arms queue 0 (RX) + queue 1 (TX), re-arms post-exit, and `netsend` drives TX end to end through bounded BSS staging with polled used-ring drain; `net`/`netsend` monitor commands (registry 32→34). Honest bounds: RX buffer supply + used-ring drain + MAC filtering + `net recv` are card N2. | ✅ done (merged PR #85, `53483f5`) | [Claim 1373](claims/1373-net-tx.md); prompt: [m5-net-tx-prompt](m5-net-tx-prompt.md) | Landed 2026-08-11 on `agent/buffy/m5-net-tx`: **runner** `--net <capture-file>` (socketpair attachment, reader thread appends every guest datagram to the capture file byte-exactly; `config.networkDevices = []` without the flag — the 29-gate aggregate re-ran green); **guest driver** `kernel/src/virtio_net.zig` (injectable transport ops — the fat.zig pattern — 19 unit tests): discovery finds DID 0x1041 class 0x020000 at D1 (the modern spec DID confirmed on VZ); feature negotiation walks a candidate ladder because the device REJECTS VER1-only and VER1\|MAC masks — it NEEDS `VIRTIO_NET_F_MTU` accepted (observed: `feat=0x28/0x1` = VER1\|MTU\|MAC; status readback 0x03 on the rejected attempts); the host-set MAC is read via the feature path (`mac=02:00:00:00:00:01 source=feature`, matching the raw config bytes `cfgmac=02:00:00:00:00:01`); queues 0/1 armed size 4; post-exit re-arm is idempotent and the device does NOT reset at ExitBootServices (**observed `net: pre-rearm st=0f` — differs from blk/entropy's `st=00`**); TX buffers carry a ZEROED 12-byte virtio_net_hdr prefix because the device consumes one on EVERY TX buffer (observed — the first live gate stripped exactly 12 bytes; corrected `tx_hdr_len` 0→12); `netsend` builds the known frame (broadcast dst, own MAC src, ethertype 0x0800, payload byte i = i & 0xff, honest 1500-byte truncation) in fixed BSS staging (no heap) and drains the used ring polled. **Class-B gate `tools/verify-live-net-tx.sh` PASS 2/2 on VZ**: phase 1 asserts the full `net` report (did/class/mac/feat/queues/status/rearm) and that the host capture is byte-exactly the 46-byte known frame; phase 2 asserts ring reuse + honest truncation (46 + 46 + 1514 bytes, `frames=3`). Full class A green (fmt, 19+230 unit tests, byte-identical transcript, build/image/inspect, swift build, context, coordination ×2, mmu-debt); the **29-gate `verify-vz` aggregate re-ran green** (proof the `--net` mode left the default VM byte-identical; evidence `artifacts/live-net-tx-*`, `artifacts/live-net-tx-vz-sweep.log`). |
| N2 | **Raw Ethernet RX.** Used-ring drain into a bounded frame FIFO (IRQ-context push + shell-idle drain, the card-3d pattern), MAC filtering (own MAC + broadcast), `net recv` prints the frame; the host injects a known frame and the guest echoes it byte-exact. Live gate `tools/verify-live-net-rx.sh` — "raw Ethernet frames back and forth" is the first hurdle; this is where it falls. | ✅ done 2026-08-11 (live-gated; merged PR #87, claim 6076) | [Claim 6076](claims/6076-net-rx.md); prompt: [m5-net-rx-prompt](m5-net-rx-prompt.md) | Landed on `agent/buffy/m5-net-rx` from merged main `0a3139e`: **runner** `--net-inject <file>` (host→guest direction — writes the SAME attachment's socket end ONCE on the guest's `net: rx-armed` serial trigger; OFF by default); **guest driver** RX path in `kernel/src/virtio_net.zig` (+4 host tests, 23 total): queue-0 buffer supply from fixed BSS (one 4096-byte buffer, one-request-at-a-time like N1's TX), kick + polled used-ring drain, MAC filter (own + broadcast accepted, other dropped with a counter), bounded 4-slot frame FIFO (pure BSS, drop-oldest overflow, card-3d push/drain), `net recv` subcommand printing the received frame byte-exact, `net` report gains the RX counters + `rx-rings` + `rx-obs`; the shell idle loop drains the FIFO. **Claim-time observations pinned in the hardware contract:** the device WRITES a 12-byte virtio_net_hdr into RX buffers (first frame device-len 72 for 60 bytes; first 16 bytes `00…01 00 ff ff ff ff` — `num_buffers=1`; the N1 header question answered), and REFUSES an RX buffer under 1530 bytes (`rx_buf_len=1526/1528/1529` wedged the device — no frame written, used ring never advanced, TX stalled — 1530 works; production 4096); the net device's used-buffer IRQ line remains unobserved — drain is polled (recorded, not assumed). **Class-B gate `tools/verify-live-net-rx.sh` PASS 3/3 on VZ**: phase 1 injects the 60-byte broadcast known frame → `net recv` prints it byte-exact (frames=1) AND the guest re-sends it (`netsend 46`) — the host capture is byte-exactly the injected fixture (the round trip); phase 2 own-MAC 46-byte frame received byte-exact; phase 3 foreign-MAC frame dropped (`net recv: no frames`, `filtered=1`, rx-obs still records the delivery). Full class A green (fmt, 30-module unit suite incl. 23 virtio_net tests, byte-identical transcript, build/image/inspect, swift build, context, coordination ×2, mmu-debt); the **29-gate `verify-vz` aggregate re-ran green 29/29** (evidence `artifacts/live-net-rx-*`, `artifacts/m5-net-rx-vz-sweep.log`) — the `--net-inject` mode left the default VM byte-identical. |
| N3 | **ARP.** Resolve peers (send requests, parse replies) and answer requests for our protocol address. Static IP first (`net ip <a.b.c.d>`, bounded, no config heap); DHCP is a later card. Live gate: the host ARPs for the guest IP and the reply carries the right MAC; the guest resolves the host's MAC from a crafted reply. | ✅ done 2026-08-11 (live-gated; PR #89, claim 7293) | [Claim 7293](claims/7293-arp.md); prompt: [m5-arp-prompt](m5-arp-prompt.md) | Landed on `agent/buffy/m5-arp` from the N3 prompt commit (PR #88, merged main `c9fcb68`): **guest ARP layer** `kernel/src/arp.zig` (NEW, pure logic — RFC 826, IPv4-only, 6 host tests): dotted-quad parse, build_request/build_reply (byte-exact fixtures), classify (request/reply/not-ARP/malformed), a bounded 4-slot BSS table (update-in-place on a hit, first-free, drop-oldest via a cursor, no expiry — documented), counters (requests sent / replies answered / replies learned / dropped / reply TX failures); **wiring** in `kernel/src/virtio_net.zig` (+2 mock-transport tests, 27 total): the RX drain dispatches ARP — a request whose tpa == our static IP is answered (the 42-byte reply built in `tx_staging` with a ZEROED virtio_net_hdr prefix, transmitted on the N1 one-request-at-a-time TX path), a reply is learned into the table, everything else dropped with a counter; `net_arp_request` transmits a resolve request; the mock's used-ring became per-queue (RX vs TX indices) so the reply path drains its own ring; **monitor/shell**: `net ip <a.b.c.d>` (echo line `net ip: ip=10.0.0.1` — the live gate's injection marker) and `net arp [<a.b.c.d>]` (table + counters / resolve, refused honestly without an IP) as `net` SUBCOMMANDS (registry stays 34), `net` gains the `ip=`/`arp=` report line, help/usage/transcript fixture updated; **runner** `--net-arp-respond <host-ip>` (flag-gated, OFF by default, requires `--net`): the capture thread detects an ARP request datagram and writes the synthesized reply (host MAC 02:00:00:00:00:02 @ the IP — the card-N2 fds[1] direction) — a deterministic host-side answer, request-driven, never a sleep; the `--net-inject` marker poll tightened 0.5s→20ms (claim-7293 finding: the guest executes a scripted burst in tens of ms, so a mid-script marker needed a sub-100ms poll) and the gate uses the `--script2` 0.5s-settle pattern so the observation commands never race the frame. **Class-B gate `tools/verify-live-net-arp.sh` PASS 3/3 on VZ**: phase 1 injects the 42-byte ARP request "who has 10.0.0.1, tell 10.0.0.2" at the ip-set marker — `net recv` prints it byte-exact (observed 12-byte RX header + 42-byte request, device len 54), the guest's 42-byte REPLY is byte-exact in the host capture (02:00:00:00:00:01/10.0.0.1 → 02:00:00:00:00:02/10.0.0.2), repl=1; phase 2 `net arp 10.0.0.2` — the guest's broadcast request is byte-exact in the capture AND `--net-arp-respond 10.0.0.2`'s answer lands: `net arp` shows `10.0.0.2 -> 02:00:00:00:00:02`, learn=1; phase 3 injects a request for 10.0.0.99 (not our address) — NO reply (capture empty), drop=1, repl=0, and the frame is still observable via `net recv` (the N2 seam regression). Claim-time observation pinned in the hardware contract: the device delivers/transmits the 42-byte ARP frames unpadded (below the Ethernet 60-byte minimum — no padding on either direction; device len exactly 12+42). Full class A green (fmt, 32-module unit suite incl. 27 virtio_net + 6 arp tests, byte-identical transcript, build/image/inspect, swift build, context, coordination ×2, mmu-debt); the **31-gate `verify-vz` aggregate re-ran green 31/31** (evidence `artifacts/live-net-arp-*`, `artifacts/m5-arp-vz-sweep.log`; `verify-live-concurrent` flaked once on a scheduler-timing `interleave=0` and passed on immediate retry — recorded, not hidden) — the `--net-arp-respond` mode left the default VM byte-identical. |
| N4 | **IPv4.** Minimal IPv4 TX/RX with ones-complement checksums (host-testable); ICMP echo as the proof — the guest answers echo requests and can send its own; honest bounds: no fragmentation, no reassembly (drop fragments, documented). Live gate: the file-handle host sends an ICMP echo request to the guest IP and the reply is byte-exact; with NAT attached, the guest pings an outbound address. | ✅ done 2026-08-11 (live-gated; PR #91, claim 0148) | [Claim 0148](claims/0148-ipv4.md); prompt: [m5-ipv4-prompt](m5-ipv4-prompt.md) | Landed on `agent/buffy/m5-ipv4` from the N4 prompt commit (PR #90) atop the N3 claim head (`5ed7eab`): **guest IPv4 layer** `kernel/src/ipv4.zig` (NEW, pure logic — RFC 791/792, 5 host tests): RFC 1071 one's-complement checksum (odd-length fold), ethertype/field accessors, is_ipv4 (ethertype 0x0800, version 4, IHL exactly 5 — no options), is_fragment (MF or nonzero offset — N4 does NOT reassemble, honest bound, counted), header_checksum_ok (the header folds to 0x0000 — a valid checksum, not 0xffff), ICMP echo request/reply builders (byte-exact fixtures: the request carries ping_id/ping_seq + payload 01 02 03 04; the reply swaps src/dst, ECHOES the identification + id/seq/payload, recomputes both checksums), handle_rx dispatch (answer an echo request for OUR static IP; observe an echo reply — pongs_observed + last_seq; count-and-drop fragments/bad-checksum/foreign-protocol/foreign-address; non-IPv4 untouched, NOT counted). The ONE copy of the static IP stays `arp.own_ip` (no duplicate address state). **wiring** in `kernel/src/virtio_net.zig`: the RX drain dispatches ethertype 0x0800 BESIDE the N3 ARP dispatch — `rx_ipv4` builds the reply in `tx_staging` (zeroed virtio_net_hdr prefix, N1 TX path, `replies_sent`/`reply_tx_fail` counted); `net_ping_request` transmits an echo request to a peer already in the ARP table (refused `.no_peer` — a NEW SendResult case — when the MAC is unknown: an echo needs a unicast dst, unlike ARP's broadcast; `net arp <ip>` resolves first); **monitor/shell**: `net ping <a.b.c.d>` subcommand (sends the echo request, bumps ping_seq, refuses honestly without an IP / without the peer / unready transport), `net` report gains the `icmp=req=,repl=,pong=,drop=,fail=,seq=` line (drop = the sum of the five counted causes), help/usage/transcript fixture updated, registry stays 34; **runner** `--net-icmp-respond <host-ip>` (flag-gated, OFF by default, requires `--net`): the capture thread detects an ICMP echo request datagram (ethertype 0x0800, version 4/IHL 5, NON-fragment, protocol 1, type 8, dst IP match) and writes the synthesized echo reply (type 0, id/seq/payload echoed byte-exact, both checksums recomputed — `@Sendable` RFC-1071 helper) into the card-N2 fds[1] direction — deterministic, request-driven, never a sleep. **Class-B gate `tools/verify-live-net-icmp.sh` PASS 3/3 on VZ**: phase 1 injects the 46-byte echo request 10.0.0.2→10.0.0.1 (id 0x1234, seq 0x5678, ident 0xabcd) at the ip-set marker — `net recv` prints it byte-exact (observed 12-byte RX header + 46-byte request, device len 58), the guest's 46-byte REPLY is byte-exact in the host capture (the identification + id/seq/payload echoed), repl=1; phase 2 `net arp 10.0.0.2` then `net ping 10.0.0.2` — the broadcast ARP request + the 46-byte echo request (id 1, seq 1, ident 1) are byte-exact in the capture AND `--net-icmp-respond 10.0.0.2`'s answer lands: `net` shows `pong=1` with `seq=1` (the reply echoed the guest's sequence — the ping proof); phase 3 injects an echo request for 10.0.0.99 (not our address) — NO reply (capture empty), drop=1, repl=0, and the frame is still observable via `net recv` (the N2 seam regression). One gate-engineering note: the first fixture run silently built the injected frame with ICMP type 0x00 (the byte was never set) — the guest honestly counted it as a PONG (it WAS a reply) and never answered; the fixture was fixed and re-run green. No NEW hardware-contract entry — the 46-byte frames travel unpadded on both directions, consistent with N3's sub-60-byte observation (recorded in the contract). Full class A green (fmt, 33-module unit suite incl. 5 ipv4 tests, byte-identical transcript, build/image/inspect, swift build, context, coordination ×2, mmu-debt); the **32-gate `verify-vz` aggregate re-ran green 32/32** (evidence `artifacts/live-net-icmp-*`, `artifacts/m5-ipv4-vz-sweep.log`) — the `--net-icmp-respond` mode left the default VM byte-identical. |

| N5 | **UDP.** Minimal UDP TX/RX over the N4 IPv4 seam (RFC 768): datagrams in/out with the pseudo-header checksum, a bounded listen table + bounded per-listener datagram buffers, and a real LOOPBACK path (send to our own IP delivers locally, no device round trip) — the bounded host/loopback test surface. Live gate: the file-handle host sends a UDP datagram to a guest port and `net udp recv` prints it byte-exact; the guest sends to the host and the host answers. | ✅ done 2026-08-11 (live-gated; PR #99, claim 8552) | [Claim 8552](claims/8552-udp.md); prompt: [m5-udp-prompt](m5-udp-prompt.md) | Landed on `agent/buffy/m5-udp` from merged main `e685586` (N1–N4 all merged) with the N5 prompt commit (PR #92): **guest UDP layer** `kernel/src/udp.zig` (NEW, pure logic — RFC 768, 7 host tests): the 8-byte header (src/dst port, length, checksum) with the checksum over the IPv4 PSEUDO-HEADER (src/dst IP, zero, protocol 17, UDP length — RFC 768 §2 / RFC 1071, computed ALWAYS; the claim-time fix: word 5 is 0x00 0x11 — a reversed byte order silently breaks every datagram against standard peers, caught by the gate's byte-exact fixtures), a bounded 4-slot LISTEN table (`net udp listen`/`close`, refuse full/duplicate), a bounded per-listener datagram ring (4 × 72 bytes, drop-oldest), LOOPBACK (`net udp send` to our OWN IP builds the datagram with src == dst == own_ip and delivers DIRECTLY into the local receive path — no device round trip; the bounded host/loopback test surface), build_datagram + build_frame (the full Ethernet + IPv4 + UDP frame, byte-exact fixtures), counters (received / sent / loopbacked / dropped_badsum / dropped_closed / dropped_len); **wiring**: `ipv4.zig`'s `handle_rx` gains ONE dispatch branch — protocol 17 calls `udp.handle_rx` on ALREADY-VALIDATED frames (checksum/fragment/dst checks stay in ipv4, never duplicated; TCP/other still `dropped_proto`); `virtio_net.zig` gains `net_udp_send` (the `net_ping_request` shape — `.no_peer` on an unresolved MAC, `net arp <ip>` first; own-IP sends take the loopback path; the fixed src port 7000; the deterministic payload 01 02 03 04… ≤ 64); **monitor/shell**: `net udp [listen <port>|close <port>|send <addr> <port> <len>|recv [<port>]]` subcommands (the `net` command's `max_args` 2→5 for the deepest form), `net` gains the `udp=rx=,tx=,loop=,drop=` report line, help/usage/transcript fixture updated, registry stays 34; **runner** `--net-udp-respond <host-ip>:<host-port>` (flag-gated, OFF by default, requires `--net`): the capture thread detects a UDP datagram (ethertype 0x0800, version 4/IHL 5, non-fragment, proto 17, dst IP + port match) addressed to host-ip:host-port and writes the synthesized echo datagram (FROM host-ip:host-port TO the sender's ip:src-port, the SAME payload byte-exact, both checksums recomputed — `@Sendable` pseudo-header helper) into the card-N2 fds[1] direction — deterministic, request-driven, never a sleep. **Class-B gate `tools/verify-live-net-udp.sh` PASS 4/4 on VZ**: phase 1 LOOPBACK — `net udp send 10.0.0.1 7000 4` (our OWN IP) → `net udp recv` prints the 12-byte datagram byte-exact (src 7000, dst 7000, checksum, payload), rx=1 tx=1 loop=1, and the host capture stays EMPTY (nothing hit the device — the loopback test surface, live); phase 2 host→guest — the injected 46-byte datagram 10.0.0.2:9999→10.0.0.1:7000 is delivered byte-exact (`net udp recv` + the raw frame at device len 58 via `net recv`), rx=1 drop=0; phase 3 guest→host round trip — `net arp 10.0.0.2` + `net udp send 10.0.0.2 9999 4`: the ARP request + the 46-byte datagram are byte-exact in the capture AND `--net-udp-respond 10.0.0.2:9999`'s answer (same payload) lands in the listener buffer (rx=1 tx=1); phase 4 scope — a datagram to a CLOSED port (10.0.0.1:9998) is dropped (drop=1, no delivery, no reply) while remaining observable via `net recv` (the N2 seam regression). One gate-engineering note: the first runs exposed the pseudo-header byte-order bug above (phase 1 recv-exact + phases 2/3 failed against the standard fixtures; the loopback self-verified because the guest built AND verified with the same wrong sum) — fixed, re-run green 4/4. No NEW hardware-contract entry — the 46-byte frames travel unpadded on both directions, consistent with the N3/N4 sub-60-byte observation. Full class A green (fmt, 34-module unit suite incl. 7 udp tests, byte-identical transcript, build/image/inspect, swift build, context, coordination ×2, mmu-debt); the **33-gate `verify-vz` aggregate re-ran green 33/33** (evidence `artifacts/live-net-udp-*`, `artifacts/m5-udp-vz-sweep.log`) — the `--net-udp-respond` mode left the default VM byte-identical. |

## Agent split / collision rules

- **N1** (claim 1373, `agent/buffy/m5-net-tx`): owns `host/vm-runner/Sources/VMRunner/main.swift` (the `--net` mode), `kernel/src/virtio_net.zig`, the `net`/`netsend` registry rows + monitor commands, `kernel/src/shell.zig` help, the transcript fixture, `tools/verify-live-net-tx.sh`, the justfile `verify-vz` entry, and this tracker's N1 row. No syscall numbering (ADR 0007 frozen), no heap, no scheduler/process changes.
- **N2** (claim 6076, `agent/buffy/m5-net-rx`): owns the runner's `--net-inject` flag, the RX path in `kernel/src/virtio_net.zig` (buffer supply, polled drain, MAC filter, FIFO), the `net recv` subcommand + RX counters, the transcript fixture's `net recv` line, and `tools/verify-live-net-rx.sh`.
- **N3** (claim 7293, `agent/buffy/m5-arp`): owns `kernel/src/arp.zig`, the ARP dispatch in the RX drain + `net_arp_request`, the `net ip`/`net arp` subcommands, the runner's `--net-arp-respond` flag, and `tools/verify-live-net-arp.sh`.
- **N4** (claim 0148, `agent/buffy/m5-ipv4`): owns `kernel/src/ipv4.zig`, the IPv4/ICMP dispatch in the RX drain + `net_ping_request`, the `net ping` subcommand + the `net` report's `icmp=` line, the runner's `--net-icmp-respond` flag, and `tools/verify-live-net-icmp.sh`.
- **N5** (claim 8552, `agent/buffy/m5-udp`): owns `kernel/src/udp.zig`, the ipv4 protocol-17 dispatch + `net_udp_send`, the `net udp` subcommands + the `net` report's `udp=` line, the runner's `--net-udp-respond` flag, and `tools/verify-live-net-udp.sh`.
- **N6+ (future cards)**: each later card claims on merged main per the repo workflow and owns its gate script; the N5 layer (static IP, ARP, IPv4/ICMP, UDP with the loopback path) is what N6+ builds on.
- Cross-cutting docs (`status.md`, `gate-inventory.md`, `hardware-contract.md`) are updated per card at claim close-out, never during implementation.
