# M1.5 march — Interactive Kernel Monitor (living tracker)

> **Why this file exists:** the per-step tracker and the best-agent-split
> table used to live inside `docs/status.md`. Agents marking steps landed
> edits in the same file where gates and milestone status changed, so
> parallel work collided on every merge. The march is now **one file per
> milestone** — this file holds only M1.5's per-step progress and agent
> split, while `docs/status.md` holds the milestone-level facts (position,
> gates, hard gates) and links here. Marking a step in this file never
> collides with status.md edits.
>
> Milestone scope, definition of done, and hard gates: [`docs/status.md`](status.md).
> Claim work first (`docs/claims/`), append to your branch's log
> (`docs/logs/`), and record per-step progress in your claim file — then
> flip your row here. Evidence in `artifacts/`; no evidence, no `✅`.

Legend: ⬜ not started · 🔄 in progress · ✅ done · ⛔ blocked (note why).

| # | Step | Observable result | Status | Notes / evidence |
|---:|------|-------------------|--------|------------------|
| 1 | **Freeze the target.** Name it Milestone 1.5: Interactive Kernel Monitor. Keep the milestone-two kernel exactly as merged (no new firmware work). | Scope document says exactly what counts as done and what is deferred. | ✅ | `docs/status.md` is the scope/status doc (frozen 2026-08-06); this file is the per-step tracker. |
| 2 | **Define the finish line.** Boot into a terminal, display a banner, accept commands at `dipshit>`, execute ≥ 10 useful commands. | Written acceptance checklist (in `docs/status.md`) prevents agents from wandering into scheduler astrology. | ✅ | Hard gates in `docs/status.md`; fs gate **closed 2026-08-09 (claim 3475)** — pre-exit ESP snapshot + NVRAM-persisted `write`, live-observed end to end (`verify-live-fs.sh`) — **and upgraded the same day to a real FAT32 storage driver (claim 6420)**: `write` persists to the ESP's FAT volume on the disk itself. |
| 3 | **Create a dedicated integration branch.** e.g. `m1.5-interactive-monitor`. | All monitor work has one landing zone while agents use smaller branches. | ⬜ | Not created as of 2026-08-06; streams A and C targeted `main` directly per ADR 0003 / branch protection (PRs #12/#13). Revisit if parallel kernel-wiring streams collide. |
| 4 | **Add interactive mode to the Swift runner.** Give `VZFileHandleSerialPortAttachment` a readable host handle, initially standard input. | Bytes typed in the host terminal can reach the guest serial device. | ✅ | `--console` wires a stdin pipe as `fileHandleForReading` (non-nil); `--debug-input` proves bytes are handed to the attachment (`artifacts/m15-host-console-gate.txt`). Guest receipt is **not** claimed — RX is agent B. |
| 5 | **Tee guest output.** Send output to the terminal **and** `artifacts/vm-serial.log`. | Interact live without sacrificing reproducible evidence. | ✅ | Console mode streams guest output via a pipe tee to terminal + log (no full-log reloads). Evidence path (`zig build run`) keeps file-polling, unchanged. |
| 6 | **Handle terminal state safely.** Raw/character-mode input; restore the terminal on exit and signals. | Backspace, Enter, Ctrl-C behave predictably. | ✅ | termios character mode (ICANON/ECHO off, ISIG on) restored via atexit + dispatch signal sources (^C/SIGTERM/SIGHUP) + failure path; PTY gate observed exit 130 with termios back to ICANON+ECHO. Backspace/Enter/Ctrl-C documented honestly (raw passthrough; ^C ends the host session). |
| 7 | **Add a first-class launch command.** `zig build console` and `just console`. | One command builds, images, boots, and opens DipshitOS interactively. | ✅ | `zig build console` (depends on `image`) and `just console` exist; observed booting with full diagnostics + SIGTERM restore (`artifacts/m15-host-console-cmd.txt`). `just verify-host-console` runs the gate. |
| 8 | **Confirm the serial console.** The M2 probe (`probe_serial`) already selects a MMIO candidate (PL011/16550/virtio-MMIO); verify which kind/base it drives and that TX reaches `vm-serial.log` on a real VZ run. | Log proves console kind + base and the first post-exit serial evidence (`DipshitOS kernel has seized control.`). | ✅ (claim 1517) | **Done 2026-08-08 (claim 1517):** `zig build run` passes — the exact banner, `memory-map descriptors=0x…` print, `kernel terminal state`, and the live `dipshit>` prompt land in `vm-serial.log` on real VZ hardware. History: discovery (claim 0013, 2026-08-07) found a **modern virtio-pci console** (bus 0 D5, `VID=0x1af4 DID=0x1043`, class 0x078000) via pre-exit PCI enumeration over ECAM `0x40000000`; the declared MMIO windows are Apple's efivars store + an internal debug UART. The post-MMU transport hang was root-caused by claims 6460/7896 (translation start-level mismatch: T0SZ=25/W=39 walks the L0-rooted tables at level 1; the no-TLBI crutch, ADR 0006, only rode stale firmware TLB entries) and fixed in production by claim 1517: **T0SZ=16 + `tlbi vmalle1` at the switch** (cell B proved 9/9 on this host). The NVRAM fallback channel (claim 0015) remains for nvram-console builds. The virtio TX gate (claim 0002) is closed and the **RX path is live (claim 6684)** — host keystrokes reach the shell end to end (`verify-live-transcript.sh`); the live reboot/shutdown observation is done (claim 0527, `verify-live-reboot.sh`). |
| 9 | **Build a console abstraction.** `write`, `putc`, `flush` on top of the M2 `uart` module; `readByte` is the RX gap to close (step 4 host side + a guest RX path). | Kernel code stops caring which MMIO candidate carries the bytes. | ✅ | `Console.VTable` gained a polled `readByte: ?u8` (null = no input now); `MockConsole` gained a scripted input queue (`feed`/`readByte`, overflow-flagged). Host-tested (`artifacts/m15-shell-core-tests.txt`). **Live RX is wired (claim 6684, 2026-08-08):** the kernel adapter's `readByte` polls the virtio receive queue (queue 0) when the console is virtio — host keystrokes reach the shell end to end (`verify-live-transcript.sh` passes; the post-MMU TX layer that unblocked it is claim 1517). |
| 10 | **Make input bounded and boring.** Fixed 256-byte line buffer; CR/LF, backspace, Ctrl-C, overflow rejection. | Editable command lines without an allocator. | ✅ | `kernel/src/lineedit.zig`: fixed 256-byte buffer, CR/LF submit (CR alone works), backspace (`\x08 \x08` erase, bell at start), Ctrl-C (clear + `^C`), overflow **refused** with bell (never truncated mid-word), CRLF pair = one line. All edges host-tested incl. 255/256/257 chars, backspace at start, CR without LF (`artifacts/m15-shell-core-tests.txt`). |
| 11 | **Implement tokenization.** Fixed argument count, whitespace handling, optional quoted strings. | `echo "elephant business"` works without heap allocation. | ✅ | `kernel/src/tokenizer.zig`: ≤ 17 tokens (command + 16 args, mirrors `max_args_limit`), space/tab collapse, optional double-quoted strings, `""` empty arg, quote-mid-token literal, explicit unbalanced-quote (rest of line = literal + warning) and too-many (refuse) behavior. Host-tested; proven end to end in the mock-fed transcript (`artifacts/m15-shell-core-transcript.txt`). |
| 12 | **Create a command registry.** Name, help text, function pointer per command; `help` generated from the registry. | Adding commands is mechanical. | ✅ | Comptime `registry` in `kernel/src/monitor.zig` (14 commands at step close: name/help/usage/min+max args/handler); `lookup` + `exec(argv)`; `help` derives its listing from the registry (host-tested, `artifacts/m15-commands-tests.txt`). **Now 20 commands** — `ls`/`cat`/`write` added by claim 3475. |
| 13 | **Add identity commands.** `help`, `about`, `version`, `uname`, `handoff`. | The system explains what it is, how it booted, and which ABI it received (handoff **v2** struct — x3 is no longer the ESP root). | ✅ | All five implemented + exact-output tests. `version` prints build info, **no invented release number** ("DIPSHITOS 0.1" exists only in the DoD screen sketch); `handoff` prints validated v2 fields (magic/version/base/size/system table/image handle/stack bounds/flags) + validity. |
| 14 | **Add memory inspection.** `mem` from the EFI map the kernel captured **before** `ExitBootServices`: total conventional RAM, reserved regions, descriptor count, kernel bounds. | CLI reports actual machine state. | ✅ | `mem` summarizes the captured map view (`kernel/src/memmap.zig`): descriptor count/size/version/key, usable/conventional/loader/boot/runtime/reserved/mmio bytes+pages, kernel bounds from handoff. Derived from the map — **no hardcoded "256 MiB"** (the banner omits it; DoD screen's claim stays unclaimed). |
| 15 | **Add filesystem commands.** Root-only `ls`, `cat`, `write`, `touch` on the ESP. | Decision point: the ESP root died with Boot Services (x3 carries handoff v2). Either a **pre-exit file window** (kernel reads/writes evidence files before exit and the monitor reports them) or defer fs commands to a real storage driver milestone. | ✅ **closed 2026-08-09 (claim 3475), upgraded to a real FAT driver 2026-08-09 (claim 6420)** | **Decision recorded 2026-08-06: defer fs commands to a storage-driver milestone** (no ESP access post-exit; hard gate 5 open). **Superseded 2026-08-09 by the step-15-sanctioned alternative — the pre-exit file window:** `kernel/src/esp.zig` snapshots the ESP root pre-exit via the EFI Simple File System into a bounded BSS window; `ls` lists it, `cat` prints snapshotted content, and `write` persists content as the non-volatile runtime variable `DipshitF:<name>` — live-observed on VZ 1/1 pair (claim 3475; the store-exhaustion root cause is fixed: per-flush TX markers + probe persist are now first-flush-only / `-Dprobe-var`-gated). **Claim 6420 then replaces the medium entirely with a real FAT32 storage driver:** `kernel/src/fat.zig` (GPT + FAT32 mount/list/read/write, injected sector I/O, 11 host tests) over `kernel/src/virtio_blk.zig` (modern virtio-pci transport; **VZ presents the disk as DID 0x1042** — not the spec's 0x1041 — and **resets the device at ExitBootServices**, so the queue is re-armed post-MMU with common-config MMIO writes). `write` allocates clusters, updates both FAT copies, and writes the directory entry to the disk; files persist through reboot on the ESP itself (`verify-live-fs.sh`, 1/1 pair — run B lists `HELLO.TXT [esp]` and cats the content). NVRAM variables are no longer the persistence medium. |
| 16 | **Add basic shell utilities.** `echo`, `clear`, `hex`, `repeat`. | The monitor feels like a tiny environment. | ✅ | All four implemented + tested. `clear` emits documented `ESC[2J ESC[H`; `hex` parses decimal/0x-hex with explicit invalid-input errors; `repeat` bounded (count 1..64, output ≤ 4096 B). |
| 17 | **Add machine controls.** `reboot`, `shutdown`, `halt` via the Runtime Services table captured pre-exit (`ResetSystem` survives ExitBootServices) or a documented fallback (WFE loop / VM teardown). | The session can end intentionally. | ✅ (real mechanism, unit-proven; live gate ✅ observed 2026-08-08, claim 0527) | `reboot`/`shutdown` issue **real EFI Runtime Services `ResetSystem` calls** — cold for `reboot`, shutdown for `shutdown`, status 0, null data — from `kernel/src/machine.zig` (claim 0011, `agent/buffy/m15-machine-controls`), wired through the `MachineControl` seam; the runtime-services pointer is captured pre-exit (`machine.init`) and the claim-0009 NVRAM channel persists the `M2_RST!` stage immediately before the call. Arg construction is **unit-proven** (reboot→cold, shutdown→shutdown, no-pointer→honest `not_implemented`); if a firmware returns anyway the kernel parks in WFE rather than running off. **The live half is OBSERVED end to end (claim 0527, 2026-08-08):** with the RX path live (claim 6684), `verify-live-reboot.sh` drives `reboot`/`shutdown` through a real `dipshit>` shell and observes the machine-level effects — `reboot` resets the machine (second full takeover + fresh memory-map key in `vm-serial.log`, VM keeps running), `shutdown` powers it off (runner reports VM state → stopped), 4/4 boots. The `M2_RST!` marker write is best-effort by design and was lost in the teardown race (absent from the store snapshot); the reset/power-off itself is the evidence. Hard gate 6 is **closed** (evidence `artifacts/live-reboot-*`). |
| 18 | **Install the sacred nonsense.** `elephant`, a rotating boot message, and one deeply stupid command (`beans`). | DipshitOS has an identity. 🐘 | ✅ | `elephant` (fixed art + diagnostics: trunk/ears/console/handoff/memory), `beans` (bounded, deterministic), a stateless boot-message pool + `banner()` for the shell stream. All host-tested. |
| 19 | **Automate the transcript test.** Feed `help`, `version`, `mem`, `echo test`; assert exact output in `vm-serial.log`. | `zig build test-console` proves prompt + commands without manual typing. | ✅ (mock) | `zig build test-console` / `just test-console` / CI run `tools/verify-transcript.sh`: `zig test kernel/src/shell.zig` (65 tests) then a byte-exact diff of the emitted mock transcript (`artifacts/m15-mock-transcript.txt`) against the canonical fixture `tests/transcript-console.txt` (2299 B; `-text`-locked via `.gitattributes`) — evidence `artifacts/m15-transcript-{gate,unit-tests,coord}.txt`. Gate is on bytes the shell actually emitted, not file evidence. The **live `vm-serial.log` half passes (claim 6684, 2026-08-08):** `bash tools/verify-live-transcript.sh` boots the production image, forwards scripted keystrokes into the virtio receive queue, and asserts the live transcript (banner, `dipshit> help` echo, command output, echo reply) in `vm-serial.log` — 3/3 boots, byte-identical. |
| 20 | **Close the milestone honestly.** Update README, roadmap, architecture, testing docs; regenerate the deterministic context snapshot; tag only after all gates pass. | Repo says "interactive firmware-assisted kernel monitor", not "complete OS". | ✅ **closed 2026-08-09 (tag `m1.5-interactive-monitor`)** | Docs updated (README, roadmap, architecture, status, gate-inventory); context regenerated. **All 7 M1.5 hard gates pass**: the VZ serial gate (claim 1517), the RX path / live transcript (claim 6684), the live reboot/shutdown observation (claim 0527), and — closing the last, previously-deferred one — the **filesystem gate** (claim 3475: `ls`/`cat`/`write` persist through reboot via the pre-exit ESP file window + NVRAM-persisted writes, `verify-live-fs.sh`, 1/1 pair). **Post-tag the filesystem gate was upgraded to a real FAT32 storage driver (claim 6420)** — the ESP's FAT volume mounted + written through a virtio-blk transport, files persisting on the disk itself. Tagged `m1.5-interactive-monitor` 2026-08-09. See `docs/status.md`. |

## Best agent split

| Agent | Owns | Depends on |
|-------|------|------------|
| **A — Host plumbing** | Swift runner, duplex serial attachment, terminal handling, output teeing, `zig build console`, scripted input | — |
| **B — Console & shell core** | RX path + console abstraction (`uart` + read), line editor, tokenizer, command registry, prompt loop | A proving input reaches the serial attachment |
| **C — Commands & personality** | Memory reporting (captured map), fs-command decision, reboot/shutdown, banner, `elephant`, documentation, transcript fixtures | Can build against a mock console before A's proof lands |

Merge through the integration branch — do not let three agents redecorate
`kernel/src/main.zig` with chainsaws at once. **Before starting, claim your
slot via a claim file in `docs/claims/` and append a log entry under
`docs/logs/`.**
