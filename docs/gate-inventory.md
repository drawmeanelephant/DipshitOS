# DipshitOS verification gate inventory

> Canonical classification of every verification command. **A green GitHub CI
> badge proves class A only** — it says nothing about the Apple-silicon
> Virtualization.framework hardware gates (class B), the interactive gates
> (class C), or the diagnostics (class D). Run the class-B set with
> `just verify-vz` on a real Apple silicon host. Per-gate pass/fail status
> lives in [`docs/status.md`](status.md); this file is the classification,
> not the status.

## Classes

- **A — portable / build CI.** Deterministic, no Apple silicon, no VZ VM.
  Runs in GitHub CI (`.github/workflows/ci.yml`) and as `just verify-portable`
  (`just verify` is a legacy alias). A green CI badge means exactly these
  passed and nothing else.
- **B — Apple-silicon Virtualization.framework hardware gate.** Boots a real
  VZ VM on Apple silicon. GitHub-hosted CI does **not** run these and cannot
  prove them; run `just verify-vz` on a development host. Evidence lives
  under `artifacts/` and status in `docs/status.md`.
- **C — interactive / manual hardware gate.** Requires a human at the
  keyboard. Not automatable, not in CI.
- **D — diagnostic experiment.** Answers a question (claims 0017/0018/0020/
  0021); **not an acceptance gate**. Passing a diagnostic proves nothing
  about the milestone.

## Gate table

| ID | Class | Kind | Gate? | CI? | Apple silicon + VZ? | Command |
|----|-------|------|-------|-----|----------------------|---------|
| `fmt` | A | gate | yes | yes | no | `zig fmt --check boot/src/*.zig kernel/src/*.zig build.zig` |
| `unit-tests` | A | gate | yes | yes | no | `bash tools/verify-unit-tests.sh` |
| `glyph-raster` | A | gate | yes | yes | no | `bash tools/verify-glyph-raster.sh` — **claim 9358**: the font8x8 LSB-first raster convention as a NAMED gate (issue 125 hardening). Runs the three glyph-bearing module tests explicitly (font8x8's pinned table fingerprint + asymmetry census, text.zig's + driving_award.zig's full-95-glyph round-trips reading the RAW table bits inline), the offline in-cell mirror self-test, and the MUTATION CHECK (delegated to `verify-mutations.sh`). Deterministic, no Apple silicon |
| `mutations` | A | gate | yes | yes | no | `bash tools/verify-mutations.sh` — **claim 3485**: the generalized bit-order MUTATION CHECK (issue 125 hardening). A manifest of the kernel's bit-order-sensitive seams — (1) `font8x8.row_pixel` reversal (glyph raster), (2) FAT `read_le` flipped to big-endian (the FAT on-disk format is little-endian; `write_le` untouched so the write path still emits the little-endian fixtures the tests compare against — a pure READ-side flip), (3) the virtio chain-walker testing the WRITE bit instead of the NEXT bit in `free_chain` — each is applied, the named module tests MUST FAIL, and the file is restored. Design rule (the issue-125 lesson): a mutation must not be self-consistent with the code under test — mutating a shared constant both sides read is NOT a valid check, so each entry flips the READING of a bit/byte, never a shared constant. A golden that cannot detect its mutation fails the gate. Deterministic, no Apple silicon |
| `transcript-mock` | A | gate | yes | yes | no | `zig build test-console` (→ `bash tools/verify-transcript.sh`) |
| `guest-build` | A | gate | yes | yes | no | `zig build` |
| `image-build` | A | gate | yes | yes | no | `zig build image` |
| `inspect` | A | gate | yes | yes | no | `zig build inspect` |
| `swift-runner-build` | A | gate | yes | yes | no | `swift build --package-path host/vm-runner` (build only — does not boot) |
| `swift-spike-build` | A | gate | yes | yes | no | `swift build --package-path host/vm-runner -Xswiftc -DSPIKE` on the `xcode-27` public-preview arm64 runner (macOS 27 SDK; compiles the custom-virtio spike, claim 5844; does not boot) |
| `context` | A | gate | yes | yes | no | `zig build context` |
| `coordination` | A | gate | yes | yes | no | `bash tools/verify-coordination.sh` |
| `coordination-tooling` | A | gate | yes | yes | no | `bash tools/status/test-coordination.sh` |
| `mmu-debt` | A | gate | yes | yes | no | `bash tools/verify-mmu-debt.sh` |
| `verify-portable` | A | aggregate | no | no | no | `just verify-portable` (= legacy `just verify`) — the full class-A set |
| `bad-handoff` | B | gate | yes | no | yes | `bash tools/verify-bad-handoff.sh` |
| `marker` | B | gate | yes | no | yes | `bash tools/verify-marker.sh` (mechanism: `zig build marker`) |
| `nvram-console` | B | gate | yes | no | yes | `bash tools/verify-nvram-console.sh` (mechanism: `zig build nvram-console`) |
| `host-console-pty` | B | gate | yes | no | yes | `bash tools/verify-host-console.sh` |
| `serial-takeover` | B | gate | yes | no | yes | `zig build run` — **PASS 2026-08-08** (claim 1517); in `verify-vz` |
| `live-transcript-rx` | B | gate | yes | no | yes | `bash tools/verify-live-transcript.sh` — **PASS 2026-08-08** (claim 6684): live RX, host scripted keystrokes reach the kernel end to end and the `dipshit>` transcript is asserted in `vm-serial.log` |
| `live-help` | B | gate | yes | no | yes | `bash tools/verify-live-help.sh` — **milestone-eight card U1 (claim 3275)**: the ADR 0008 help walk on real VZ — scripted `help`/`help net`/`help networking|windows|storage|graphics`/`help syscalls` keystrokes assert the grouped catalog (group headers + topic footer), the per-command detail (name - help + `usage:` line), and the topic pages (and that `help syscalls` resolves to the command, not a topic) in `vm-serial.log` |
| `live-exceptions` | B | gate | yes | no | yes | `bash tools/verify-live-exceptions.sh` — **PASS 2026-08-08** (claim 9746): VBAR_EL1 vectors installed; `dipshit> fault` triggers a real synchronous `udf` reported and resumed live (shell continues) |
| `live-timer` | B | gate | yes | no | yes | `bash tools/verify-live-timer.sh` — **PASS 2026-08-09** (claim 9187): a real CNTP PPI enters the EL1 IRQ vector; requires first-IRQ evidence plus `ticks=5 irq=5 poll=0` and a responsive shell; 3/3 boots |
| `live-tasks` | B | gate | yes | no | yes | `bash tools/verify-live-tasks.sh` — **PASS 2026-08-09** (claim 5275): tick-driven round-robin scheduler; the worker's report line (`tasks worker advances=N`, N≥1 — only possible after ≥ 2 real context switches) plus a shell that keeps answering commands proves both tasks advance across ticks |
| `live-userspace` | B | gate | yes | no | yes | `bash tools/verify-live-userspace.sh` — **PASS 2026-08-09** (claim 8215): real EL0t execution, two sequenced `svc #0` entries, return to EL0, timer preemption, and a responsive EL1h shell |
| `live-svc` | B | gate | yes | no | yes | `bash tools/verify-live-svc.sh` — **PASS 2026-08-10** (claim 3594, 1/1): real EL0 write, timer preemption, yield, and non-returning exit through the runtime syscall table; exact one-shot counters and a post-exit shell reply |
| `live-uaccess` | B | gate | yes | no | yes | `bash tools/verify-live-uaccess.sh` — **claim 6120**: EL0 passes an unmapped bad pointer to `sys_write`, observes `-3`/EFAULT, and survives to write a marker; the `uaccess` monitor command runs a validated copy (`valid=1`) and a raw copy from an unmapped address that takes a real EL1 data abort, recovered into EFAULT (`recovered=1`) with the shell still responsive |
| `live-addrspaces` | B | gate | yes | no | yes | `bash tools/verify-live-addrspaces.sh` — **claim 5804**: per-task TTBR0 with an EL1-only kernel overlay (VZ TTBR1 fallback — TTBR1=0, kernel identity-mapped in TTBR0, `t0sz=16`), the EL1h shell/worker tasks on the kernel root, the EL0 task on its OWN root whose EL0-accessible leaves are exactly text+stack (`text=0x00400000 stack=0x80000000`, `el0 >= 3`) with **zero EL0-accessible Device leaves (`el0_device=0`, MMIO excluded from EL0)**; the EL0 payload + uaccess recovery still pass on the new address space |
| `live-lifecycle` | B | gate | yes | no | yes | `bash tools/verify-live-lifecycle.sh` — **claim 6729**: the user task lifecycle — the EL0 task's `sys_exit` turns it into a zombie (`tasks user-el0 exited status=7`), the scheduler-owned **idle task** reaps it (`tasks user-el0 reaped`, pool shrinks), a runtime `spawn` command registers the demo task which enters the ring and advances (`spawn: spawn-demo id=N`, `tasks spawn-demo advances=N`), the `tasks` command reports **explicit states** (`state=` per row + `pool=/5 zombies=` header), and the shell stays responsive throughout |
| `live-exec` | B | gate | yes | no | yes | `bash tools/verify-live-exec.sh` — **claim 6783**: a real user program (`USER.BIN`, a DSK1 flat image) is loaded from the ESP through the claim-6420 FAT path by `exec USER.BIN`, the EL0 user root is rebuilt around its page (claim 5804), and the program executes at EL0 — its `sys_write` markers (`user: hello from the ESP`, `user: exec ok`) and sequenced pings prove the loaded image runs, and `sys_exit` (status 42) + the idle reap (`tasks user-exec exited status=42` / `reaped`) close the claim-6729 lifecycle; the shell stays responsive |
| `live-procs` | B | gate | yes | no | yes | `bash tools/verify-live-procs.sh` — **claim 3848** (milestone-four card 3): the PROCESS abstraction above the task pool — after `exec USER.BIN` the `procs` command shows the exec'd program as a process (`name=USER.BIN state=running stack=0x…`) with the boot payload's process still `exited` (status 7 kept past the executor's reap), and the process-level exit report `procs USER.BIN exited status=43` prints alongside the unchanged task lifecycle (`tasks user-exec exited status=43` + reap) |
| `live-args` | B | gate | yes | no | yes | `bash tools/verify-live-args.sh` — **claim 4636** (milestone-four follow-on 3, card 3e): exec arguments to EL0 — `exec USER.BIN alpha` + `exec USER.BIN beta` back to back: the SAME binary loads twice (`exec: loaded USER.BIN size=` x2), the procs snapshot shows two `name=USER.BIN state=running` rows with distinct task ids + stack VAs, and the DISTINCT markers (`user: arg=alpha` / `user: arg=beta`) prove which invocation is which (the argv block is a bounded 8×32 B read-only leaf in the program's own text page, argc/argv VA passed via the entry contract's x0/x1 — no syscall, ADR 0007 frozen); both programs complete (status 43, EXACT FIFO counts), a third exec is `pool_full` (5/5), and the shell stays responsive |
| `live-concurrent` | B | gate | yes | no | yes | `bash tools/verify-live-concurrent.sh` — **claim 0826** (milestone-four follow-on): TWO live user processes — `exec USER.BIN` runs TWICE back to back (the old one-user-at-a-time `user_busy` gate is gone), the `procs` snapshot shows two `name=USER.BIN state=running` rows with DISTINCT task ids and stack VAs (per-process roots/pages/regions), every EL0 sys_write marker lands twice with the worker's advances between the programs' sleep/wake phases (true interleaving), and both programs reach the exit syscall (`user: awake` x2 — the exit/reap reports are bounded name+status FIFOs (card 3d, claim 1014), so the lines assert EXACT counts: 2 `tasks user-exec exited status=43` / 2 `procs USER.BIN exited status=43` / 2 `tasks user-exec reaped`); the gate captures the full window (no early script-expect) because the 1 s tick makes a USER.BIN lifetime ~10 s |
| `live-long-lived` | B | gate | yes | no | yes | `bash tools/verify-live-long-lived.sh` — **claim 4613** (milestone-four follow-on 2): a LONG-LIVED process among live peers — the second image COUNTER.BIN (distinct `counter: alive` markers, sys_write + sys_yield only, NO sys_exit) runs forever while USER.BIN is exec'd, runs, exits, is reaped (its 5 allocator pages return — the `pages` free count recovers), and is re-exec'd into the freed slot; with the counter + the re-exec'd program both live a further exec reports `pool_full` (the capacity gate, leak-free); the runner's new `--script2`/`--script2-after` second phase forwards the re-exec after the first reap (the primary script is one burst — claim 6684); the counter is `state=running` at the FINAL `procs` and its markers span the whole log (first before the first USER.BIN exit, still landing after the last); the exit/reap reports are bounded FIFOs (card 3d, claim 1014), so the phase-1 exit + the phase-2 re-exec exit assert EXACT counts (2 of each line) with the boot payload's `tasks user-el0 exited status=7` its own distinct line |
| `live-kill` | B | gate | yes | no | yes | `bash tools/verify-live-kill.sh` — **claim 7786** (milestone-four follow-on 3, card 3c): the OS, not the program, owns process lifetime — a NEVER-EXITING COUNTER.BIN is force-terminated by the `kill <pid|name>` monitor command with the reserved status 137; NO `counter: alive` marker lands after the kill line (only one task runs at a time, so the killed task never executes again); the kill flows through the REAL lifecycle (`tasks user-exec exited status=137`, `procs COUNTER.BIN exited status=137`, `tasks user-exec reaped`, procs row `state=exited task=reaped exit=137`); the `pages` free recovers by EXACTLY 5 at the reap; and a phase-3 re-exec lands in the freed slot (same task id the counter had). Three scripted phases (`--script`/`--script2`/`--script3`): phase 1 execs the counter + snapshots, phase 2 kills after the first marker, phase 3 snapshots after the reap |
| `live-ipc` | B | gate | yes | no | yes | `bash tools/verify-live-ipc.sh` — **claims 5965/3179** (milestone-four follow-on 3 card 3f + follow-on 4 card 4b): IPC — distinct processes EXCHANGE DATA on real VZ — `exec PEER.BIN` + `exec COUNTER.BIN 1` back to back (the counter's target pid is argv[0] — card 3e's entry contract): the counter's `ipc: ping N` sends (sys_ipc_send, slot 5) and the peer's byte-exact `peer: got ping N` echoes (sys_ipc_recv, slot 6) INTERLEAVE across the whole log (end-to-end data flow between TWO never-exiting processes through the bounded per-process kernel mailbox — 8×64 B BSS rings, the card-4b capacity); the counter sends BURSTS of 6 messages back-to-back in one quantum (card 4b — `ipc: ping N` … `ipc: ping N+5` with no echo between), so the gate asserts the log's peak (sends − echoes) — the ring depth at its deepest — is >= 5 (> 4 messages queued at once) and <= 8 (never over the re-derived capacity), and ZERO `ipc: enospc` refusal markers; every send except the last in-flight burst window (<= 6) has its byte-exact echo, first echo after the first send; the phase-2 `mbox` snapshot shows PEER.BIN pending <= 8 with sent − recv == pending (the bounded ring DRAINED — observed `pending=5 sent=6 recv=1` mid-drain) and COUNTER.BIN `pending=0 sent=0 recv=0`; both processes are `state=running` at the final `procs` and neither ever exits; a fifth exec is `exec: no free scheduler pool slot` (7/7 pool — card 3g re-derived the refusal); the shell stays responsive. Two scripted phases (`--script` after the boot payload exits, `--script2` after `peer: got ping 1` — the flow is established before the drain snapshot) |
| `live-scale` | B | gate | yes | no | yes | `bash tools/verify-live-scale.sh` — **claim 5795** (milestone-four follow-on 3, card 3g — the pool-scale capstone): FOUR live user programs at the 7-slot budget on VZ — `exec COUNTER.BIN` + `exec USER.BIN` ×3 back to back: the procs snapshot shows FOUR `state=running` user rows (one counter + three USER.BINs) with FOUR distinct executor task ids + stack VAs, the programs' markers + the counter's `counter: alive` markers interleave with the worker's advances across the whole log, a FIFTH exec is `exec: no free scheduler pool slot` (7/7 — shell + worker + 4 users + idle; `pool_full` checked before any allocation, leak-free), `addrspaces: tables=150/256` stays inside the 256-page carve-out (106 pages headroom), the counter is still `state=running` at the final procs, and the shell stays responsive. One scripted phase (`--script` after the boot payload exits) |
| `live-procs-syscall` | B | gate | yes | no | yes | `bash tools/verify-live-procs-syscall.sh` — **claim 5799** (milestone-four follow-on 4, card 4a): the process table read FROM EL0 — `exec PEER.BIN` + `exec COUNTER.BIN 1`: PEER.BIN polls `sys_procs` (slot 7) once per quantum until the snapshot shows a running process other than itself, then prints `peer: sees <pid> <name> <state>` per row — the serial log shows `peer: sees 0 user-el0 exited` / `peer: sees 1 PEER.BIN running` / `peer: sees 2 COUNTER.BIN running` (the counter's RUNNING row visible FROM EL0 — distinct from the EL1h monitor's own `procs` read, which shows both with distinct tasks + stacks); after the snapshot the peer enters its recv loop and the IPC flow still echoes byte-exact (`peer: got ping N`); both processes are `state=running` at the final procs and neither ever exits; the shell stays responsive. Two scripted phases (`--script` after the boot payload exits, `--script2` after the first echo) |
| `live-wait` | B | gate | yes | no | yes | `bash tools/verify-live-wait.sh` — **claim 9946** (milestone-four follow-on 4, card 4c): exit-status propagation to a peer — `exec STATUS43.BIN` + `exec COUNTER.BIN 0 1` (the counter's argv[1] is the wait target): STATUS43.BIN prints its alive marker, sleeps 6 scheduler ticks, then exits 43; the counter prints `ipc: waiting pid=1` and blocks in `sys_wait` (slot 8), and the phase-2 `tasks` snapshot (forwarded after the waiting marker) shows TWO `state=blocked` user-exec rows — the sleeping STATUS43 + the waiting counter — while `procs` still shows `id=1 name=STATUS43.BIN state=running` (the target ALIVE while the waiter is blocked — the blocking proof); then `status43: exiting`, the counter's `ipc: saw pid=1 status=43` (the observation — the ring resumes the counter directly after the target's exit, so it precedes the shell's report drain), and the agreeing kernel records `tasks user-exec exited status=43` / `procs STATUS43.BIN exited status=43` / `tasks user-exec reaped` (the runner's `--script-expect` terminal line); the shell stays responsive. Two scripted phases (`--script` after the boot payload exits, `--script2` after the waiting marker) |
| `live-sleep` | B | gate | yes | no | yes | `bash tools/verify-live-sleep.sh` — **claim 3200**: the ESP-loaded user program yields (sys_yield, slot 2), sleeps 2 scheduler ticks (sys_sleep, slot 4 — blocked, woken by timer-driven wakeup, return 0), writes the `user: awake` marker after the wake, and exits (status 43); the scheduler's `blocked` state and the worker's advance lines during the sleep window prove live progress of other tasks; `syscalls` reports `4 sys_sleep calls=1` |
| `live-entropy` | B | gate | yes | no | yes | `bash tools/verify-live-entropy.sh` — **claim 2665**: the kernel's `random` is served by a REAL virtio entropy device (DID 0x1044) — `entropy: seeded n=64` (the boot-time seed after the post-MMU re-arm), `random 32` emits 64 hex chars, the shell stays responsive, exec still loads with a CSPRNG-randomized stack VA, and TWO boots produce DIFFERENT byte sequences and stack placements (the non-determinism proof) |
| `live-reboot-shutdown` | B | gate | yes | no | yes | `bash tools/verify-live-reboot.sh` — **PASS 2026-08-08** (claim 0527): hard gate 6 — a real EFI `ResetSystem` from a live `dipshit>` shell; `reboot` resets the machine (second full takeover, fresh map key), `shutdown` powers it off (VM state → stopped), 4/4 boots |
| `live-fs` | B | gate | yes | no | yes | `bash tools/verify-live-fs.sh` — **PASS 2026-08-09** (claims 3475/6420): hard gate 5 — `ls`/`cat`/`write` persist through reboot **on the disk** via the FAT32 storage driver (claim 6420: `fat.zig` over the virtio-blk transport, run A writes to the live FAT volume, run B — same disk image — still lists/cats it; NVRAM persistence replaced), 1/1 pair |
| `live-gfs` | B | gate | yes | no | yes | `bash tools/verify-live-gfs.sh` — **claim 3678** (milestone-four card 2): the GENERAL (non-ESP) filesystem on VZ — the disk's second FAT32 partition (Linux-FS type GUID, 36 MiB) is mounted by GUID via `mount data`, its window labeled `[data]` and listed, its files read, `hello.txt` written to it, and run B — same disk image — still lists and prints the file from the DATA volume (persistence on the second volume) |
| `live-net-tx` | B | gate | yes | no | yes | `bash tools/verify-live-net-tx.sh` — **claim 1373** (milestone five, card N1): the virtio-net TRANSPORT + TX on real VZ — runner `--net <capture>` attaches `VZVirtioNetworkDeviceConfiguration` (fixed host MAC 02:00:00:00:00:01); the guest discovers DID 0x1041, negotiates VER1\|MTU\|MAC (the device needs MTU — `feat=0x28/0x1`), reads the host-set MAC via the feature path, arms queues 0/1 size 4, re-arms post-exit (net device does NOT reset at EBS — `net: pre-rearm st=0f`), and `netsend` drives TX with a zeroed 12-byte virtio_net_hdr prefix (observed contract) — the host capture file holds the EXACT Ethernet frames: phase 1 asserts the 46-byte known frame byte-for-byte, phase 2 asserts ring reuse + honest truncation (46 + 46 + 1514 bytes, frames=3); 2/2 phases PASS |
| `live-net-rx` | B | gate | yes | no | yes | `bash tools/verify-live-net-rx.sh` — **claim 6076** (milestone five, card N2): the virtio-net RX path + the ROUND TRIP on real VZ — the runner's `--net-inject <file>` writes the `--net` attachment's socket end ONCE on the guest's `net: rx-armed` serial trigger (host→guest; OFF by default); the guest supplies queue 0 with one fixed BSS buffer, drains the used ring POLLED (the net device's used-buffer IRQ unobserved — recorded, not assumed), MAC-filters (own + broadcast, drop the rest with a counter) into a bounded 4-slot frame FIFO, and `net recv` prints the frame byte-exact; phase 1 injects the 60-byte broadcast known frame and asserts `net recv` byte-exact + the counters AND the round trip (the guest re-sends it — the host capture is byte-exactly the injected fixture), phase 2 asserts an own-MAC frame received byte-exact, phase 3 asserts a foreign-MAC frame dropped (`filtered=1`, rx-obs still records the delivery); 3/3 phases PASS. Claim-time observations pinned in the hardware contract: the device writes a 12-byte virtio_net_hdr into RX buffers (num_buffers=1) and refuses an RX buffer under 1530 bytes |
| `live-net-arp` | B | gate | yes | no | yes | `bash tools/verify-live-net-arp.sh` — **claim 7293** (milestone five, card N3): ARP live on real VZ over the N2 RX seam — `kernel/src/arp.zig` (static IP via `net ip`, byte-exact request/reply builds, bounded 4-slot table, counters) wired into the RX drain (answer requests for our IP, learn replies, drop the rest) + `net arp [<ip>]` + the runner's `--net-arp-respond <host-ip>` (deterministic host-side answer, OFF by default); phase 1 the guest answers the injected request for its IP (42-byte reply byte-exact in the capture, repl=1); phase 2 the guest resolves 10.0.0.2 (request byte-exact in the capture; the host answer lands as `10.0.0.2 -> 02:00:00:00:00:02`, learn=1); phase 3 a request for 10.0.0.99 is dropped (drop=1, repl=0, still observable via `net recv`); 3/3 phases PASS. Claim-time observation: the device delivers/transmits the 42-byte ARP frames unpadded (below the Ethernet 60-byte minimum) |
| `live-net-icmp` | B | gate | yes | no | yes | `bash tools/verify-live-net-icmp.sh` — **claim 0148** (milestone five, card N4): IPv4/ICMP live on real VZ over the N2 RX seam BESIDE the ARP dispatch — `kernel/src/ipv4.zig` (RFC 1071 checksums, byte-exact ICMP echo request/reply builders, fragments dropped counted — no reassembly) wired into the RX drain (answer echo requests for our static IP, observe echo replies — pong + seq, drop the rest) + `net ping <a.b.c.d>` (needs the peer in the ARP table — an echo has a unicast dst) + the runner's `--net-icmp-respond <host-ip>` (deterministic host-side echo answer, OFF by default); phase 1 the guest answers the injected 46-byte echo request for its IP (reply byte-exact in the capture with the identification + id/seq/payload echoed, repl=1, `net recv` observes the request at device len 58); phase 2 the guest resolves 10.0.0.2 and pings it (the ARP request + the 46-byte echo request id/seq 1 are byte-exact in the capture; the host answer lands as pong=1 with seq=1); phase 3 an echo request for 10.0.0.99 is dropped (drop=1, repl=0, still observable via `net recv`); 3/3 phases PASS. No new hardware-contract entry — the 46-byte frames travel unpadded, consistent with the N3 observation |
| `live-net-udp` | B | gate | yes | no | yes | `bash tools/verify-live-net-udp.sh` — **claim 8552** (milestone five, card N5): UDP live on real VZ over the N4 IPv4 seam — `kernel/src/udp.zig` (RFC 768: the 8-byte header, the checksum over the IPv4 pseudo-header computed ALWAYS, a bounded 4-slot LISTEN table, bounded per-listener datagram rings, LOOPBACK — a send to our own IP delivers locally, no device round trip) wired into ipv4.zig's protocol dispatch (protocol 17 → udp on already-validated frames) + `net udp [listen|close|send|recv]` + the runner's `--net-udp-respond <host-ip>:<host-port>` (deterministic host-side echo answer, OFF by default); phase 1 LOOPBACK: a send to our own IP is delivered byte-exact (rx=1 tx=1 loop=1) with an EMPTY capture; phase 2 the host's injected datagram is delivered to the listener byte-exact (device len 58); phase 3 the guest resolves 10.0.0.2 and sends to it (the ARP request + the 46-byte datagram are byte-exact in the capture; the host answer lands in the listener buffer, rx=1 tx=1); phase 4 a datagram to a closed port is dropped (drop=1, no delivery, still observable via `net recv`); 4/4 phases PASS. Claim-time fix recorded: the pseudo-header zero/protocol word was initially reversed (0x1100 vs 0x0011) — caught by the byte-exact fixtures, fixed. No new hardware-contract entry — the 46-byte datagrams travel unpadded, consistent with the N3/N4 observation |
| `live-net-udp-syscall` | B | gate | yes | no | yes | `bash tools/verify-live-net-udp-syscall.sh` — **claim 1384** (milestone five, card N6): the UDP SYSCALL SEAM live on real VZ — the ADR 0007 amendment slots 9/10/11 (`sys_udp_listen`/`sys_udp_send`/`sys_udp_recv`, implemented 9 → 12) driven end to end by UDP.BIN (the first network-syscall user program, loaded by `exec`): `sys_udp_listen(7000)` → `udp: listen ok`; the LOOPBACK send+recv to its OWN IP → `udp: loop ping` (the 12-byte datagram, byte-exact); the peer send to 10.0.0.2:9999 + poll `sys_udp_recv` for the host's `--net-udp-respond` answer → `udp: got ping` (cooperative `sys_yield` between polls — the ring returns to the program; the recv DRAINS THE DEVICE FIRST, the claim-6076 polled-drain contract); the `EINVAL` mapping from EL0 (unbound-port recv + unresolved-peer send → `udp: recv err -1` / `udp: send err -1`, nothing transmitted); `sys_exit(17)` → `procs UDP.BIN exited status=17` / `tasks user-exec exited status=17` / `tasks user-exec reaped`; the capture is byte-exact (the 42-byte ARP request + the 46-byte datagram); phase 2 the observation commands on the SAME kernel state: `syscalls` rows 0–11 (implemented=12, rows 9/10/11 counted) + `net udp`/`net` counters rx=2 tx=2 loop=1 drop=0; 4/4 phases PASS. Gate-engineering lessons recorded: the expect is keyed on the program's OWN completion markers (`udp: got ping` / `tasks user-exec reaped`) — an early expect killed the VM at ~5 s before the ring returned to the program after its yield and a HEALTHY kernel looked hung (switches=5, all tasks ready, the answer already rx'd); the marker greps carry `|| true` so an absent marker FAILS the gate instead of killing it under `set -euo pipefail` |
| `live-net-nat` | B | gate | yes | no | yes | `bash tools/verify-live-net-nat.sh` — **claim 4678** (milestone five, card N7): outbound connectivity live on real VZ through `VZNATNetworkDeviceAttachment` (the runner's `--net-nat` flag; mutually exclusive with `--net`, OFF by default — the default VM stays byte-identical). NO guest code: the existing stack (static IP + ARP + ICMP) is driven against the NAT gateway and the gate asserts GUEST-OBSERVED COUNTERS — the capture-file byte-exact evidence does not apply through NAT (the host translates the frames; the card's documented gate-shape change). Claim-time observations pinned in the hardware contract: the NAT attachment HONORS the configured locally-administered MAC (`net: mac=02:00:00:00:00:01 source=feature`); the observed subnet/gateway is 192.168.64.0/24/.1 (VZ exposes no prefix API); the gateway answers ARP for its IP (the guest learns the gateway MAC, `learn=1`) and ICMP echo (`net ping` → `pong=1` `seq=1` — the deterministic proof, no internet); the NAT router MAC varies per boot (gate asserts the learned-line prefix, never a hardcoded MAC); the router sends IPv6 multicast at boot (dropped by the N2 MAC filter, `filtered=3`); off-subnet addresses are not proxy-ARP'd (the guest's ping honestly refuses — no routing rung); ONE run, 11/11 assertions PASS |
| `live-net-dhcp` | B | gate | yes | no | yes | `bash tools/verify-live-net-dhcp.sh` — **claim 0351** (milestone five, card N8): the bounded RFC 2131 DHCP client on the N5/N6 UDP layer live on real VZ, TWO phases. Phase 1 (deterministic file-handle, `--net` + `--net-dhcp-respond 10.0.0.2`): the guest's `net dhcp` runs the FULL handshake against the host's crafted server — the 286-byte DISCOVER byte-exact in the capture (dst ff*6, src 02:00:00:00:00:01, 0x0800, 68→67, op 1, cookie 0x63825363, option 53 = 1) → OFFER → the 298-byte REQUEST (the SAME xid, option 53 = 3) → ACK → `net: dhcp bound ip=10.0.0.2 mask=255.255.255.0 gw=10.0.0.1 server=10.0.0.2 lease=3600`, the report counters `discover=1,offer=1,request=1,ack=1,nack=0,timeout=0,mal=0`, and the host's NET-DHCP OFFER/ACK lines (16/16 assertions). Phase 2 (real NAT, rides `--net-nat`): the CLAIM-TIME observation — the VZ NAT attachment serves NO DHCP server on this host (the DISCOVER went out, `offer=0, mal=0`; honestly recorded in the hardware contract with the saved log under `artifacts/live-net-dhcp-nat-explore/`, never faked), and the guest is NOT stranded: the static fallback still reaches the NAT gateway (`pong=1 seq=1`) |
| `live-net-dhcp-renew` | B | gate | yes | no | yes | `bash tools/verify-live-net-dhcp-renew.sh` — **claim 9489** (milestone five, card N9) + **audit follow-up 3 (issue #119)**: the DHCP lease lifecycle (RFC 2131 §4.4.5) live on real VZ — REBUILT for the autonomous reality: the idle-loop poll now drives T1/T2/expiry, so the gate proves the rungs against the new flow with the refusal knobs. Run A (lease 100 s, `--net-dhcp-respond-norenew`): the poll RENEWs at T1 with a UNICAST REQUEST to the server (byte-assertable in the 1222-B capture — dst 02:00:00:00:00:02, src/dst IP 10.0.0.2, ciaddr 10.0.0.2), the host REFUSES it (its own NET-DHCP line), and at T2 the client ESCALATES to REBINDING (the new RFC 2131 §4.4.5 RENEWING→REBINDING rung) with a BROADCAST REQUEST (frame: dst ff:ff:ff:ff:ff:ff), whose ACK restarts the lease; counters `renew=1,rebind=1,renewed=1,expired=0`. Run B (lease 100 s, `--net-dhcp-respond-norebind`): the poll attempts the REBINDING broadcast at T2 (REFUSED), and at expiry the lease is RELEASED (`lease expired (elapsed=… >= lease=100)`; the report `dhcp=idle,ip=0.0.0.0,…,rebind=1,renewed=0,expired=1` — rebind=1 records the autonomous rebind attempt) and the client RECOVERS with a fresh DISCOVER → BOUND again (the ciaddr==0 initial REQUEST is still answered). The runner's `--net-dhcp-respond-norenew`/`-norebind` knobs are flag-gated, defaults unchanged |
| `live-net-dhcp-autonomous` | B | gate | yes | no | yes | `bash tools/verify-live-net-dhcp-autonomous.sh` — **audit follow-up 3 (issue #119)**: THE AUTONOMY PROOF live on real VZ — the DHCP lease lifecycle advances from the kernel's shell idle loop with NO typed `net dhcp`. Lease 100 s + `--net-dhcp-respond-norenew`: phase 1 binds (`net dhcp` ×12) + resolves the server MAC (`net arp 10.0.0.2`); phase 2 (delay 92, past T2=87) types NO net dhcp — only a marker + a `net` REPORT (a report reads the lifecycle, it cannot advance it). The poll must RENEW at T1=50 (the UNICAST REQUEST, refused by the host) and ESCALATE to REBINDING at T2=87 (the BROADCAST REQUEST, ACKed). Assertions: the bound lease, the renewing (T1) + rebinding (T2) transition lines, the report `renew=1,rebind=1,renewed=1,expired=0`, the host's refused-unicast line, the 1222-B capture (RENEW unicast dst 02:00:00:00:00:02 / dst IP + ciaddr 10.0.0.2, REBIND broadcast), the premise self-check (the phase-2 script contains no `net dhcp`), the runner flags, the serial markers — closing the audit finding that the N9 enforced lease only ran on demand (while `tcp.poll_rto` was already autonomous). The re-DISCOVER after expiry stays command-triggered (bounded handshake; the client honestly drops to idle) |
| `live-net-tcp` | B | gate | yes | no | yes | `bash tools/verify-live-net-tcp.sh` — **claim 7026** (milestone five, card N10): the bounded RFC 793 TCP client live on real VZ. THREE runs: Run A (deterministic file-handle, `--net-tcp-respond 10.0.0.2:9999` + `--net-arp-respond 10.0.0.2`) — the FULL lifecycle byte-exact: SYN (54 B, src 8000 → 9999) → SYN-ACK (the FIXED server ISN 0x12345678) → the handshake ACK (ack 0x12345679) → ESTABLISHED → `send 5` (the data 01 02 03 04 05) → the host ECHOES it (ack 0x1234567e) → `recv` prints it → `close` (FIN) → FIN-ACK → the final ACK (0x1234567f) → closed, then a second connect + `reset` (a real RST); counters `syn=2,synack=2,ack=4,data_s=1,data_r=1,fin=1,finack=1,rst_s=1,rst_r=0,timedout=0,mal=0`; the 533-B capture's NINE frames pass the python walk (the seq/ack chain + every TCP checksum). Run B (ARP-responder-only — a deterministic black hole) — the bounded connect timeout (30 s of guest ticks): `tcp=syn_sent,syn=1,synack=0` → after 31 s `connect refused (no SYN-ACK after 30s)` → `tcp=idle,peer=0.0.0.0:0,timedout=1`. Run C (real NAT, `--net-nat`) — the CLAIM-TIME observation: the VZ NAT gateway answers the SYN with a RST (no TCP listener on 192.168.64.1:9999 — `rst_r=1`, `tcp=closed`, the drive returns the client to idle; recorded `[observed]`, never faked). 36/36 assertions |
| `live-net-tcp-rto` | B | gate | yes | no | yes | `bash tools/verify-live-net-tcp-rto.sh` — **claim 5357** (milestone five, card N11): the bounded TCP retransmission + retransmit timer live on real VZ. THREE runs: Run A (deterministic file-handle, ARP-responder-only — a black-hole SYN) — the idle-loop RTO poll retransmits the pending SYN AUTONOMOUSLY (the `syn retransmitted (1/10)` / `(2/10)` lines), `retx=2,abort=0` in the report (still `syn_sent` — the 30 s connect timeout has not expired), and the capture holds the byte-identical SYN frames (the SAME seq/bytes/checksums — a python walk). Run B (deterministic file-handle + the full responder) — the SYN-ACK clears the pending state: `retx=0` despite the 7 s wait, no retransmission lines, EXACTLY ONE SYN in the capture (the handshake completes). Run C (deterministic file-handle + the `:handshake` responder — the data black hole) — connect → established → `send 5` (never ACKed) → the data retransmitted TEN times → `retransmission limit reached (10) — connection aborted` → the report releases (`tcp=idle,peer=0.0.0.0:0,retx=10,abort=1`), `net tcp` reads no connection, and the capture holds the ELEVEN byte-identical data frames. The runner's `--net-tcp-respond …:handshake` mode is flag-gated (default unchanged). 34/34 assertions |
| `live-screen` | B | gate | yes | no | yes | `bash tools/verify-live-screen.sh` — **claim 6053** (milestone six, card G1): the virtio-GPU TRANSPORT + FRAMEBUFFER live on real VZ — the runner's `--display`/`--screenshot` attach `VZVirtioGraphicsDeviceConfiguration` (1280×720 scanout; OFF by default); the guest's `kernel/src/virtio_gpu.zig` drives the modern virtio-pci gpu (DID 0x1050, class 0x038000, dev 7), negotiates VER1-only (device offers RING_PACKED\|RING_EVENT_IDX\|RING_INDIRECT_DESC\|VERSION_1), arms controlq 0 + cursorq 1 (size 4), re-arms post-exit (**VZ resets the gpu at ExitBootServices — `pre-rearm st=00` observed**), and runs the spec 2D path GET_DISPLAY_INFO → CREATE_2D (B8G8R8X8) → ATTACH_BACKING (4K-aligned BSS framebuffer) → SET_SCANOUT → TRANSFER → FLUSH; the 1.2 `display_one` (24-B pmodes), a `next=0` tail descriptor, cache cleans before kick/transfer, and an OPAQUE alpha byte are the claim-time fixes. The scripted boot runs `screen` (report) + `screen fill 00ff00` + `screen peek` (guest-side bytes); the gate asserts the transport report (did/feat/scanout/status/rearm/setup healthy — cmds is session-dynamic since G3's terminal drains add TRANSFER+FLUSH pairs — fill transfer=ok flush=ok, peek p1=0xff) AND decodes the captured PNG itself: since G3 (Road Pops) the terminal renders over the raw fill, so the pixel phase asserts the NON-BLANK terminal frame (the text region samples fg=0.300 over bg=0.700) with the fill proven guest-side. The capture MUST come from ScreenCaptureKit (the composited window, pixel-identical to `--display`; any cacheDisplay-fallback capture fails the gate). **The first non-blank guest framebuffer** (G1's claim-time pixel moment). 1/1 PASS; evidence `artifacts/live-screen-*`, `artifacts/gpu-screen-*s` |
| `live-text` | B | gate | yes | no | yes | `bash tools/verify-live-text.sh` — **claim 3194** (milestone six, card G2): FRAMEBUFFER TEXT live on real VZ — `kernel/src/text.zig` (the public-domain 8x8 bitmap font, ASCII 0x20–0x7e, fixed BSS glyphs; putc/puts, cursor, wrap, a bounded 128-line scrollback ring, `clear`; the pure renderer host-tested against an injectable mock canvas — 21 tests) paints the SAME banner + `dipshit>` prompt the serial log carries over G1's framebuffer (fg 0x00ff00 on bg 0x101418) through G1's transfer/flush unchanged; `text`/`text put <string>`/`text clear` (registry 35→36). The scripted boot runs `text` (report); the gate asserts the serial evidence (`text: boot banner presented` — emitted by the Road Pops tee's first present since G3 — the report's STABLE parts `text: rows=90 cols=160 cell=8x8 … fg=0x…ff00 bg=0x…1418` — cur/lines are session-dynamic since the echoed session lands in the ring — and the banner lines + prompt on serial, the shared seam) AND decodes the captured PNG itself: the banner region samples BOTH green-family foreground (fg=0.255) and dark background (bg=0.745) — the screen is no longer monochrome — with the region below all background. Honest bound (the G1 precedent): byte-exact glyphs live in the class A mock; the LIVE assertion is "text visible with the expected color family" (color-managed + retina-scaled). The capture MUST come from ScreenCaptureKit (the composited window; any cacheDisplay-fallback capture fails the gate). 1/1 PASS; evidence `artifacts/live-text-*`, `artifacts/gpu-screen-*s` |
| `live-roadpops` | B | gate | yes | no | yes | `bash tools/verify-live-roadpops.sh` — **claim 1574** (milestone six, card G3): ROAD POPS live on real VZ — the boot TERMINAL is on the screen. `kernel/src/road_pops.zig` is a TEE console: every byte still reaches serial FIRST (the shared seam — the transcript gates keep passing) AND G2's text layer paints the same banner + prompt + every reply, drained ONE full-frame present per output batch by the shell idle loop; the G2 one-shot boot paint is replaced by the tee rendering the shell's OWN banner (its first present emits `text: boot banner presented`). Claim-time fix (claim-0015 redux, observed live): the `Target` struct literal was folded into `.rodata` with link-time `&fn` addresses — the tee's first write faulted at the link-time `rp_text_put_bytes`; the Target is built in RAM now. `roadpops` command (registry 36→37: armed/dirty/presents). Scripted boot: `echo ROADPOPS` + `uname` + `roadpops`; the gate asserts the serial session (arm line, boot-banner evidence, `roadpops: armed=1 … presents>=1`, the banner + echo + uname replies — the shared seam) AND decodes the captured PNG: the banner region AND the terminal region below it both show green-family foreground over the dark background (fg=0.255 banner / fg=0.124 session) — a WORKING terminal, not a one-shot splash. The capture MUST come from ScreenCaptureKit (the composited window; any cacheDisplay-fallback capture fails the gate). 1/1 PASS; evidence `artifacts/live-roadpops-*`, `artifacts/gpu-screen-*s` |
| `live-glyphs` | B | gate | yes | no | yes | `bash tools/verify-live-glyphs.sh` — **the MIRROR-REGRESSION TRIPWIRE** (follow-on to the ScreenCaptureKit switch, 2026-08-12): the captured framebuffer is decoded against the kernel's OWN font table (`kernel/src/font8x8.zig` via `tools/decode-screen-glyphs.py`) and the text must read FORWARD. The pixel gates prove text IS there; this gate proves it reads the right way — a mirrored screen fails mechanically. The matcher finds the green glyph grid (scored pitch+origin phase search over the bright stroke core — the composited-window captures render the guest framebuffer with display smoothing, so the threshold sits at g>140 where strokes sit at their ideal 2x positions, excluding the anti-aliased smear; verified cell-by-cell on the 'D'), decodes the visible session in BOTH orientations, and asserts: forward unknowns ≈ 0 (allowance 2: the cursor block + at most one mid-present partial cell; measured 0/604), mirrored unknowns ≫ forward (measured 549/595 — the matcher can tell the difference), and the decoded text contains the boot banner + prompt (the semantic forward proof). **The decoder ALSO decodes the Driving Award clock overlay** (the window manager's amber title bar + `DRIVING AWARD` accent on navy) in both orientations — the title must decode as `clock` and the body as `DRIVING AWARD` (Phase 2d), covering the window-manager path (G5's draw_string + blit_rect), which shares the forward glyph blit but uses a different color pair, so a mirror there would NOT trip the green-terminal matcher. An OFFLINE in-cell mirror self-test (`--self-test`, class A, wired into `verify-portable`/CI) renders the clock forward, mirrors each glyph in-cell, and asserts the tripwire fires without a VZ boot. The SCK evidence assertion is enforced here too (Phase 0). Mirror simulations (whole-image flip + in-cell flip) both explode the forward decode to ~120 unknown cells — the tripwire fires. 1/1 PASS; evidence `artifacts/live-glyphs-*`, `artifacts/gpu-screen-*s` |
| `live-xhci` | B | gate | yes | no | yes | `bash tools/verify-live-xhci.sh` — **claim 4272** (milestone seven, card I1): the XHCI host-controller transport live on real VZ — the runner's `--input` flag (OFF by default) attaches the keyboard + pointing devices (VZUSBKeyboardConfiguration + VZUSBScreenCoordinatePointingDeviceConfiguration), which claim 3868 established present as an Apple XHCI USB host controller (VID=0x106b DID=0x1a06 CLS=0x0c0330, NOT virtio-input). `kernel/src/xhci.zig` discovers the device pre-exit (bus 0 dev 8), maps its MMIO register space post-MMU (BAR0=0x50001000 cap regs / BAR1=0x50000000; CAPLENGTH=0x20, HCIVERSION=0x110, DBOFF=0x940, RTSOFF=0x520, HCSPARAMS1=0x10002010 = 16 slots/32 intrs/16 ports), sets up the command ring + event ring + ERST + primary interrupter, drives a NO-OP command TRB to a Command Completion Event (CC=1 — the ring machinery proven), and reads the port status. The gate asserts the guest's own `usb` report (the card's documented gate-shape change — byte-exact host capture does not apply to a memory-mapped controller): the boot-time init line, device identity, MMIO BARs + base, parsed register map, HCSPARAMS decode, the pre-reset observation (USBSTS=0x9/USBCMD=0x0 — **VZ does NOT reset the controller at ExitBootServices**), the NO-OP completion (cc=1, USBSTS=0x0 running), ports 9+10 connected (CCS=1 — the two HID devices, the I2 handoff) with ports 1-8/11-16 unconnected, and a responsive shell. ONE run, 14/14 assertions PASS; evidence `artifacts/live-xhci-*`, `artifacts/xhci-discovery-*` |
| `live-usb` | B | gate | yes | no | yes | `bash tools/verify-live-usb.sh` — **claim 4116** (milestone seven, card I2): USB enumeration + HID over the XHCI transport live on real VZ — the runner's `--input` flag (OFF by default) attaches the keyboard + pointing devices, and `kernel/src/xhci.zig` now enumerates them end to end (port reset → Enable Slot → Address Device → device + config descriptors over the control endpoint → Set Configuration 1 → interrupt-IN endpoint armed) and parses the HID boot-protocol reports. The gate asserts the guest's own `usb devices` + `usb report` lines (the card's evidence shape — a memory-mapped controller + device reports have no host-side byte-exact capture): BOTH devices enumerated (`usb: enumerated=2`), the keyboard row (port 9/slot 1, VID 0x05ac PID 0x8105, boot protocol=1, EP1-IN maxpkt 8, Set_Protocol(boot) ACCEPTED boot=1), the pointer row (port 10/slot 2, VID 0x05ac PID 0x8106, protocol=0 — NOT a boot mouse — EP1-IN maxpkt 10, Set_Protocol(boot) honestly REFUSED boot=0), the observed 8-byte keyboard report `00 00 04 00 00 00 00 00` (mod 0, HID usage 0x04 = 'a') produced by the runner's minimal synthesized-key seam (`--input-key 0` + `--input-key-after "usb: enumerated"` — VZ has NO programmatic keyboard API, so one synthesized NSEvent keyDown is dispatched into the VZVirtualMachineView), its decode (`mod=0x0 keys=0x4`), and a responsive shell. ONE run, 11/11 assertions PASS; evidence `artifacts/live-usb-*`, `artifacts/usb-discovery-*` |
| `live-input` | B | gate | yes | no | yes | `bash tools/verify-live-input.sh` — **claim 6050** (milestone seven, card I3): SCRIPTED KEYSTROKES DRIVE ROAD POPS live on real VZ — the runner's `--input` flag (OFF by default) attaches the keyboard + pointing devices, and `kernel/src/input.zig` (a bounded pure-BSS event FIFO + HID-usage → ASCII keymap + the shell-idle drain, the card-3d pattern) decodes the XHCI interrupt-IN reports and feeds the Road Pops tee's read path so keystrokes reach the line editor. VZ has NO programmatic keyboard API, so the runner's `--input-string`/`--input-string-after` synthesizes one NSEvent per keyDown/keyUp into the VZVirtualMachineView. The gate types `input\n` (the I3 monitor command) and asserts the guest's own `input` report (the card's evidence shape): `events=6` (i,n,p,u,t,Enter) with `dropped=0` and `kb-usage=0x28 kb-byte=0xa` (Enter) — the typed command ran end to end. Claim-time observations pinned in the hardware contract: VZ delivers ~one report per Road Pops present cadence (the seam types at 2 s per keystroke; faster drops reports); the input drain runs BEFORE the Road Pops present so a report is never starved behind a slow full-frame present. **SUPERSEDED observation (2026-08-15, issue #117):** the "single-TRB arming is correct — a multi-TRB depth experiment wrapped the transfer ring at the 8th report" conclusion rested on the PRE-U2 arm bug (claim 1809 fixed the wrap) AND the fast-typing drop is a VZ host delivery-model limit, not a guest arming bug (the host keyboard flushes ~one report per present; keyDown/keyUp pairs inside that window net to zero). The depth-8 re-test lives in `live-input-depth` (claim 7302). ONE run, 8/8 assertions PASS; evidence `artifacts/live-input-*` |
| `live-input-depth` | B | gate | yes | no | yes | `bash tools/verify-live-input-depth.sh` — **claim 7302 (audit follow-up 2, issue #117)**: the MULTI-TRB INTERRUPT-IN DEPTH 8 RE-TEST live on real VZ — the keyboard types `echo fastok <Enter> input <Enter>` at 2.0 s/chord (the measured VZ keyboard delivery ceiling; the runner's new `--input-chords-delay` knob, default 3.0 s so every existing gate stays byte-identical) and the gate asserts the byte-exact result: `fastok` echoed exactly once, the keyboard-typed `input` report shows `dropped=0 events=18` (echo fastok = 12, input = 6), `kb-usage=0x28 kb-byte=0xa` (Enter), the serial marker, and the runner's chord flag with the 2.0 s delay. Claim-6050's single-TRB conclusion is superseded on both grounds: its multi-TRB experiment ran on the PRE-U2 arm (read one past the buffer array at the Link wrap, claim 1809) and the measured fast-typing ceiling is VZ's own delivery model — at ≤1.0 s/chord VZ delivers NOTHING (state-based keyboard, ~one report per full-frame present), so no guest arming depth raises the steady-state rate. Depth-8's value is the correct XHCI shape + burst buffering when VZ's main queue stalls. ONE run, 9/9 assertions PASS; evidence `artifacts/live-input-depth-*` |
| `live-editing` | B | gate | yes | no | yes | `bash tools/verify-live-editing.sh` — **milestone-eight card U2 (claim 1809)**: the ADR 0008 D2 line editor on real VZ — the runner's new `--input-chords` seam synthesizes one NSEvent per chord (printable chars + Up/Left arrows; keyDown + keyUp, 3 s spacing) into the VZVirtualMachineView, the I3 keymap (`hid_to_bytes`) decodes the arrows to `ESC [ <final>` sequences, and `kernel/src/lineedit.zig` recalls/edits them: the gate types `echo ab <Left> c <Enter> <Up> <Enter> echo u2done <Enter>` and asserts the guest's own echo output — `acb` exactly twice (the mid-line insert turned `echo ab` into `echo acb`, then the Up recall re-ran it) and `u2done` exactly once (the runner's exit marker) — over the XHCI transport end to end; evidence `artifacts/live-editing-*` |
| `live-win-hig` | B | gate | yes | no | yes | `bash tools/verify-live-win-hig.sh` — **claim 0935** (milestone eight, card U5, ADR 0008 D4): the window chrome is VISIBLE and MOVES with focus — ONE `--input --display` run: `win cycle` focuses the clock, `exec WINLOOP.BIN` opens a user window (which takes focus), the capture fires on `winloop: present ok`, and the decoded capture asserts the white 3-px FOCUS RING on the focused window's rect corner, the terminal's screen edge NOT ringed, and the user window's dark-blue TITLE BAR strip (scale-aware sampling — the capture arrives at 1x or 2x). Cycling is driven by `win cycle` over serial (the Alt+Tab HID decode is host-tested; synthesized modifiers never reach VZ's HID report, so the chord is not gated live). Card U4's pointer half is host-tested but ungated here: five synthesized pointer delivery routes observed producing ZERO guest pointer reports (hardware contract). PASS 8/8; evidence `artifacts/win-hig-*`, `artifacts/hig-screen-*` |
| `live-win` | B | gate | yes | no | yes | `bash tools/verify-live-win.sh` — **claim 1543** (milestone six, card G5): DRIVING AWARD, the window manager, live on real VZ — `kernel/src/driving_award.zig` (a bounded fixed-BSS window registry, z-order = array order, focus tracked by id, topmost-window hit-testing, and a dirty-rect compositor that repaints from the LOWEST dirty window up) makes Road Pops window 0 (the full-screen terminal) and a 1 Hz clock overlay window 1 (960,16 304×192, amber title bar 0xb58900 / navy body 0x0a1a2e, own BSS back-buffer). The G3 tee's present routes through the compositor and the I3 keyboard read source is gated on `terminal_focused()`. `win`/`win focus <n>`/`win raise <n>`/`win hit <x> <y>` (registry 39→40); 9 host tests pin the hit-test/raise/focus/repaint/blit contracts. The gate asserts the serial session (`win: windows=2 focused=0`, the two `win[]` rows, `win hit 1000,100 -> 1` focusing the clock, `win hit 100,400 -> 0` re-focusing the terminal) + a KEYBOARD-typed `uname` landing in the focused terminal (`DipshitOS aarch64`), AND decodes the captured PNG: the clock's amber title bar + navy body OVER the terminal (no terminal foreground inside the clock rect — z-order) with the terminal's green glyphs beside it. ONE run, 8/8 serial assertions + the decoded-capture phase PASS; evidence `artifacts/live-win-*`, `artifacts/gpu-screen-*s` |
| `live-win-syscall` | B | gate | yes | no | yes | `bash tools/verify-live-win-syscall.sh` — **claim 0487** (milestone six, card G6): THE DRAW/WINDOW SYSCALL SEAM + PER-PROCESS OWNERSHIP live on real VZ — the ADR 0007 amendment slots 12/13/14 (`sys_win_open`/`sys_win_fill`/`sys_win_present`, implemented 12 → 15, then a teardown follow-on adds slot 15 `sys_win_close` + `win close` → 16) expose the G5 window manager's user-window surface to EL0, with a window OWNED by its opening process and AUTO-CLOSED when it exits (the scheduler's `exit_current` calls `driving_award.close_owner`; fill/present/close are owner-restricted). WIN.BIN (`user/src/win.zig`, the first graphics user program, loaded by `exec`) drives it end to end: `win: open id=2` → `win: fill ok` (dark-blue background + red/cyan/white blocks) → `win: present ok` → `sys_exit(87)` → `procs WIN.BIN exited status=87` — and its window AUTO-CLOSES (`win: windows=2`, `sys_win_close calls=0`). WINLOOP.BIN (`user/src/winloop.zig`, the eighth user program) then opens the same window and yield-loops forever so the decoded capture pixel-proves an EL0-rendered window on the scanout: `win: windows=3` + `win[2]: user user rect=64,64,256,192`, `syscalls` implemented=21 with open=2/fill=8/present=2/close=0; the `win` report's OWNER column shows the owning pid (win[2] owner=2, fixed windows owner=-) and `win list 2` → matches=1 while a non-owner `win list 0` → matches=0. ONE run PASS; evidence `artifacts/live-win-syscall-*`, `artifacts/gpu-screen-*s` |
| `live-win-close` | B | gate | yes | no | yes | `bash tools/verify-live-win-close.sh` — **claim 0487 teardown follow-on** (milestone six, card G6): THE DRAW/WINDOW RELEASE PROOF live on real VZ — WINCLOSE.BIN (`user/src/winclose.zig`, the SEVENTH graphics user program, loaded by `exec`) drives the full seam and then tears the window down entirely from EL0: `sys_win_open` (slot 12) → `sys_win_fill` (slot 13) → `sys_win_present` (slot 14) → `sys_win_close` (slot 15) → `sys_exit(88)` ('X'). The gate proves the window DISAPPEARS (`win: windows=2` after the close, no `win[2]:` row ever) and the SLOT IS REUSABLE (a re-exec prints `win: open id=2` AGAIN — never id 3 — the first close freed the slot instead of leaking it), with the script2 `syscalls` snapshot on the same kernel state (implemented=21, open=1/close=1). ONE run, 8/8 assertions PASS; evidence `artifacts/live-win-close-*` |
| `live-win-move` | B | gate | yes | no | yes | `bash tools/verify-live-win-move.sh` — **claim 0487 move/raise follow-on** (milestone six, card G6): THE DRAW/WINDOW MOVE/RESTACK PROOF live on real VZ — WINMOVE.BIN (`user/src/winmove.zig`, the NINTH graphics user program, loaded by `exec`) drives the ADR 0007 slots 16/17/18/19/20 (`sys_win_move`/`sys_win_raise`/`sys_win_get`/`sys_win_query`/`sys_win_set_visible`, implemented 16 → 21) entirely from EL0: `sys_win_open` → `sys_win_fill` ×4 → `sys_win_present` → `sys_win_move(800,400)` → `sys_win_move(1200,700)` (the CLAMP proof — it falls off the 1280×720 scanout, so it clamps to the corner (1024,528)) → `sys_win_raise` → `sys_win_present` → `sys_win_get` (reads the clamped rect back — `winmove: get 1024,528,256,192`) → `sys_win_query` (reads the FULL state — `winmove: query 1024,528,256,192 z=2 focused=1 visible=1 dirty=1`) → `sys_win_set_visible(2,0)` (HIDE, held 2 ticks) → `sys_win_set_visible(2,1)` (SHOW) → yield-forever. The gate proves the window MOVED and the PIXEL DISAPPEARS/RETURNS: `win: windows=3` + `win[2]: user user rect=1024,528,256,192 owner=<pid> visible=1`, `syscalls` implemented=21 with open=1/fill=4/present=3/move=2/raise=1/get=1/query=1/set_visible=2/close=0, the EL1h halves `win move 2 1024 528` + `win raise 2`, the marker-driven capture (`--screenshot-after "winmove: hide ok"`) shows NO red/cyan/white blocks at the clamped spot (the hide landed), and the LATEST decoded capture shows the window's colors BACK at the NEW position with the terminal where it used to be. ONE run PASS; evidence `artifacts/live-win-move-*`, `artifacts/gpu-screen-*` |
| `verify-vz` | B | aggregate | no | no | yes | `just verify-vz` — serial takeover + bad-handoff + marker + nvram-console + host-console + live-transcript + live-fs + live-gfs + live-exceptions + live-timer + live-tasks + live-userspace + live-svc + live-uaccess + live-addrspaces + live-lifecycle + live-exec + live-procs + live-concurrent + live-args + live-long-lived + live-kill + live-ipc + live-scale + live-procs-syscall + live-wait + live-sleep + live-entropy + live-reboot-shutdown + live-net-tx + live-net-rx + live-net-arp + live-net-icmp + live-net-udp + live-net-udp-syscall + live-net-nat + live-net-dhcp + live-net-dhcp-renew + live-net-dhcp-autonomous + live-net-tcp + live-net-tcp-rto + live-screen + live-text + live-roadpops + live-glyphs + live-xhci + live-usb + live-input + live-input-depth + live-win + live-win-syscall + live-win-close + live-win-move + live-editing + live-help + live-win-hig (Apple silicon only) |
| `console` | C | interactive | yes | no | yes | `zig build console` — interactive host serial console; needs a human at the keyboard |
| `vz-irq-api-audit` | D | diagnostic | no | no | yes | `bash tools/audit-vz-irq-api.sh` — read-only selected-Xcode SDK audit of Virtualization.framework and Hypervisor.framework GIC surfaces; claim 9187; no VM boot |
| `preexit-tx` | D | diagnostic | no | no | yes | `bash tools/verify-preexit-tx.sh` (mechanism: `zig build preexit-tx`) — claim 0017 |
| `tx-diag` | D | diagnostic | no | no | yes | `bash tools/verify-tx-diag.sh` (mechanism: `zig build tx-diag`) — claim 0018 |
| `tx-transition` | D | diagnostic | no | no | yes | `bash tools/verify-tx-transition.sh` — claim 0020 |
| `fw-mmu-capture` | D | diagnostic | no | no | yes | `bash tools/verify-fw-mmu-capture.sh` — claim 0021 |
| `t0sz25` | D | diagnostic | no | no | yes | `bash tools/verify-t0sz16.sh` (mechanism: `zig build kernel -Dt0sz25`) — claims 6460/1517 (legacy start-level regression) |
| `walk-probe` | D | diagnostic | no | no | yes | `zig build kernel -Dwalk-probe` (cold-address probe battery, `M2_WP_*` markers) — claims 7896/1517 |
| `t0sz16-walkprobe` | D | diagnostic | no | no | yes | `bash tools/verify-t0sz16-walkprobe.sh` — claims 7896/1517 (start-level/residual separation + production regression matrix) |

Notes:

- **Raw build steps are classed with their gate.** `zig build marker`,
  `zig build nvram-console`, `zig build preexit-tx`, `zig build tx-diag`,
  `zig build bad-handoff` are the mechanisms behind their verify scripts and
  boot VZ VMs, so they carry the same class (B or D) and the same Apple
  silicon requirement. `zig build kernel` and `zig build bad-handoff` build
  artifacts only (no VM) and are class-A tooling, not gates.
- **Tooling that is not a verification gate:** `ragshit`, `just impact`,
  `just refresh-indexes` are developer tooling, not gates.
- **Class A is exactly what GitHub CI proves.** CI also builds the Swift
  runner, but a successful build is not a boot; only class-B runs on Apple
  silicon produce hardware-gate evidence.
- **`swift-spike-build` runs on the `xcode-27` public-preview runner.**
  GitHub announced the Xcode 27 image (arm64, macOS 26 OS with Xcode 27 /
  the macOS 27.0 SDK) on 2026-07-16. It is the only GitHub-hosted way to
  compile the macOS-27-only custom-virtio spike (claim 5844); the
  macos-latest job builds the base runner against the macOS 26 SDK. The
  spike check boots no VM, so it stays class A despite needing the newest
  SDK.

## Machine-readable records

Fixed-width prefix, `cmd=` is the last field and may contain spaces. A
preflight can extract records with
`sed -n '/^<!-- GATE_INVENTORY:START -->$/,/^<!-- GATE_INVENTORY:END -->$/p'`
and filter on `class=…`, `ci=…`, `apple=…`, `gate=…`.

<!-- GATE_INVENTORY:START -->
GATE id=fmt class=A kind=gate ci=yes apple=no gate=yes cmd=zig fmt --check boot/src/*.zig kernel/src/*.zig build.zig
GATE id=unit-tests class=A kind=gate ci=yes apple=no gate=yes cmd=bash tools/verify-unit-tests.sh
GATE id=glyph-raster class=A kind=gate ci=yes apple=no gate=yes cmd=bash tools/verify-glyph-raster.sh  # claim 9358 (issue 125 hardening): the font8x8 LSB-first raster convention as a NAMED class-A gate — the three glyph-bearing module tests (font8x8 fingerprint + asymmetry census; text.zig + driving_award.zig full-95-glyph round-trips against the RAW table bits), the offline in-cell mirror self-test, and the MUTATION CHECK (delegated to verify-mutations.sh)
GATE id=mutations class=A kind=gate ci=yes apple=no gate=yes cmd=bash tools/verify-mutations.sh  # claim 3485 (issue 125 hardening): the generalized bit-order MUTATION CHECK — a manifest of the kernel's bit-order-sensitive seams (font8x8.row_pixel reversal; FAT read_le -> big-endian, write_le untouched so the write path still emits the little-endian fixtures the tests compare against; virtio free_chain walking the WRITE bit instead of the NEXT bit). Each mutation is applied, the named module tests MUST FAIL, the file is restored. Design rule: a mutation must flip the READING of a bit/byte, never a shared constant both sides read (self-consistent mutations are not valid checks — the glyph lesson). A golden that cannot detect its mutation fails the gate
GATE id=transcript-mock class=A kind=gate ci=yes apple=no gate=yes cmd=zig build test-console
GATE id=guest-build class=A kind=gate ci=yes apple=no gate=yes cmd=zig build
GATE id=image-build class=A kind=gate ci=yes apple=no gate=yes cmd=zig build image
GATE id=inspect class=A kind=gate ci=yes apple=no gate=yes cmd=zig build inspect
GATE id=swift-runner-build class=A kind=gate ci=yes apple=no gate=yes cmd=swift build --package-path host/vm-runner
GATE id=swift-spike-build class=A kind=gate ci=yes apple=no gate=yes cmd=swift build --package-path host/vm-runner -Xswiftc -DSPIKE  # xcode-27 public-preview runner (macOS 27 SDK); claim 5844
GATE id=context class=A kind=gate ci=yes apple=no gate=yes cmd=zig build context
GATE id=coordination class=A kind=gate ci=yes apple=no gate=yes cmd=bash tools/verify-coordination.sh
GATE id=coordination-tooling class=A kind=gate ci=yes apple=no gate=yes cmd=bash tools/status/test-coordination.sh
GATE id=mmu-debt class=A kind=gate ci=yes apple=no gate=yes cmd=bash tools/verify-mmu-debt.sh
GATE id=verify-portable class=A kind=aggregate ci=no apple=no gate=no cmd=just verify-portable
GATE id=bad-handoff class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-bad-handoff.sh
GATE id=marker class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-marker.sh
GATE id=nvram-console class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-nvram-console.sh
GATE id=host-console-pty class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-host-console.sh
GATE id=serial-takeover class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=zig build run
GATE id=live-transcript-rx class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-transcript.sh
GATE id=live-help class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-help.sh  # milestone-eight card U1 (claim 3275): ADR 0008 help walk
GATE id=live-exceptions class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-exceptions.sh
GATE id=live-timer class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-timer.sh
GATE id=live-tasks class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-tasks.sh
GATE id=live-userspace class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-userspace.sh
GATE id=live-svc class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-svc.sh
GATE id=live-uaccess class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-uaccess.sh  # claim 6120: EFAULT contract + fault recovery
GATE id=live-addrspaces class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-addrspaces.sh  # claim 5804: per-task TTBR0 + EL1-only kernel overlay, MMIO excluded from EL0 (VZ TTBR1 fallback)
GATE id=live-lifecycle class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-lifecycle.sh  # claim 6729: user task lifecycle — spawn / exit / reap + explicit states + idle task
GATE id=live-exec class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-exec.sh  # claim 6783: load + exec a user program from the ESP at EL0
GATE id=live-procs class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-procs.sh  # claim 3848: process abstraction — procs table + process exit report above the task pool
GATE id=live-args class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-args.sh  # claim 4636 (card 3e): exec args to EL0 — same binary, distinct argv (user: arg=alpha / arg=beta), read-only 8×32 B block in the program's own text page, argc/argv via entry-contract x0/x1, both programs live + pool_full third exec, EXACT report counts
GATE id=live-concurrent class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-concurrent.sh  # claim 0826 + card 3d (claim 1014): two live user processes — exec twice without exit, distinct roots/stacks/tasks, markers interleaving, EXACT exit/reap report counts (FIFO)
GATE id=live-long-lived class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-long-lived.sh  # claim 4613 + card 3d (claim 1014): a long-lived process among live peers — never-exiting COUNTER.BIN, USER.BIN exit/reap/re-exec, pages returned, pool_full with both live (two-phase script via --script2), EXACT report counts
GATE id=live-kill class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-kill.sh  # claim 7786: the kernel owns process lifetime — kill a never-exiting COUNTER.BIN (reserved status 137, no markers after the kill line, exact +5 page recovery, re-exec into the freed slot; three-phase script via --script3)
GATE id=live-ipc class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-ipc.sh  # claim 5965 (card 3f): IPC — two never-exiting processes exchange data through the bounded per-process mailbox (slots 5/6) — ipc: ping N sends interleave with byte-exact peer: got ping N echoes, mbox drained (pending<=1, sent-recv==pending), both still running, fifth exec pool_full at 7/7 (two-phase script via --script2)
GATE id=live-scale class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-scale.sh  # claim 5795 (card 3g): pool-scale capstone — FOUR live user programs at the 7-slot budget (counter + 3 USER.BINs, distinct tasks + stacks), markers interleave with the worker, fifth exec pool_full, addrspaces tables=150/256 inside the carve-out
GATE id=live-procs-syscall class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-procs-syscall.sh  # claim 5799 (card 4a): process observability — PEER.BIN reads the process table FROM EL0 via sys_procs (slot 7) — peer: sees 2 COUNTER.BIN running (the counter visible from EL0), the exited boot row too, ipc flow still echoes, both never-exiting (two-phase script via --script2)
GATE id=live-wait class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-wait.sh  # claim 9946 (card 4c): exit-status propagation — STATUS43.BIN sleeps 6 ticks then exits 43; COUNTER.BIN blocks in sys_wait (slot 8) and observes ipc: saw pid=1 status=43, with a phase-2 snapshot of TWO blocked user-exec tasks while the target's procs row still reads running (two-phase script via --script2)
GATE id=live-entropy class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-entropy.sh  # claim 2665: virtio entropy -> CSPRNG seed -> random command (2 boots, non-deterministic)
GATE id=live-reboot-shutdown class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-reboot.sh
GATE id=live-fs class=B kind=gate ci=no apple=yes gate=yes status=pass cmd=bash tools/verify-live-fs.sh  # claim 6420: FAT32 storage driver (persistence on the disk, not NVRAM)
GATE id=live-gfs class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-gfs.sh  # claim 3678: general (non-ESP) filesystem — data partition mounted by GUID, persistent across boots
GATE id=live-net-tx class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-tx.sh  # claim 1373 (milestone five, card N1): virtio-net transport + TX — runner --net attaches the net device (fixed host MAC); the guest discovers DID 0x1041, negotiates VER1|MTU|MAC (device needs MTU), reads the MAC via the feature path, arms queues 0/1, re-arms post-exit (no EBS reset — pre-rearm st=0f), prepends the 12-byte virtio_net_hdr (observed), and netsend's frames land in the host capture byte-exactly (46-byte fixture, ring reuse, 1500-byte truncation)
GATE id=live-net-rx class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-rx.sh  # claim 6076 (milestone five, card N2): virtio-net RX + the round trip — the runner's --net-inject writes the --net attachment's socket end once on the guest's net: rx-armed serial trigger (host->guest; OFF by default); the guest supplies queue 0 with one fixed BSS buffer, drains the used ring polled (used-buffer IRQ unobserved — recorded, not assumed), MAC-filters (own + broadcast, drop the rest with a counter) into a bounded 4-slot frame FIFO, and net recv prints the frame byte-exact; phase 1 broadcast round trip (recv byte-exact + the guest re-sends — the capture is byte-exactly the injected fixture), phase 2 own-MAC received byte-exact, phase 3 foreign-MAC dropped (filtered=1, rx-obs still records the delivery); 3/3 PASS. Claim-time observations: the device writes a 12-byte virtio_net_hdr into RX buffers (num_buffers=1) and refuses an RX buffer under 1530 bytes
GATE id=live-net-arp class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-arp.sh  # claim 7293 (milestone five, card N3): ARP live on VZ over the N2 RX seam — kernel/src/arp.zig (static IP, byte-exact request/reply builds, bounded 4-slot table, counters) wired into the RX drain + net arp [<ip>] + the runner's --net-arp-respond <host-ip> (deterministic host-side answer; OFF by default); phase 1 the guest answers the injected request for its IP (42-byte reply byte-exact in the capture, repl=1), phase 2 the guest resolves 10.0.0.2 (request byte-exact in the capture; the host answer lands as 10.0.0.2 -> 02:00:00:00:00:02, learn=1), phase 3 a request for 10.0.0.99 is dropped (drop=1, repl=0, still observable via net recv); 3/3 PASS. Claim-time observation: the device delivers/transmits the 42-byte ARP frames unpadded (below the Ethernet 60-byte minimum)
GATE id=live-net-icmp class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-icmp.sh  # claim 0148 (milestone five, card N4): IPv4/ICMP live on VZ over the N2 RX seam BESIDE the ARP dispatch — kernel/src/ipv4.zig (RFC 1071 checksums, byte-exact ICMP echo request/reply builders, fragments dropped counted — no reassembly) wired into the RX drain (answer echo requests for our static IP, observe echo replies — pong + seq, drop the rest) + net ping <a.b.c.d> (needs the peer in the ARP table — an echo has a unicast dst) + the runner's --net-icmp-respond <host-ip> (deterministic host-side echo answer; OFF by default); phase 1 the guest answers the injected 46-byte echo request for its IP (reply byte-exact in the capture with the identification + id/seq/payload echoed, repl=1, net recv observes the request at device len 58), phase 2 the guest resolves 10.0.0.2 and pings it (the ARP request + the 46-byte echo request id/seq 1 are byte-exact in the capture; the host answer lands as pong=1 with seq=1), phase 3 an echo request for 10.0.0.99 is dropped (drop=1, repl=0, still observable via net recv); 3/3 PASS. No new hardware-contract entry — the 46-byte frames travel unpadded, consistent with the N3 observation
GATE id=live-net-udp class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-udp.sh  # claim 8552 (milestone five, card N5): UDP live on VZ over the N4 IPv4 seam — kernel/src/udp.zig (RFC 768: the 8-byte header, the checksum over the IPv4 pseudo-header computed ALWAYS, a bounded 4-slot LISTEN table, bounded per-listener datagram rings, LOOPBACK — a send to our own IP delivers locally, no device round trip) wired into ipv4.zig's protocol dispatch (protocol 17 -> udp on already-validated frames) + net udp [listen|close|send|recv] + the runner's --net-udp-respond <host-ip>:<host-port> (deterministic host-side echo answer; OFF by default); phase 1 LOOPBACK: a send to our own IP is delivered byte-exact (rx=1 tx=1 loop=1) with an EMPTY capture, phase 2 the host's injected datagram is delivered to the listener byte-exact (device len 58), phase 3 the guest resolves 10.0.0.2 and sends to it (the ARP request + the 46-byte datagram are byte-exact in the capture; the host answer lands in the listener buffer, rx=1 tx=1), phase 4 a datagram to a closed port is dropped (drop=1, no delivery, still observable via net recv); 4/4 PASS. Claim-time fix recorded: the pseudo-header zero/protocol word was initially reversed (0x1100 vs 0x0011) — caught by the byte-exact fixtures, fixed. No new hardware-contract entry — the 46-byte datagrams travel unpadded, consistent with the N3/N4 observation
GATE id=live-net-udp-syscall class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-udp-syscall.sh  # claim 1384 (milestone five, card N6): the UDP syscall seam live on VZ — ADR 0007 slots 9/10/11 (sys_udp_listen/sys_udp_send/sys_udp_recv, implemented 9 -> 12) driven end to end by UDP.BIN (the first network-syscall user program): listen(7000) -> udp: listen ok; the LOOPBACK send+recv to our own IP -> udp: loop ping (12-byte datagram, byte-exact); the peer send to 10.0.0.2:9999 + poll recv of the host's --net-udp-respond answer -> udp: got ping (cooperative yield between polls; the recv drains the device first — the claim-6076 polled-drain contract); the EINVAL mapping from EL0 (unbound-port recv + unresolved-peer send -> udp: recv err -1 / udp: send err -1); sys_exit(17) -> procs UDP.BIN exited status=17 / tasks user-exec exited status=17 / tasks user-exec reaped; the capture is byte-exact (42-byte ARP request + 46-byte datagram); phase 2 observes syscalls rows 0-11 (implemented=12) + net udp/net counters rx=2 tx=2 loop=1 drop=0; 4/4 PASS. Gate-engineering lessons recorded: the expect is keyed on the program's OWN completion markers (udp: got ping / tasks user-exec reaped) — an early expect killed the VM at ~5 s before the ring returned to the program after its yield and a healthy kernel looked hung; the marker greps carry || true so an absent marker fails the gate instead of killing it under set -euo pipefail
GATE id=live-net-nat class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-nat.sh  # claim 4678 (milestone five, card N7): outbound connectivity through VZNATNetworkDeviceAttachment live on VZ — the runner's --net-nat attaches the NAT device (mutually exclusive with --net, OFF by default); NO guest code — the existing stack is driven against the NAT gateway and the gate asserts GUEST-OBSERVED COUNTERS (the capture-file shape does not apply through NAT); observed: the NAT attachment honors the configured locally-administered MAC (mac=02:00:00:00:00:01 source=feature), the subnet/gateway is 192.168.64.0/24/.1 (observed, VZ exposes no prefix API), the gateway answers ARP (gateway MAC learned, learn=1) + ICMP echo (pong=1 seq=1, no internet), the NAT router MAC varies per boot (gate asserts the learned-line prefix), the router sends IPv6 multicast at boot (N2 MAC filter drops, filtered=3), off-subnet addresses are not proxy-ARP'd (the guest honestly refuses); one run, 11/11 assertions PASS
GATE id=live-net-dhcp class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-dhcp.sh  # claim 0351 (milestone five, card N8): the bounded RFC 2131 DHCP client on the N5/N6 UDP layer live on VZ, TWO phases — phase 1 (deterministic file-handle, --net + --net-dhcp-respond 10.0.0.2): the full handshake against the crafted server (DISCOVER 286 B byte-exact in the capture -> OFFER -> REQUEST 298 B, the same xid -> ACK -> bound ip=10.0.0.2 mask=255.255.255.0 gw=10.0.0.1 server=10.0.0.2 lease=3600, counters discover=1,offer=1,request=1,ack=1,nack=0,timeout=0,mal=0, the host's NET-DHCP lines), 16/16 assertions; phase 2 (real NAT): the claim-time observation that the VZ NAT serves NO DHCP server on this host (the DISCOVER went out, offer=0, mal=0 — honestly recorded, never faked; saved log under artifacts/live-net-dhcp-nat-explore/), the static fallback still reaches the gateway (pong=1 seq=1)
GATE id=live-net-dhcp-renew class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-dhcp-renew.sh  # claim 9489 (milestone five, card N9) + audit follow-up 3 (issue #119): the DHCP lease lifecycle (RFC 2131 §4.4.5) live on VZ, REBUILT for the autonomous reality (the idle-loop poll drives T1/T2/expiry); run A (lease 100 s, --net-dhcp-respond-norenew): the poll RENEWs at T1 with a UNICAST REQUEST (byte-exact in the 1222-B capture, dst 02:00:00:00:00:02), the host REFUSES it, and at T2 the client ESCALATES to REBINDING (the new RENEWING->REBINDING rung) with a BROADCAST REQUEST whose ACK restarts the lease, counters renew=1,rebind=1,renewed=1,expired=0; run B (lease 100 s, --net-dhcp-respond-norebind): the REBINDING broadcast at T2 is REFUSED, at expiry the lease is RELEASED (dhcp=idle,ip=0.0.0.0,...,rebind=1,renewed=0,expired=1) and the client RECOVERS (a second DISCOVER -> BOUND again; the ciaddr==0 initial REQUEST is still answered); the refusal knobs are flag-gated (defaults unchanged)
GATE id=live-net-dhcp-autonomous class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-dhcp-autonomous.sh  # audit follow-up 3 (issue #119): the AUTONOMY PROOF live on VZ — the DHCP lease lifecycle advances from the shell idle loop with NO typed net dhcp. Lease 100 s + --net-dhcp-respond-norenew; phase 1 binds + resolves the server MAC; phase 2 (delay 92, past T2=87) types NO net dhcp (a marker + a net REPORT only). The poll must RENEW at T1=50 (unicast REQUEST, refused) and ESCALATE to REBINDING at T2=87 (broadcast REQUEST, ACKed). Assertions: bound lease, the renewing (T1) + rebinding (T2) transition lines, the report renew=1,rebind=1,renewed=1,expired=0, the host's refused-unicast line, the 1222-B capture, the premise self-check (no net dhcp in the phase-2 script), runner flags, serial markers — closing the audit finding that the N9 enforced lease only ran on demand
GATE id=live-net-tcp class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-tcp.sh  # claim 7026 (milestone five, card N10): the bounded RFC 793 TCP client live on VZ — run A (file-handle + --net-tcp-respond 10.0.0.2:9999 + --net-arp-respond 10.0.0.2): the full lifecycle byte-exact (SYN -> handshake ACK 0x12345679 -> established -> send 5 -> the echoed 01 02 03 04 05 -> FIN -> final ACK 0x1234567f -> closed, then a second connect + reset), counters syn=2,synack=2,ack=4,data_s=1,data_r=1,fin=1,finack=1,rst_s=1,rst_r=0,timedout=0,mal=0, the 533-B capture's NINE frames python-walked (the seq/ack chain + every TCP checksum); run B (ARP-responder-only black hole): the bounded 30 s connect timeout -> connect refused -> timedout=1; run C (real NAT): the claim-time observation — the VZ NAT gateway RSTs the SYN (rst_r=1, connection refused), recorded [observed], never faked; 36/36 assertions
GATE id=live-net-tcp-rto class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-net-tcp-rto.sh  # claim 5357 (milestone five, card N11): the bounded TCP retransmission + retransmit timer live on VZ — run A (file-handle, ARP-responder-only black hole): the idle-loop RTO poll retransmits the pending SYN autonomously (syn retransmitted (1/10)/(2/10)), retx=2,abort=0, the byte-identical SYN frames in the capture; run B (file-handle + the full responder): the SYN-ACK clears the pending state — retx=0 despite the wait, exactly ONE SYN, the handshake completes; run C (file-handle + the :handshake responder — SYN-ACK yes, data silent): send 5 (never ACKed) -> the data retransmitted TEN times -> retransmission limit reached (10) -> connection aborted (tcp=idle,retx=10,abort=1) -> no connection, the ELEVEN byte-identical data frames in the capture; the :handshake mode is flag-gated (default unchanged); 34/34 assertions
GATE id=live-screen class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-screen.sh  # claim 6053 (milestone six, card G1): virtio-gpu transport + framebuffer live on VZ — the runner's --display/--screenshot attach VZVirtioGraphicsDeviceConfiguration (1280x720 scanout; OFF by default); the guest's kernel/src/virtio_gpu.zig discovers the modern virtio-pci gpu (DID 0x1050, class 0x038000, dev 7; config layout common@+0x0000/ISR@+0x1000/notify@+0x4000 mult 4/devcfg@+0x8000), negotiates VER1-only (device offers RING_PACKED|RING_EVENT_IDX|RING_INDIRECT_DESC|VERSION_1), arms controlq 0 + cursorq 1 (size 4), re-arms post-exit (VZ resets the gpu at ExitBootServices — pre-rearm st=00 observed, like blk/entropy), and runs the spec 2D path GET_DISPLAY_INFO -> CREATE_2D (B8G8R8X8) -> ATTACH_BACKING (4K-aligned BSS framebuffer) -> SET_SCANOUT -> TRANSFER -> FLUSH; claim-time fixes: the 1.2 display_one (24-B pmodes — the 20-B shape wedged the queue with DEVICE_NEEDS_RESET), a next=0 tail descriptor (VZ walks it), cache cleans before the kick/transfer (MMU-on), and an OPAQUE alpha byte (X/A 0 renders fully transparent — the final black-screen bug). Scripted boot: screen (report) + screen fill 00ff00 + screen peek (guest-side bytes); the gate asserts the transport report (did/feat/scanout 1280x720/status 0x0f rearm=1 setup=1, cmds session-dynamic since G3 (errors=0 timeouts=0 is the health contract), fill transfer=ok flush=ok, peek p1=0xff) AND decodes the captured PNG itself — since G3 (Road Pops) the terminal renders over the raw fill, so the pixel phase asserts the NON-BLANK terminal frame (text region fg over bg; the fill is proven guest-side): THE FIRST NON-BLANK GUEST FRAMEBUFFER (G1's claim-time pixel moment). The capture MUST come from ScreenCaptureKit (the composited window; any cacheDisplay-fallback capture fails the gate). 1/1 PASS; evidence artifacts/live-screen-*, artifacts/gpu-screen-*s
GATE id=live-text class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-text.sh  # claim 3194 (milestone six, card G2): framebuffer text live on VZ — kernel/src/text.zig (public-domain 8x8 bitmap font, ASCII 0x20-0x7e, fixed BSS glyphs; putc/puts, cursor, wrap, bounded 128-line scrollback ring, clear; pure renderer host-tested against an injectable mock canvas — 21 tests incl. golden glyphs) paints the SAME banner + dipshit> prompt the serial log carries over G1's framebuffer (fg 0x00ff00 on bg 0x101418) through G1's transfer/flush unchanged; text/text put <string>/text clear (registry 35->36). Scripted boot: text (report); the gate asserts the serial evidence (text: boot banner presented — emitted by the Road Pops tee's first present since G3; the report's STABLE parts rows=90 cols=160 cell=8x8 + fg/bg — cur/lines are session-dynamic since the echoed session lands in the ring; the banner + prompt on serial, the shared seam) AND decodes the captured PNG itself: the banner region samples BOTH green-family foreground (fg=0.255) and dark background (bg=0.745) — the screen is no longer monochrome — with the region below all background. Honest bound (the G1 precedent): byte-exact glyphs live in the class A mock; the LIVE assertion is text visible with the expected color family (color-managed + retina-scaled). The capture MUST come from ScreenCaptureKit (the composited window; any cacheDisplay-fallback capture fails the gate). 1/1 PASS; evidence artifacts/live-text-*, artifacts/gpu-screen-*s
GATE id=live-roadpops class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-roadpops.sh  # claim 1574 (milestone six, card G3): ROAD POPS live on VZ — the boot terminal is on the screen. kernel/src/road_pops.zig is a TEE console: every byte still reaches serial FIRST (the shared seam; the transcript gates keep passing byte-identical) AND G2's text layer paints the same banner + prompt + every reply, drained ONE full-frame present per output batch by the shell idle loop; the G2 one-shot boot paint is replaced by the tee rendering the shell's OWN banner (its first present emits text: boot banner presented). Claim-time fix (claim-0015 redux, observed live): the Target struct literal was folded into .rodata with link-time &fn addresses — the tee's first write faulted at the link-time rp_text_put_bytes (esr=0x02000000 elr=0x14260); the Target is built in RAM now. roadpops command (registry 36->37: armed/dirty/presents). Scripted boot: echo ROADPOPS + uname + roadpops; the gate asserts the serial session (roadpops: armed target=fbtext, text: boot banner presented, roadpops: armed=1 dirty=[01] presents>=1, the banner + echo reply + uname replies — the shared seam) AND decodes the captured PNG: the banner region (fg=0.255) AND the terminal region below it (fg=0.124) both show green-family foreground over the dark background — a WORKING terminal, not a one-shot splash; the region below all background. The capture MUST come from ScreenCaptureKit (the composited window; any cacheDisplay-fallback capture fails the gate). 1/1 PASS; evidence artifacts/live-roadpops-*, artifacts/gpu-screen-*s
GATE id=live-glyphs class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-glyphs.sh  # issue #125 / claim 8742 MIRROR-REGRESSION TRIPWIRE: Daniel Hepper font8x8 source rows are LSB-left; both kernel rasterizers consume them through font8x8.row_pixel, while tools/decode-screen-glyphs.py independently normalizes each source row to the screen's MSB-left sampled bitstring. A hard-coded asymmetric C source/screen golden pins that conversion so the oracle cannot repeat the renderer's mistake. The matcher finds the green terminal grid (scored pitch+origin phase search; bright stroke core g>140 for the smoothed ScreenCaptureKit image), decodes it in both orientations, requires forward unknowns <= 2 and the banner + prompt, and requires the mirrored score to be decisively worse. It separately decodes the amber/navy Driving Award clock title and body in both orientations. The offline --self-test renders LSB-first, proves the asymmetric C convention, then mirrors every glyph in-cell and requires the tripwire to fire. Phase 0 requires ScreenCaptureKit. Observed 2026-08-14 after the correction: terminal forward 0/604 unknown glyphs versus 549/595 mirrored; clock title/body exactly 'clock' / 'DRIVING AWARD' versus 4/5 and 10/13 unknowns mirrored. Class-A hardening (claim 9263, 2026-08-15): full-95-glyph raster round-trips in both renderers read the RAW table bits inline (not row_pixel — mutation-tested: reversing the helper fails 90/95 glyphs), plus a pinned FNV-1a 64 table fingerprint and asymmetry census (90/95 asymmetric; symmetric set ' !*_|'). 1/1 PASS; evidence artifacts/live-glyphs-*, artifacts/gpu-screen-*s
GATE id=live-xhci class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-xhci.sh  # claim 4272 (milestone seven, card I1): XHCI host-controller transport (MMIO + command/event rings + NO-OP + port status) — the runner's --input attaches the keyboard/pointer (the Apple XHCI controller DID 0x1a06, NOT virtio-input); the guest discovers it pre-exit (bus 0 dev 8), maps BAR0 0x50001000 (cap regs) + BAR1 0x50000000, parses the register map (CAPLENGTH 0x20/VER 0x110/DBOFF 0x940/RTSOFF 0x520/HCSPARAMS1 0x10002010 = 16 slots/32 intrs/16 ports), sets up command+event rings+ERST+interrupter 0 (at RTSOFF+0x20 — the MFINDEX wedged-Emulation fix), drives a NO-OP to CC=1 (the ring-machinery proof), and reads port status; observed: VZ does NOT reset the controller at EBS (pre-reset USBSTS=0x9/USBCMD=0x0), USBSTS=0x0 running after HCRST+RS, ports 9+10 connected (CCS=1 — the two HID devices, the I2 handoff); the gate asserts the guest's own usb report (the card's gate-shape change — byte-exact host capture does not apply to a memory-mapped controller)
GATE id=live-usb class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-usb.sh  # claim 4116 (milestone seven, card I2): USB enumeration + HID over the XHCI transport — port reset -> Enable Slot -> Address Device -> descriptors -> Set Configuration -> interrupt-IN armed for BOTH devices (port 9/slot 1 = keyboard VID 0x05ac PID 0x8105, boot protocol, EP1-IN maxpkt 8, boot=1; port 10/slot 2 = absolute pointer VID 0x05ac PID 0x8106, protocol=0 — NOT a boot mouse — EP1-IN maxpkt 10, Set_Protocol(boot) honestly refused boot=0); the runner's minimal synthesized-key seam (--input-key 0 + --input-key-after "usb: enumerated" — VZ has no programmatic keyboard API, one synthesized NSEvent keyDown into the VZVirtualMachineView) produces the observed 8-byte report 00 00 04 00 00 00 00 00 (mod 0, HID usage 0x04 = 'a'); the gate asserts the guest's own usb devices + usb report lines (no host-side byte-exact capture applies to a memory-mapped controller)
GATE id=live-input class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-input.sh  # claim 6050 (milestone seven, card I3): scripted keystrokes drive Road Pops — kernel/src/input.zig (bounded pure-BSS event FIFO + HID-usage -> ASCII keymap + shell-idle drain) decodes the XHCI interrupt-IN reports into bytes that feed the Road Pops tee's read path; the runner's --input-string synthesizes one NSEvent per keyDown/keyUp (2 s per keystroke — VZ delivers ~one report per Road Pops present cadence) and types input\n; the gate asserts the guest's own input report (events=6 = i,n,p,u,t,Enter, dropped=0, kb-usage=0x28 kb-byte=0xa = Enter) — the typed command ran end to end. SUPERSEDED observation (2026-08-15, issue #117): "single-TRB arming is correct" rested on the PRE-U2 wrap bug; the depth-8 re-test is live-input-depth (claim 7302)
GATE id=live-input-depth class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-input-depth.sh  # claim 7302 (audit follow-up 2, issue #117): multi-TRB interrupt-IN depth-8 re-test on real VZ — the keyboard types echo fastok <Enter> input <Enter> at 2.0 s/chord (the measured VZ keyboard delivery ceiling; runner knob --input-chords-delay, default 3.0 s) and the gate asserts fastok echoed exactly once + the typed input report dropped=0 events=18 + kb-usage=0x28 kb-byte=0xa + serial marker + runner flags. Claim-6050's single-TRB conclusion is superseded on both grounds: its multi-TRB experiment ran on the PRE-U2 arm (read one past the buffer at the Link wrap, claim 1809) and the fast-typing ceiling is VZ's own delivery model (state-based keyboard, ~one report per present; <=1.0 s/chord delivers NOTHING — no guest arming depth raises the steady-state rate). Depth-8 = correct XHCI shape + burst buffering
GATE id=live-editing class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-editing.sh  # milestone-eight card U2 (claim 1809): ADR 0008 D2 line editor on real VZ — the runner's --input-chords seam synthesizes one NSEvent per chord (printable chars + Up/Left arrows, 3 s spacing) into the VZVirtualMachineView, input.zig's hid_to_bytes decodes the arrows to ESC sequences, and lineedit.zig recalls/edits them: the gate types echo ab <Left> c <Enter> <Up> <Enter> echo u2done <Enter> and asserts the guest's own echo output (acb x2 = mid-line insert + Up recall re-ran it; u2done x1 = the runner's exit marker) over the XHCI transport end to end
GATE id=live-win-hig class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-win-hig.sh  # claim 0935 (milestone eight card U5, ADR 0008 D4): the focus ring + title bars visible and moving with focus — win cycle + exec WINLOOP.BIN + a marker capture; scale-aware pixel asserts (ring on the focused window, terminal edge not ringed, the title bar). PASS 8/8
GATE id=live-win class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-win.sh  # claim 1543 (milestone six, card G5): Driving Award, the window manager — kernel/src/driving_award.zig (bounded fixed-BSS window registry, z-order = array order, focus by id, topmost-window hit-test, dirty-rect compositor repainting from the lowest dirty window up) makes Road Pops window 0 (the full-screen terminal) and a 1 Hz clock overlay window 1 (960,16 304x192, amber title bar + navy body, own BSS back-buffer); the G3 tee's present routes through the compositor and the I3 keyboard read source is gated on terminal_focused(); win/win focus/win raise/win hit (registry 39->40); 9 host tests pin the hit-test/raise/focus/repaint/blit contracts. The gate asserts the serial session (win: windows=2 focused=0, the two win[] rows, win hit 1000,100 -> 1 focusing the clock, win hit 100,400 -> 0 re-focusing the terminal) + a keyboard-typed uname landing in the focused terminal (DipshitOS aarch64), AND decodes the captured PNG: the clock's amber title bar + navy body over the terminal (no terminal foreground inside the clock rect — z-order) with the terminal's green glyphs beside it; evidence artifacts/live-win-*, artifacts/gpu-screen-*s
GATE id=live-win-syscall class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-win-syscall.sh  # claim 0487 (milestone six, card G6): the draw/window syscall seam + per-process ownership — ADR 0007 slots 12/13/14 (sys_win_open/sys_win_fill/sys_win_present, implemented 12 -> 15) expose the G5 window manager's user-window surface to EL0, with a window OWNED by its opening process and AUTO-CLOSED on exit (close_owner from the scheduler's exit path; fill/present/close owner-restricted); WIN.BIN (the first graphics user program) opens a bounded window (id 2..3, fixed BSS back-buffer <= 256x192 B8G8R8X8), fills it (dark-blue + red/cyan/white blocks), presents it, and exits 87 — its window auto-closes (win windows=2, sys_win_close calls=0); WINLOOP.BIN (the eighth program) then keeps its window alive (yield-forever) for the decoded-capture pixel proof (win windows=3 + win[2]: user user rect=64,64,256,192, syscalls implemented=21 with open=2/fill=8/present=2/close=0; the win report's owner column shows WINLOOP's pid (win[2] owner=2, fixed windows owner=-) and the win list <pid> filter returns matches=1 for pid 2 vs matches=0 for pid 0); evidence artifacts/live-win-syscall-*, artifacts/gpu-screen-*s
GATE id=live-win-close class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-win-close.sh  # claim 0487 teardown follow-on (milestone six, card G6): the draw/window RELEASE proof — WINCLOSE.BIN (the seventh graphics user program) opens/fills/presents/CLOSES a user window through ADR 0007 slot 15 entirely from EL0 (exit 88), twice; win shows windows=2 after the close (window gone, no win[2]: row) and the re-exec re-opens id 2 (the freed slot reused, never id 3); syscalls implemented=21 with open=1/close=1 in the pre-re-exec snapshot; evidence artifacts/live-win-close-*
GATE id=live-win-move class=B kind=gate ci=no apple=yes gate=yes cmd=bash tools/verify-live-win-move.sh  # claim 0487 move/raise follow-on (milestone six, card G6): the draw/window MOVE/RESTACK + HIDE/SHOW proof — WINMOVE.BIN (the ninth graphics user program) opens/fills/presents/moves (the second move clamps to the scanout corner)/raises/reads-back (sys_win_get, slot 18, printing winmove: get 1024,528,256,192)/queries (sys_win_query, slot 19, printing winmove: query 1024,528,256,192 z=2 focused=1 visible=1 dirty=1)/hides-then-shows (sys_win_set_visible, slot 20, printing winmove: hide ok + winmove: show ok) through ADR 0007 slots 16/17/18/19/20 entirely from EL0 (implemented 16 -> 21), then yield-loops forever; win shows win[2]: user user rect=1024,528,256,192 owner=<pid> visible=1, syscalls reports implemented=21 with open=1/fill=4/present=3/move=2/raise=1/get=1/query=1/set_visible=2/close=0, the EL1h win move 2 1024 528 + win raise 2 halves, the marker-driven capture (--screenshot-after "winmove: hide ok") shows NO red/cyan/white blocks at the clamped spot (the pixel DISAPPEARED), and the LATEST decoded capture shows the window's colors BACK at the NEW position with the terminal where it used to be; evidence artifacts/live-win-move-*, artifacts/gpu-screen-*
GATE id=verify-vz class=B kind=aggregate ci=no apple=yes gate=no cmd=just verify-vz  # serial takeover + bad-handoff + marker + nvram-console + host-console + live-transcript + live-fs + live-gfs + live-exceptions + live-timer + live-tasks + live-userspace + live-svc + live-uaccess + live-addrspaces + live-lifecycle + live-exec + live-procs + live-concurrent + live-args + live-long-lived + live-kill + live-ipc + live-scale + live-procs-syscall + live-wait + live-sleep + live-entropy + live-reboot-shutdown + live-net-tx + live-net-rx + live-net-arp + live-net-icmp + live-net-udp + live-net-udp-syscall + live-net-nat + live-net-dhcp + live-net-dhcp-renew + live-net-dhcp-autonomous + live-net-tcp + live-net-tcp-rto + live-screen + live-text + live-roadpops + live-glyphs + live-xhci + live-usb + live-input + live-input-depth + live-win + live-win-syscall + live-win-close + live-win-move
GATE id=console class=C kind=interactive ci=no apple=yes gate=yes cmd=zig build console
GATE id=vz-irq-api-audit class=D kind=diagnostic ci=no apple=yes gate=no cmd=bash tools/audit-vz-irq-api.sh
GATE id=preexit-tx class=D kind=diagnostic ci=no apple=yes gate=no cmd=bash tools/verify-preexit-tx.sh
GATE id=tx-diag class=D kind=diagnostic ci=no apple=yes gate=no cmd=bash tools/verify-tx-diag.sh
GATE id=tx-transition class=D kind=diagnostic ci=no apple=yes gate=no cmd=bash tools/verify-tx-transition.sh
GATE id=fw-mmu-capture class=D kind=diagnostic ci=no apple=yes gate=no cmd=bash tools/verify-fw-mmu-capture.sh
GATE id=t0sz25 class=D kind=diagnostic ci=no apple=yes gate=no cmd=bash tools/verify-t0sz16.sh
GATE id=walk-probe class=D kind=diagnostic ci=no apple=yes gate=no cmd=zig build kernel -Dwalk-probe
GATE id=t0sz16-walkprobe class=D kind=diagnostic ci=no apple=yes gate=no cmd=bash tools/verify-t0sz16-walkprobe.sh
<!-- GATE_INVENTORY:END -->

## Known flakes (evidence registry)

`artifacts/` is gitignored, so per-run evidence is regenerable by re-running
its gate — EXCEPT timing-dependent flakes, whose captures cannot be
regenerated on demand. Record every known flake here (append-only; a fix
closes the row with a pointer). A future agent hitting the same symptom can
then tell "documented pre-existing flake" from "new regression".

| Flake | Gate(s) | Symptom | First seen / record | Status |
|-------|---------|---------|---------------------|--------|
| NAT ARP-learn latency | `live-net-tcp` run C (also affects `live-net-nat` runs) | the guest's ARP request for the NAT gateway occasionally times out under live-NAT latency (`learn=0` / an ARP-learn assertion fails) | 2026-08-12, claim 5357 (N11): reproduced on clean main, recorded as not-an-N11-regression; the original capture dir `artifacts/live-net-tcp-arp-flake-baseline/` was local-only and LOST to `.gitignore` — this row is the surviving record (issue #123) | open — see issue #123; re-record evidence under a tracked path when next hit |

A machine-readable prefix (`FLAKE id=... gate=... status=...`) may be added
here alongside the table if a preflight ever needs to parse it; today the
table is the record (docs only, no tooling depends on it).
